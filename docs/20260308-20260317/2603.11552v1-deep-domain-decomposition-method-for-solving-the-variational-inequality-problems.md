---
title: Deep Domain Decomposition Method for Solving the Variational Inequality Problems
title_zh: 求解变分不等式问题的深度区域分解方法
authors: "Yiyang Wang, Qijia Zhou, Shengyuan Deng, Chenliang Li"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11552v1"
tags: ["query:pinn"]
score: 9.0
evidence: 将PINN与区域分解法结合求解椭圆变分不等式
tldr: 本研究提出了一种深度领域分解方法，用于解决椭圆变分不等式问题。该方法结合了物理信息神经网络（PINN）与领域分解技术，通过Ritz变分法将问题转化为优化问题，并在子域中使用Ritz-PINN求解。引入残差自适应训练策略以提升复杂区域的学习效果。实验表明，该方法具有高精度（MSE达1.0e-07）且迭代次数与网格长度无关，展现了良好的收敛性和鲁棒性。
selection_source: fresh_fetch
motivation: 旨在结合深度学习与传统数值方法，提高求解复杂椭圆变分不等式问题的效率和精度。
method: 将变分不等式转化为优化问题，利用Ritz-PINN在各子域并行求解，并采用残差自适应数据集更新策略。
result: 数值实验显示均方误差可降至1.0e-07，且在均匀重叠条件下，迭代次数不受网格长度影响。
conclusion: 该深度领域分解方法为解决变分不等式提供了一种高效、高精度且具有良好扩展性的数值方案。
---

## 摘要
通过将物理信息神经网络 (PINN) 技术与区域分解方法相结合，本文提出了一种用于求解椭圆变分不等式问题的深度区域分解方法。基于 Ritz 变分法，首先将椭圆变分不等式问题重新表述为一个优化问题，然后利用 Ritz-PINN 方法求解每个子区域中的子问题，其中网络参数通过 Adam 优化器进行更新，并引入残差自适应数据集更新策略进行残差自适应训练，以逐步引导模型学习更复杂的区域。此外，本文还探讨了重叠区域对新算法性能的影响。数值结果证明了所提算法的有效性，均方误差可达到 1.0e-07，且在均匀重叠条件下，迭代次数与网格长度 h 无关。

## Abstract
By integrating physics-informed neural network (PINN) techniques with domain decomposition method, a deep domain decomposition method is presented for solving elliptic variational inequality problems. Based on the Ritz variation method, the elliptic variational inequality problem is firstly reformulated as an optimization problem, and then the subproblem in each subdomain is solved by using the Ritz-PINN method, which the parameters in the network are updated by the Adam optimizer, and the residual-adaptive training by introducing a residual-adaptive dataset update strategy to gradually guide the model to learn more complex regions. Additionally, the impact of overlapping regions on the performance of the new algorithm is explored. Numerical results demonstrate the effectiveness of the proposed algorithm, the mean square error can be reached 1.0e-07, and the number of iterations is independent of grid length h under uniform overlap conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Deep Domain Decomposition Method for Solving the Variational Inequality Problems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文旨在解决**椭圆变分不等式（Elliptic Variational Inequality）**问题的数值求解难题。
*   **研究动机**：传统的数值优化方法在处理非线性、高维及复杂约束时计算成本高。虽然物理信息神经网络（PINN）在求解偏微分方程（PDE）方面表现出色，但在处理大区域和多尺度问题时存在精度不足和效率低下的瓶颈。
*   **核心目标**：通过将 PINN 与**区域分解法（Domain Decomposition Method, DDM）**结合，提出一种“深度区域分解方法（Deep DDM）”，以提高求解变分不等式问题的精度、收敛速度及对大规模问题的处理能力。

### 2. 方法论
核心思想是将全局复杂的变分不等式问题分解为多个子域上的简单优化问题，并利用改进的 PINN 进行并行求解。
*   **Ritz 变分转化**：利用 Ritz 变分法将椭圆变分不等式转化为等价的泛函最小化问题（优化问题）。
*   **区域分解**：将计算域 $\Omega$ 划分为 $N$ 个重叠子域。在每个子域内独立构建并训练一个 Ritz-PINN 模型。
*   **损失函数设计**：损失函数包含四个部分：
    1.  **区域内部损失**：基于 Ritz 泛函的积分项。
    2.  **边界条件损失**：强制满足 Dirichlet 或其他边界条件。
    3.  **子域接口匹配损失**：确保相邻子域在重叠区域的信息交换与连续性。
    4.  **变分不等式约束损失**：通过惩罚项（如 $\max(-u, 0)$）强制满足 $u \ge 0$ 的约束。
*   **关键技术细节**：
    *   **Adam 优化器**：用于更新网络参数。
    *   **残差自适应数据集更新策略**：动态调整训练点，使模型聚焦于残差较大的复杂区域。
    *   **贝叶斯优化**：用于自动优化损失函数中各项权重的超参数。

### 3. 实验设计
*   **实验场景**：论文设计了一个典型的二维椭圆变分不等式问题，其解析解已知，用于验证算法准确性。
*   **Benchmark 与对比**：
    *   对比了不同网格精细度（$h$ 从 0.05 到 0.001）下的表现。
    *   对比了不同重叠区域大小（$\delta$ 取 $h, 2h, 0.1, 0.2$）对迭代次数和计算时间的影响。
    *   以解析解（Analytical Solution）作为基准进行误差分析。

### 4. 资源与算力
论文明确列出了实验所使用的硬件资源：
*   **CPU**：192 核，主频 1130.5 MHz。
*   **GPU**：**NVIDIA RTX 4090D (24GB)**。
*   **训练设置**：每个子域迭代 1000 次，学习率默认为 0.001，网络架构为每层 32 个单元，激活函数为 ReLU。

### 5. 实验数量与充分性
*   **实验规模**：论文通过两组核心表格（迭代次数表和计算时间表）展示了在 24 种不同参数组合（$h$ 与 $\delta$ 的交叉组合）下的实验结果。
*   **充分性评价**：实验设计较为客观，涵盖了从“小重叠”到“固定比例重叠”的多种情况。通过 3D 误差图、2D 误差分布图以及 MSE、MAE、相对 L2 误差等多个指标进行了全面评估。但实验仅针对一个特定的二维算例，缺乏在更高维或更复杂几何形状下的泛化性验证。

### 6. 主要结论与发现
*   **高精度**：该方法在求解变分不等式时表现出极高的精度，均方误差（MSE）可达到 **$1.49 \times 10^{-7}$**。
*   **收敛独立性**：在**均匀重叠（Consistent Overlap）**条件下，算法的迭代次数与网格长度 $h$ 无关，表现出良好的数值稳定性。
*   **重叠区影响**：在小重叠情况下，迭代时间随 $h$ 减小而增加；但在固定比例重叠情况下，计算效率相对稳定。
*   **有效性**：残差自适应策略能有效引导模型学习复杂区域，提升了整体训练效果。

### 7. 优点
*   **结合优势**：成功将传统数值分析中的区域分解思想与现代深度学习优化技术融合。
*   **约束处理**：通过 Ritz-PINN 框架优雅地处理了变分不等式中的不等式约束。
*   **自适应性**：引入残差自适应采样和贝叶斯权重优化，减少了人工调参的负担，提高了模型的鲁棒性。

### 8. 不足与局限
*   **算例单一**：仅展示了一个二维凸域上的算例，未在非凸域、三维空间或更复杂的非线性变分不等式上进行测试。
*   **计算开销**：虽然 DDM 理论上支持并行，但论文未详细讨论在大规模分布式系统上的加速比。
*   **激活函数限制**：实验中使用了 ReLU，对于某些高阶导数敏感的 PDE 问题，ReLU 可能不如 Tanh 或 Sin 等平滑激活函数。

（完）
