---
title: Wiener Chaos Expansion based Neural Operator for Singular Stochastic Partial Differential Equations
title_zh: 基于维纳混沌展开的奇异随机偏微分方程神经算子
authors: "Dai Shi, Luke Thompson, Andi Han, Peiyan Hu, Junbin Gao, José Miguel Hernández-Lobato"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08219v1"
tags: ["query:pinn"]
score: 10.0
evidence: 用于奇异随机偏微分方程的神经算子
tldr: 本研究针对奇异随机偏微分方程（SPDE），如动态 Φ⁴₂ 和 Φ⁴₃ 模型，提出了一种基于维纳混沌展开（WCE）的神经算子改进模型 WCE-FiLM-NO。该方法通过引入特征线性调制（FiLM）机制，有效捕捉了奇异解与其平滑余项之间的依赖关系。实验证明，该模型在无需重整化因子的情况下，在 Φ⁴₂ 模型上表现优异，并成功应用于更具挑战性的 Φ⁴₃ 模型，为统计量子场论提供了高效的数据驱动代理模型。
selection_source: fresh_fetch
motivation: 旨在解决奇异随机偏微分方程在数值模拟中难以捕捉奇异解与平滑部分复杂依赖关系的问题。
method: 提出 WCE-FiLM-NO 模型，利用特征线性调制（FiLM）将维纳混沌展开特征融入神经算子中。
result: 在 Φ⁴₂ 模型上取得了极低的相对 L2 误差和优异的分布外泛化性能，且无需重整化处理。
conclusion: 该研究为动态 Φ⁴₃ 等复杂奇异 SPDE 提供了一个高效且具有潜力的通用数据驱动模拟框架。
---

## 摘要
在本文中，我们探讨了最近开发的基于维纳混沌展开（WCE）的神经算子（NO）如何应用于奇异随机偏微分方程（SPDE），例如近期工作中模拟的动态 $\boldsymbolΦ^4_2$ 模型。与以往通过简单地将 Wick-Hermite 特征插入骨干 NO 模型来求解 SPDE 的 WCE-NO 不同，我们利用特征线性调制（FiLM）来恰当地捕捉奇异 SPDE 的解与其光滑余项之间的依赖关系。由此产生的 WCE-FiLM-NO 在 $\boldsymbolΦ^4_2$ 上表现出优异的性能，这体现在相对 $L_2$ 损失、分布外 $L_2$ 损失和自相关得分上；且所有这些均无需重整化因子的辅助。此外，我们还展示了模拟 $\boldsymbolΦ^4_3$ 数据的潜力，这更符合统计量子场论中的实际科学实践。据我们所知，这是开发动态 $\boldsymbolΦ^4_3$ 模型高效数据驱动代理模型的首批工作之一。

## Abstract
In this paper, we explore how our recently developed Wiener Chaos Expansion (WCE)-based neural operator (NO) can be applied to singular stochastic partial differential equations, e.g., the dynamic $\boldsymbolΦ^4_2$ model simulated in the recent works. Unlike the previous WCE-NO which solves SPDEs by simply inserting Wick-Hermite features into the backbone NO model, we leverage feature-wise linear modulation (FiLM) to appropriately capture the dependency between the solution of singular SPDE and its smooth remainder. The resulting WCE-FiLM-NO shows excellent performance on $\boldsymbolΦ^4_2$, as measured by relative $L_2$ loss, out-of-distribution $L_2$ loss, and autocorrelation score; all without the help of renormalisation factor. In addition, we also show the potential of simulating $\boldsymbolΦ^4_3$ data, which is more aligned with real scientific practice in statistical quantum field theory. To the best of our knowledge, this is among the first works to develop an efficient data-driven surrogate for the dynamical $\boldsymbolΦ^4_3$ model.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **WCE-FiLM-NO** 的新型神经算子模型，专门用于解决具有挑战性的**奇异随机偏微分方程（Singular SPDEs）**。该模型在“SPDE 建模竞赛 2”中获得了奖项。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：奇异 SPDE（如量子场论中的 $\Phi^4_d$ 模型）在数学上极难处理，因为其非线性项在时空白噪声驱动下是未定义的，通常需要复杂的**重整化（Renormalization）**程序和精细的数值离散。
*   **研究动机**：
    *   传统的神经算子（如 FNO）在处理奇异 SPDE 时往往无法收敛。
    *   现有的随机神经算子（如 NSPDE）虽然能处理部分随机性，但在奇异性极高的情况下表现未知，且往往依赖于预先计算的重整化因子。
    *   旨在开发一种高效、数据驱动的代理模型，能够捕捉奇异解与其平滑余项之间的复杂依赖关系。

### 2. 方法论
*   **核心思想**：结合**维纳混沌展开（WCE）**理论与**特征线性调制（FiLM）**机制。
*   **关键技术细节**：
    *   **维纳混沌展开（WCE）**：将 SPDE 的解表示为 Wick-Hermite 基函数（Wick features）的线性组合。论文提取了高达 3 阶的 Wick 特征，这些特征包含了噪声的结构信息。
    *   **Da Prato-Debussche 分解 (DPDD)**：将奇异解 $u$ 分解为随机卷积部分 $X$（奇异部分）和平滑余项 $v$（平滑部分）。模型的目标是学习求解关于 $v$ 的“偏移方程（Shift Equation）”。
    *   **WCE-FiLM-NO 架构**：
        1.  使用 FNO（傅里叶神经算子）作为骨干网络，输入 Wick 特征来预测平滑余项 $\hat{v}$。
        2.  引入 **FiLM 机制**：通过一个简单的卷积网络 $g_\theta$ 处理 Wick 特征、空间坐标和时间，生成缩放因子 $\gamma$ 和偏移因子 $\tau$。
        3.  **仿射变换**：最终预测值通过 $\hat{u} = (1 + \gamma) \odot \hat{v} + \tau$ 得到。这种设计允许模型在不显式输入重整化因子的情况下，动态调整奇异部分与平滑部分的关系。

### 3. 实验设计
*   **数据集**：动态 $\Phi^4_2$ 模型（基于 SPDEBench 数据集）。
*   **Benchmark 与对比方法**：
    *   对比基准：**NSPDE**（Neural Stochastic PDE）。
    *   评估场景：设置了四种不同的输入方案（是否包含初始条件 $u_0$、是否包含重整化因子 $a_\epsilon$）。
    *   **跨截断评估（Cross-truncation）**：在噪声谱截断 $\epsilon=2$ 上训练，在 $\epsilon=128$ 上测试，以验证模型的泛化能力。
*   **前瞻性实验**：展示了更复杂的 $\Phi^4_3$ 模型的模拟初步结果。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。考虑到模型基于 FNO 和简单的 2D 卷积，其计算开销通常低于传统的迭代式奇异 SPDE 求解器。

### 5. 实验数量与充分性
*   **实验规模**：主要针对 $\Phi^4_2$ 模型在不同噪声截断（$\epsilon=2, 128$）和不同输入组合下进行了对比实验（见表 1）。
*   **充分性评价**：
    *   **优点**：包含了关键的跨分布（Out-of-distribution）泛化测试，这对于奇异 SPDE 尤为重要。
    *   **不足**：实验主要集中在 $\Phi^4_2$ 上，对于 $\Phi^4_3$ 仅提供了模拟展示，缺乏量化的性能对比。此外，未对比 NORS 和 DLR-Net 等其他针对奇异 SPDE 的最新模型。

### 6. 主要结论与发现
*   **性能优异**：WCE-FiLM-NO 在所有设置下均优于 NSPDE，尤其是在相对 $L_2$ 误差方面。
*   **无需重整化因子**：该模型在不使用显式重整化因子 $a_\epsilon$ 的情况下，依然能取得极佳效果，证明了 FiLM 机制捕捉奇异性的有效性。
*   **泛化能力强**：在跨截断评估中，模型表现出比 NSPDE 更好的鲁棒性（例如在 $W \to u$ 任务中，跨截断误差显著低于对比方法）。

### 7. 优点（亮点）
*   **架构创新**：巧妙地将 FiLM 机制引入神经算子，解决了奇异解分解中各部分耦合的难题。
*   **物理启发**：模型设计紧密结合了 WCE 理论和 DPDD 分解，具有较强的数学支撑。
*   **实用性**：摆脱了对重整化因子的依赖，简化了数据预处理流程。

### 8. 不足与局限
*   **基准对比有限**：未与所有最新的奇异 SPDE 专用模型进行对比。
*   **$\Phi^4_3$ 尚不成熟**：对于三维 $\Phi^4_3$ 模型，目前仅处于初步原型阶段，尚未实现完整的重整化项（如 sideband 修正），且缺乏深度学习训练结果。
*   **超参数敏感性**：论文未详细讨论 Wick 特征阶数（K）的选择对性能和计算成本的具体影响。

（完）
