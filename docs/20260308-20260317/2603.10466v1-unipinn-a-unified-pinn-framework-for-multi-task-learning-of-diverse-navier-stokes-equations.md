---
title: "UniPINN: A Unified PINN Framework for Multi-task Learning of Diverse Navier-Stokes Equations"
title_zh: UniPINN：一种用于多种纳维-斯托克斯方程多任务学习的统一 PINN 框架
authors: "Dengdi Sun, Jie Chen, Xiao Wang, Jin Tang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10466v1"
tags: ["query:pinn"]
score: 10.0
evidence: 提出了一种统一的PINN框架，用于解决多流场场景下的纳维-斯托克斯偏微分方程。
tldr: 针对物理信息神经网络（PINN）在处理多流场场景时面临的物理规律提取难、任务间负迁移及训练不稳定等挑战，本文提出了UniPINN统一框架。该框架通过共享-专用架构、跨流注意力机制和动态权重分配策略，实现了对多种纳维-斯托克斯方程的高效多任务学习，在显著提升异构流场预测精度的同时，有效解决了多任务学习中的干扰问题。
selection_source: fresh_fetch
motivation: 现有PINN方法主要针对单一流场设计，在处理多流场任务时难以兼顾通用物理规律与特定流场特征，且存在负迁移和训练不稳定的问题。
method: 提出UniPINN框架，集成共享-专用架构以解耦特征、跨流注意力机制以抑制干扰，以及动态权重分配策略以平衡多目标优化。
result: 在三种典型流场上的实验证明，UniPINN在预测精度和性能平衡方面均优于现有方法，并成功缓解了任务间的负迁移现象。
conclusion: UniPINN为多任务流体力学建模提供了一个统一且稳健的框架，展示了在单一模型中高效学习多种异构流场的能力。
---

## 摘要
物理信息神经网络 (PINNs) 在求解不可压缩纳维-斯托克斯 (Navier-Stokes) 方程方面展现出巨大潜力，但现有方法主要针对单流场设置而设计。当扩展到多流场场景时，这些方法面临三个关键挑战：(1) 难以同时捕捉共享的物理原理和特定流场的特征；(2) 容易受到任务间负迁移的影响，从而降低预测精度；(3) 异构流态下迥异的损失量级导致训练动力学不稳定。为了解决这些局限性，我们提出了 UniPINN，这是一个统一的多流场 PINN 框架，集成了三个互补组件：一个将通用物理定律与流场特定特征解耦的共享-专用架构；一个在抑制任务无关干扰的同时选择性增强相关模式的跨流场注意力机制；以及一个自适应平衡损失贡献以稳定多目标优化的动态权重分配策略。在三种典型流场上的广泛实验表明，UniPINN 有效地统一了多流场学习，在成功缓解负迁移的同时，在异构流态下实现了卓越的预测精度和平衡的性能。本文的源代码将发布在 https://github.com/Event-AHU/OpenFusion。

## Abstract
Physics-Informed Neural Networks (PINNs) have shown promise in solving incompressible Navier-Stokes equations, yet existing approaches are predominantly designed for single-flow settings. When extended to multi-flow scenarios, these methods face three key challenges: (1) difficulty in simultaneously capturing both shared physical principles and flow-specific characteristics, (2) susceptibility to inter-task negative transfer that degrades prediction accuracy, and (3) unstable training dynamics caused by disparate loss magnitudes across heterogeneous flow regimes. To address these limitations, we propose UniPINN, a unified multi-flow PINN framework that integrates three complementary components: a shared-specialized architecture that disentangles universal physical laws from flow-specific features, a cross-flow attention mechanism that selectively reinforces relevant patterns while suppressing task-irrelevant interference, and a dynamic weight allocation strategy that adaptively balances loss contributions to stabilize multi-objective optimization. Extensive experiments on three canonical flows demonstrate that UniPINN effectively unifies multi-flow learning, achieving superior prediction accuracy and balanced performance across heterogeneous regimes while successfully mitigating negative transfer. The source code of this paper will be released on https://github.com/Event-AHU/OpenFusion

---

## 论文详细总结（自动生成）

### UniPINN：用于多种纳维-斯托克斯方程多任务学习的统一 PINN 框架

#### 1. 核心问题与整体含义（研究动机和背景）
物理信息神经网络（PINNs）在求解流体力学中的偏微分方程（PDEs）方面表现出色，但现有研究大多局限于**单任务学习**。在面对包含多种流态（如不同边界条件、几何形状和物理参数）的复杂场景时，传统 PINN 面临三大挑战：
*   **特征解耦难**：难以在捕捉通用物理规律（如质量/动量守恒）的同时，保留特定流场的细节特征（如边界层、涡结构）。
*   **负迁移效应**：不同流场任务之间可能存在干扰，导致联合训练时的精度反而低于独立训练。
*   **训练不稳定**：异构流场的损失函数量级差异巨大，容易引发“梯度病态”问题，使模型优化陷入局部最优或发散。

**UniPINN** 的提出旨在构建一个统一的框架，通过单一模型同时学习并准确预测多种不同的纳维-斯托克斯（Navier-Stokes）流场。

#### 2. 方法论：核心思想与关键技术
UniPINN 采用了“共享-专用”的解耦架构，主要包含以下三个核心组件：
*   **任务共享骨干网络（Task-Shared Backbone）**：使用多层感知机（MLP）提取通用的物理算子特征。输入包括时空坐标 $(x, y, t)$ 和任务嵌入向量（Task Embedding），旨在学习所有流场共有的基础物理模式。
*   **跨流注意力机制（Cross-flow Attention）**：
    *   **自注意力（Self-Attention）**：强化各流场内部的长程依赖和关键物理特征。
    *   **跨注意力（Cross-Attention）**：通过 Query-Key-Value 机制，让当前流场能够从其他流场中选择性地吸取相关的物理知识（如相似的涡演化模式），同时过滤无关干扰。
    *   **特征融合**：通过可学习参数平衡自身特征与跨流场信息。
*   **动态权重分配（DWA）策略**：
    *   实时监控各流场任务的损失下降率（Relative Improvement Rate）。
    *   利用 Softmax 函数和温度参数 $\tau$ 动态调整各任务的损失权重，优先关注收敛较慢的“困难任务”。
    *   引入指数移动平均（EMA）机制平滑权重波动，确保训练稳定性。

#### 3. 实验设计
*   **实验场景**：选取了三种经典的不可压缩层流：
    1.  **顶盖驱动流（Lid-driven Cavity Flow）**：封闭域，剪切驱动。
    2.  **管道流（Poiseuille Pipe Flow）**：开放域，压力驱动，具有抛物线速度剖面。
    3.  **库埃特流（Couette Flow）**：平行板间流，粘性驱动。
*   **Benchmark**：使用 PDEBench 产生的高保真数值解作为 Ground Truth。
*   **对比方法**：
    *   **基础模型**：线性回归、高斯过程回归（GPR）、纯数据驱动 DNN。
    *   **PINN 变体**：标准 PINN、LAAF-PINN（自适应激活函数）、KIH-PINN（知识集成）、AL-PINN（基于注意力）、MMPDE-Net。

#### 4. 资源与算力
论文提到实验得到了**安徽大学高性能计算平台**的支持。然而，文中**未明确列出**具体的 GPU 型号（如 A100 或 V100）、GPU 数量以及每个实验的具体训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括：
    *   **定量对比**：在三个流场上分别计算速度和压力的均方误差（MSE）。
    *   **定性分析**：绘制流场流线图，验证物理结构的还原度。
    *   **消融实验**：逐一移除注意力机制、输入归一化和 DWA 策略，验证各组件的贡献。
    *   **迁移能力实验**：测试了预训练模型在未知流场上的微调效果。
    *   **负迁移深度分析**：通过强行复用不匹配的任务特征，反向验证了注意力机制在抑制负迁移中的必要性。
*   **充分性评价**：实验设计较为全面，涵盖了从精度到稳定性、再到泛化能力的完整闭环，对比基准涵盖了近年来主流的 PINN 改进方案，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **精度领先**：UniPINN 在所有测试流场中的 MSE 均为最低，相比次优方法，误差降低了 24.4% 至 62.5%。
*   **成功抑制负迁移**：通过跨流注意力机制，模型能够识别任务间的物理相似性，避免了异构任务间的相互干扰。
*   **高效迁移**：预训练的共享骨干网络能显著降低新任务的初始损失（降低 98% 以上），加速收敛。
*   **稳定性**：DWA 策略有效平衡了不同边界条件下的梯度冲突，使多任务联合训练过程平滑且不发散。

#### 7. 优点与亮点
*   **架构优雅**：通过共享 Backbone 和专用 Attention Head 的组合，实现了物理通用性与任务特异性的平衡。
*   **物理一致性**：在单一模型中实现了对闭口流和开口流的统一建模，且流线图显示其严格遵循纳维-斯托克斯方程的约束。
*   **动态调权**：提出的 DWA 策略解决了 PINN 多目标优化中常见的梯度病态问题，具有较强的工程参考价值。

#### 8. 不足与局限
*   **维度与流态限制**：目前仅在 **2D 层流**上进行了验证，尚未扩展到更复杂的 3D 几何体或高雷诺数下的湍流模型。
*   **算力开销未详述**：虽然模型参数量可能比训练多个独立网络更少，但跨流注意力机制在处理大规模空间点时的计算复杂度（通常为平方级）未做详细讨论。
*   **应用范围**：目前仅针对不可压缩单相流，对于可压缩流、多相流或流固耦合等更复杂的物理系统，其有效性尚待验证。

（完）
