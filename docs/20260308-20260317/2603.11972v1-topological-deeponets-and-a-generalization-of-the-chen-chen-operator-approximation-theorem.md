---
title: Topological DeepONets and a generalization of the Chen-Chen operator approximation theorem
title_zh: 拓扑 DeepONets 与 Chen-Chen 算子逼近定理的推广
authors: Vugar Ismailov
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11972v1"
tags: ["query:pinn"]
score: 10.0
evidence: 深度算子网络 (DeepONets) 的拓扑扩展
tldr: 本研究针对算子神经网络（DeepONets）在处理非Banach空间输入时的局限性，提出了一种拓扑DeepONet架构。通过将输入空间扩展到任意Hausdorff局部凸空间，并利用对偶空间中的连续线性泛函构建分支网络，该研究证明了拓扑DeepONet能够一致逼近定义在紧集上的连续算子。这一成果将经典的Chen-Chen算子逼近定理从连续函数空间推广到了更广泛的局部凸空间，为处理更复杂的物理和数学问题提供了理论基础。
selection_source: fresh_fetch
motivation: 传统的DeepONet逼近理论主要局限于Banach空间，无法直接应用于更广义的局部凸空间。
method: 引入拓扑前馈神经网络，利用对偶空间中的线性泛函作为分支网络输入，并结合处理欧几里得域的躯干网络构建拓扑DeepONet。
result: 证明了定义在Hausdorff局部凸空间紧子集上的连续算子可以被拓扑DeepONet一致逼近。
conclusion: 该研究成功推广了Chen-Chen算子逼近定理，为在非Banach空间框架下应用深度算子网络提供了严谨的数学证明。
---

## 摘要
深度算子网络 (DeepONets) 提供了一种分支-主干 (branch-trunk) 神经架构，用于逼近作用于函数空间之间的非线性算子。在经典的算子逼近框架中，输入是定义在紧集 $K_1$（通常是 Banach 空间的紧子集）上的函数 $u \in C(K_1)$，而算子将 $u$ 映射到定义在紧欧几里得区域 $K_2 \subset \mathbb{R}^d$ 上的输出函数 $G(u) \in C(K_2)$。在本文中，我们开发了一种拓扑扩展，其中算子输入位于任意 Hausdorff 局部凸空间 $X$ 中。我们利用对偶空间 $X^*$ 中的连续线性泛函在 $X$ 上构建了拓扑前馈神经网络，并引入了拓扑 DeepONets，其分支组件通过此类线性测量作用于 $X$，而主干组件则作用于欧几里得输出域。我们的主定理表明，连续算子 $G:V \to C(K; \mathbb{R}^m)$（其中 $V \subset X$ 且 $K \subset \mathbb{R}^d$ 为紧集）可以通过此类拓扑 DeepONets 进行一致逼近。这不仅将经典的 Chen-Chen 算子逼近定理从连续函数空间扩展到了局部凸空间，还产生了一个超越 Banach 空间范畴的分支-主干逼近定理。

## Abstract
Deep Operator Networks (DeepONets) provide a branch-trunk neural architecture for approximating nonlinear operators acting between function spaces. In the classical operator approximation framework, the input is a function $u\in C(K_1)$ defined on a compact set $K_1$ (typically a compact subset of a Banach space), and the operator maps $u$ to an output function $G(u)\in C(K_2)$ defined on a compact Euclidean domain $K_2\subset\mathbb{R}^d$. In this paper, we develop a topological extension in which the operator input lies in an arbitrary Hausdorff locally convex space $X$. We construct topological feedforward neural networks on $X$ using continuous linear functionals from the dual space $X^*$ and introduce topological DeepONets whose branch component acts on $X$ through such linear measurements, while the trunk component acts on the Euclidean output domain. Our main theorem shows that continuous operators $G:V\to C(K;\mathbb{R}^m)$, where $V\subset X$ and $K\subset\mathbb{R}^d$ are compact, can be uniformly approximated by such topological DeepONets. This extends the classical Chen-Chen operator approximation theorem from spaces of continuous functions to locally convex spaces and yields a branch-trunk approximation theorem beyond the Banach-space setting.

---

## 论文详细总结（自动生成）

这篇论文《Topological DeepONets and a generalization of the Chen-Chen operator approximation theorem》是一篇纯理论性的数学物理与机器学习交叉研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的深度算子网络（DeepONet）及其核心理论基础——Chen-Chen 算子逼近定理，主要建立在输入空间为连续函数空间或 Banach 空间（完备赋范线性空间）的假设上。然而，在偏微分方程（PDE）和分布理论中，许多重要的空间（如 Schwartz 空间 $\mathcal{S}(\mathbb{R}^n)$ 或测试函数空间 $\mathcal{D}(U)$）是**局部凸空间（LCS）**，它们通常是不可赋范的。
*   **研究动机**：作者旨在打破 Banach 空间的限制，将 DeepONet 的架构和通用逼近理论推广到更广义的 Hausdorff 局部凸空间，从而为处理更复杂的物理系统和抽象数学对象提供理论支撑。

### 2. 论文提出的方法论
*   **核心思想**：引入“拓扑神经网络”概念，利用局部凸空间的对偶空间 $X^*$ 中的**连续线性泛函**来代替传统神经网络中的权重向量。
*   **关键技术细节**：
    *   **拓扑前馈神经网络**：定义为 $H(x) = A \sigma(T(x))$，其中 $T(x)$ 是由连续线性泛函 $f_i \in X^*$ 构成的映射。这意味着网络通过“线性测量”来感知输入。
    *   **拓扑 DeepONet 架构**：
        *   **分支网络 (Branch)**：作用于局部凸空间 $X$，通过有限个连续线性泛函提取输入 $u$ 的特征。
        *   **主干网络 (Trunk)**：作用于欧几里得域 $K \subset \mathbb{R}^d$，通常采用标准的脊函数（Ridge functions）或深度神经网络。
        *   **组合方式**：通过分支和主干输出的内积（或矩阵乘法）实现算子 $G(u)(y)$ 的分离表示。
*   **主要定理**：证明了对于定义在局部凸空间紧子集 $V$ 上的连续算子 $G$，只要激活函数 $\sigma$ 是 Tauber-Wiener 函数，拓扑 DeepONet 就能在 $V \times K$ 上实现一致逼近。

### 3. 实验设计
*   **实验性质**：本论文是一篇**纯数学证明论文**，不包含任何数值实验、代码实现或数据集测试。
*   **Benchmark 与对比**：论文的“基准”是经典的 Chen-Chen 算子逼近定理（1995）。作者通过逻辑推导证明了 Chen-Chen 定理仅是其新定理在 Banach 空间下的一个特例。
*   **应用示例**：作者提供了 6 个数学示例（如序列空间 $\ell^p$、勒贝格空间 $L^p$、Schwartz 空间等）来展示该理论如何涵盖从有限维到无限维、从赋范到不可赋范的各种场景。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何 GPU、CPU 或训练时长等资源信息。
*   **原因**：由于是纯理论证明，不涉及模型训练或推理。

### 5. 实验数量与充分性
*   **充分性评价**：从数学角度看，证明过程严谨且完备。作者通过对不同数学空间的分类讨论（见第 4 节），充分展示了该定理的普适性。
*   **客观性**：论文基于泛函分析和拓扑向量空间理论，其结论在数学逻辑上是客观且必然的。

### 6. 论文的主要结论与发现
*   **理论推广**：成功将 DeepONet 的通用逼近能力从 Banach 空间推广到了任意 Hausdorff 局部凸空间。
*   **传感器泛化**：在局部凸空间中，传统的“点采样传感器”（Point evaluations）被抽象为“连续线性泛函”。这解释了为什么 DeepONet 在处理不同类型的输入数据时具有鲁棒性。
*   **统一框架**：建立了一个统一的框架，将算子学习、分布理论和拓扑向量空间结合在一起。

### 7. 优点
*   **理论深度**：解决了算子学习领域一个长期的理论空白，即如何处理非赋范空间的输入。
*   **数学严谨性**：利用 Stone-Weierstrass 定理和 Hahn-Banach 定理等工具，给出了优雅且严密的证明。
*   **前瞻性**：为未来开发处理分布（Distributions）或广义函数的神经算子奠定了基础。

### 8. 不足与局限
*   **缺乏实证**：虽然理论上可行，但论文未讨论在不可赋范空间（如 Schwartz 空间）中如何实际参数化和训练这些线性泛函。
*   **紧致性限制**：逼近理论仍依赖于输入集 $V$ 的紧致性（Compactness），这在某些无限维应用场景中可能过于严苛。
*   **实现难度**：对于一般的局部凸空间，寻找合适的连续线性泛函基底在工程实践中可能非常困难。

（完）
