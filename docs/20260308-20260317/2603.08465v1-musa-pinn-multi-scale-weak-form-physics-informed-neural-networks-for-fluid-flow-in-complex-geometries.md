---
title: "MUSA-PINN: Multi-scale Weak-form Physics-Informed Neural Networks for Fluid Flow in Complex Geometries"
title_zh: MUSA-PINN：用于复杂几何形状中流体流动的多尺度弱形式物理信息神经网络
authors: "Weizheng Zhang, Xunjie Xie, Hao Pan, Xiaowei Duan, Bingteng Sun, Qiang Du, Lin lu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08465v1"
tags: ["query:pinn"]
score: 10.0
evidence: 用于流体流动的多尺度弱形式物理信息神经网络
tldr: "针对标准PINN在复杂几何结构（如TPMS）中因逐点残差最小化导致的收敛困难和物理守恒性差等问题，本文提出MUSA-PINN。该方法将PDE约束重构为分层球形控制体上的积分守恒定律，结合三尺度子域策略与两阶段训练方案，显著增强了复杂流道内的全局信息传播，在保持质量守恒的同时将相对误差降低了高达93%。"
selection_source: fresh_fetch
motivation: 标准PINN的逐点约束在拓扑复杂领域存在局部偏差，难以在曲折通道中传播全局信息，导致梯度不稳定和物理守恒律失效。
method: 提出多尺度弱形式框架，在分层球形控制体上强制执行通量平衡，并采用涵盖长程耦合、骨架感知及局部细化的三尺度策略。
result: "在TPMS几何结构的流体仿真实验中，MUSA-PINN相比现有方法显著提升了精度，相对误差最高降低93%并实现了优异的质量守恒。"
conclusion: 通过将积分守恒定律与多尺度子域策略相结合，MUSA-PINN为复杂几何体内的流体动力学模拟提供了一种鲁棒且高精度的无网格解决方案。
---

## 摘要
虽然物理信息神经网络 (PINNs) 为求解偏微分方程 (PDEs) 提供了一种无网格方法，但标准的逐点残差最小化在三重周期极小曲面 (TPMS) 等拓扑复杂的区域中面临收敛病态问题。逐点约束的局部偏差导致全局信息无法通过曲折通道传播，从而引起梯度不稳定和违反守恒律。为了解决这一问题，我们提出了多尺度弱形式 PINN (MUSA-PINN)，它将 PDE 约束重新表述为分层球形控制体上的积分守恒律。我们通过控制面上的通量平衡残差来强制执行连续性和动量守恒。我们的方法采用了一种三尺度子域策略——包括用于长程耦合的大体积、与传输路径对齐的骨架感知中尺度体积，以及用于局部细化的小体积——并配合优先考虑连续性的两阶段训练计划。在 TPMS 几何形状中的稳态不可压缩流实验表明，MUSA-PINN 优于最先进的基准模型，将相对误差降低了高达 93%，并保持了质量守恒。

## Abstract
While Physics-Informed Neural Networks (PINNs) offer a mesh-free approach to solving PDEs, standard point-wise residual minimization suffers from convergence pathologies in topologically complex domains like Triply Periodic Minimal Surfaces (TPMS). The locality bias of point-wise constraints fails to propagate global information through tortuous channels, causing unstable gradients and conservation violations. To address this, we propose the Multi-scale Weak-form PINN (MUSA-PINN), which reformulates PDE constraints as integral conservation laws over hierarchical spherical control volumes. We enforce continuity and momentum conservation via flux-balance residuals on control surfaces. Our method utilizes a three-scale subdomain strategy-comprising large volumes for long-range coupling, skeleton-aware meso-scale volumes aligned with transport pathways, and small volumes for local refinement-alongside a two-stage training schedule prioritizing continuity. Experiments on steady incompressible flow in TPMS geometries show MUSA-PINN outperforms state-of-the-art baselines, reducing relative errors by up to 93% and preserving mass conservation.

---

## 论文详细总结（自动生成）

以下是对论文《MUSA-PINN: Multi-scale Weak-form Physics-Informed Neural Networks for Fluid Flow in Complex Geometries》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在工业流体动力学（如高效热交换器设计）中，流道几何形状极其复杂（如TPMS结构）。传统的CFD方法依赖高质量网格，在参数化设计中成本高昂；而新兴的物理信息神经网络（PINNs）虽无需网格，但在处理此类复杂拓扑时表现不佳。
*   **核心痛点**：标准PINN依赖**逐点残差最小化（强形式）**。在长而曲折的通道中，这种局部约束难以传播全局物理信息，导致模型无法满足质量和动量守恒定律，出现梯度不稳定、物理不一致（如严重的质量泄漏）等收敛病态问题。
*   **研究目标**：提出一种能够强化全局物理耦合、保证守恒律且适用于复杂几何形状的无网格PINN框架。

### 2. 方法论
MUSA-PINN的核心是将PDE约束重构为**分层球形控制体上的积分守恒定律**。
*   **核心思想**：利用**高斯散度定理**，将体积内的PDE残差转化为控制体表面的通量平衡积分。这种“弱形式”表述不仅能提供更强的物理监督信号，还能降低对高阶导数计算的依赖。
*   **关键技术细节**：
    *   **多尺度子域策略（Multi-scale Subdomain）**：
        1.  **大尺度（Large）**：在全局范围内放置大半径球体，负责长程物理耦合，抑制全局漂移。
        2.  **中尺度（Medium）**：利用**几何骨架提取算法**（Mean Curvature Flow），沿流道中心线放置中等半径球体，确保约束信号沿传输路径传播。
        3.  **小尺度（Small）**：在内部随机采样小半径球体，用于细化局部流场细节和边界层特征。
    *   **两阶段训练计划**：
        *   **第一阶段（连续性预热）**：仅训练边界条件、强形式残差和弱形式连续性方程（质量守恒），快速建立合理的流场骨架。
        *   **第二阶段（全物理优化）**：激活弱形式动量守恒约束，进行精细化微调。
*   **算法流程**：通过蒙特卡洛采样在球形控制体表面取点，计算速度和压力的通量平衡，将其作为损失函数的一部分进行优化。

### 3. 实验设计
*   **实验场景**：四种典型的TPMS热交换器通道：Primitive (P), Gyroid (G), Diamond (D), 和 IWP。
*   **Benchmark**：使用商业软件 Ansys CFX 生成的高保真有限体积法（FVM）模拟结果作为 Ground Truth。
*   **对比方法**：
    *   **PINN-PE**：带位置编码的标准强形式PINN。
    *   **RoPINN (NeurIPS 2024)**：区域优化PINN。
    *   **CoPINN (ICML 2025)**：认知采样PINN。
    *   **MDPINN-GD (NeurIPS 2025)**：带全局动力学约束的多域PINN。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA A40 GPU**。
*   **软件框架**：PyTorch。
*   **训练时长**：
    *   第一阶段（7000次迭代）：约 7.05 小时。
    *   第二阶段（2000次迭代）：约 18.79 小时。
    *   **总计**：约 26 小时。相比之下，标准PINN-PE耗时约 10 小时。MUSA-PINN的计算开销主要源于表面积分的采样与计算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对4种不同拓扑的TPMS结构进行了系统性对比。
    *   进行了详细的**消融实验**，验证了不同尺度（大/中/小）和训练阶段对结果的影响。
    *   进行了**鲁棒性测试**，包括更高雷诺数（Re=200, 400）的实验。
    *   扩展到了**工业级案例**：液冷板（Liquid-cooling plate）和 DualMS 热交换器。
*   **评价**：实验设计非常充分且具有挑战性。对比的方法均为近两年顶会的最新成果，且在相同架构和优化器（SOAP）下进行公平对比，客观性强。

### 6. 主要结论与发现
*   **精度大幅提升**：在最复杂的 Diamond 和 IWP 结构中，基准方法基本失效（相对误差 > 90%），而 MUSA-PINN 将速度相对误差控制在 6%-12% 左右，**误差最高降低了 93%**。
*   **物理守恒性优异**：MUSA-PINN 几乎完美解决了质量泄漏问题，平均截面质量流率误差比最强基准降低了 **97.05%**。
*   **局部特征捕获**：能够清晰解析基准方法容易“洗掉”的次级流和边界层速度梯度。

### 7. 优点
*   **拓扑感知**：通过骨架感知采样（Skeleton-aware），解决了复杂曲折通道内约束覆盖不全的问题。
*   **数值稳定性**：弱形式积分显著增强了优化的稳定性，避免了强形式PINN在复杂几何中的收敛崩溃。
*   **无网格扩展性**：虽然引入了控制体，但依然保持了无网格特性，无需生成复杂的体网格。

### 8. 不足与局限
*   **计算成本**：训练时间显著高于标准PINN（约 2.5 倍），在实时性要求极高的场景下可能受限。
*   **参数依赖**：控制体的半径选择和多尺度比例目前仍基于启发式规则（Heuristic），可能需要针对不同几何形状进行手动调优。
*   **物理范围**：目前仅验证了稳态不可压缩流，尚未扩展到瞬态流、湍流或涉及能量方程的共轭传热问题。

（完）
