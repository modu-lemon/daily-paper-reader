---
title: Physics-informed fine-tuning of foundation models for partial differential equations
title_zh: 偏微分方程基础模型的物理信息微调
authors: "Vlad Medvedev, Leon Armbruster, Christopher Straub, Georg Kruse, Andreas Rosskopf"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15431v1"
tags: ["query:pinn"]
score: 10.0
evidence: 偏微分方程基础模型的物理信息微调
tldr: 针对偏微分方程（PDE）基础模型在下游任务中面临的数据稀缺和分布偏移挑战，本文提出了一种物理信息微调框架。该方法将物理约束（如PDE残差和边界条件）直接引入微调目标，使模型在无需大量标注数据的情况下实现高效适配。实验证明，该方法在未知PDE类别上表现优异，且混合微调策略在极少数据下具有极强的泛化能力，为科学机器学习提供了高效且可解释的适配路径。
selection_source: fresh_fetch
motivation: 现有的PDE基础模型在面对新任务时受限于标注数据匮乏和分布偏移，且物理信息在微调阶段的潜力尚未被充分挖掘。
method: 提出一种物理信息微调框架，通过在微调目标中加入PDE残差和边界条件等物理约束来优化预训练模型。
result: 该方法在无需PDE真实解的情况下达到了极具竞争力的精度，且混合微调策略在数据极少时展现出卓越的泛化性能。
conclusion: 物理信息微调是一种可扩展且数据高效的范式，为科学领域基础模型的适配提供了兼具物理一致性与解释性的新途径。
---

## 摘要
偏微分方程（PDE）基础模型已成为在多种物理系统上预训练的强大代理模型，但由于任务特定数据有限以及分布偏移，将其适配到新的下游任务仍具挑战性。虽然微调在自然语言处理领域已被证明具有变革性，但适配 PDE 基础模型的最佳实践仍有待探索。尽管物理信息训练已成功在广泛的 PDE 问题中训练出精确的求解器，但其在微调基于数据的基础模型方面的潜力尚未得到系统研究。在这项工作中，我们引入了一个物理信息微调框架，通过将物理约束（PDE 残差和边界条件）直接纳入微调目标，来适配预训练的 PDE 基础模型。这使得在数据稀缺的情况下能够进行有效的适配，同时促进物理一致性。我们在由未见过的 PDE 类别组成的下游任务上评估了我们的方法，并将其与数据驱动的微调对应方法进行了比较。我们的结果表明，物理信息微调在不需要 PDE 解作为训练数据的情况下，实现了具有竞争力的准确性。此外，当仅有极少量训练数据可用时，混合微调策略在分布外场景中表现出卓越的泛化能力。这些发现确立了物理信息微调作为一种可扩展且数据高效的范式，为科学机器学习中基础模型的适配提供了一条具有物理可解释性的途径。

## Abstract
Foundation models for partial differential equations (PDEs) have emerged as powerful surrogates pre-trained on diverse physical systems, but adapting them to new downstream tasks remains challenging due to limited task-specific data and distribution shifts. While fine-tuning has proven transformative in natural language processing, best practices for adapting PDE foundation models remain underexplored. Although physics-informed training has successfully trained accurate solvers across a wide range of PDE problems, its potential for fine-tuning data-based foundation models has not been systematically studied. In this work, we introduce a physics-informed fine-tuning framework that adapts pre-trained PDE foundation models by incorporating physical constraints (PDE residuals and boundary conditions) directly into the fine-tuning objective. This enables effective adaptation in data-scarce regimes while promoting physical consistency. We evaluate our method on a downstream task composed of an unseen PDE class and compare it with data-driven finetuning counterparts. Our results demonstrate that physics-informed fine-tuning achieves competitive accuracy without requiring PDE solutions for training. Furthermore, a hybrid fine-tuning strategy yields superior generalization to out-of-distribution scenarios when only minimal training data is available. These findings establish physics-informed fine-tuning as a scalable and data-efficient paradigm, providing a physically interpretable pathway for adapting foundation models in scientific machine learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Physics-informed fine-tuning of foundation models for partial differential equations》的结构化技术总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何高效地将预训练的偏微分方程（PDE）基础模型（如 POSEIDON）适配到全新的、未见过的物理任务中，特别是在标注数据（数值解）极度匮乏的情况下。
*   **研究背景**：虽然 PDE 基础模型在多物理场预训练中表现出色，但传统的微调（Fine-tuning）高度依赖于昂贵的数值模拟数据。此外，当输入分布发生偏移（Distribution Shift）时，纯数据驱动的模型往往难以保持物理一致性。
*   **整体含义**：本文首次系统性地研究了**物理信息微调（Physics-informed Fine-tuning, PIFT）**，通过将物理定律（PDE 残差）引入微调目标，实现了在少样本甚至零样本（无标注解）情况下的模型适配。

### 2. 方法论
*   **核心思想**：在微调阶段，不再仅仅依赖预测值与真实解之间的误差（Data Loss），而是引入 PDE 残差和边界条件（BC）作为约束（Physics Loss）。
*   **关键技术细节**：
    *   **损失函数构建**：总损失 $L = L_{data} + L_{physics}$。其中 $L_{physics}$ 包含 PDE 残差 $\mathcal{N}_f(u)$ 和边界残差 $\mathcal{B}_f(u)$。
    *   **导数计算**：由于模型输出通常是离散网格，论文采用**有限差分法（Finite Difference）**而非自动微分来计算 PDE 残差中的拉普拉斯算子等导数项。
    *   **边界条件强化**：通过对模型输出进行**零填充（Zero-padding）**，直接硬性强制执行齐次狄利克雷（Dirichlet）边界条件，避免了软约束的优化难题。
    *   **混合微调策略**：在微调过程中，实时生成未见过的源项（Source terms），仅利用物理残差进行监督，从而实现“在线”数据增强。

### 3. 实验设计
*   **下游任务场景**：
    1.  **二维泊松方程（Poisson Equation）**：基础模型预训练中未包含的稳态椭圆方程。
    2.  **二维亥姆霍兹方程（Helmholtz Equation）**：用于验证方法在波动问题上的通用性。
*   **数据集与分布**：
    *   **内插（Interpolation）**：测试集与训练集均采用高斯斑点（Gaussian blobs）分布。
    *   **外推（Extrapolation/OOD）**：测试集采用极端形状（如正弦波、棋盘格、螺旋线、随机线条等）。
*   **对比基准（Benchmarks）**：
    *   **POSEIDON-T**：21M 参数的基础模型（数据驱动微调 vs. 物理信息微调 vs. 混合微调）。
    *   **FNO（傅里叶神经算子）**：从零开始训练的对比模型（数据驱动 vs. 物理信息训练）。

### 4. 资源与算力
*   **硬件平台**：使用单张 **NVIDIA H200 GPU** (141 GB VRAM)。
*   **训练时长**：
    *   POSEIDON-T 微调：约 **37 分钟**。
    *   FNO 从零训练：约 **1 小时 1 分钟**。
*   **推理效率**：推理延迟约为 **1.6ms - 1.7ms**，基础模型在 FLOPs 上比 FNO 更具优势（约 4 倍减少）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了详尽的数据缩放实验（Data Scaling Curves），样本量 $M$ 从 1 跨越到 4096。
    *   针对内插和外推场景分别进行了定量评估。
    *   包含泊松方程和亥姆霍兹方程两个独立案例。
*   **充分性评价**：实验设计较为**客观且公平**。通过固定参数量（均为 21M）来对比基础模型与 FNO，并使用了相同的测试集和评估指标（相对 L1 误差和残差误差）。外推实验涵盖了多种几何形状，增强了结论的说服力。

### 6. 主要结论与发现
*   **零样本适配可行性**：物理信息微调允许模型在**完全没有标注解**的情况下学习新的 PDE 家族，且精度具有竞争力。
*   **少样本优势**：在极低数据量（如 $M=1$）下，混合微调策略的表现远超纯数据驱动方法，能够通过物理约束有效“填补”数据空白。
*   **泛化能力**：物理约束充当了“护栏”，使得模型在面对分布外（OOD）的极端输入时，预测结果更平滑、更符合物理逻辑，而纯数据驱动模型在此时往往失效。
*   **物理一致性**：虽然数据驱动微调在样本充足时像素级误差更低，但物理信息微调产生的解在 PDE 残差指标上表现更好，更符合底层物理规律。

### 7. 优点
*   **首创性**：首次将物理信息学习与 PDE 基础模型的微调相结合。
*   **数据效率**：极大地降低了适配新任务时对昂贵数值模拟数据的依赖。
*   **灵活性**：提出的混合微调框架支持实时生成无标签样本进行物理监督，具有很强的实用扩展性。

### 8. 不足与局限
*   **物理复杂度有限**：目前仅验证了稳态方程（泊松、亥姆霍兹），尚未涉及含时（Time-dependent）方程或更复杂的非线性多物理场耦合系统。
*   **超参数敏感性**：实验中直接沿用了 POSEIDON 的原始超参数，未针对物理损失项进行专门的加权优化，可能限制了性能上限。
*   **有限差分依赖**：依赖于结构化网格的有限差分计算残差，对于复杂几何边界的非结构化网格适配性有待进一步研究。

（完）
