---
title: "MUSA-PINN: Multi-scale Weak-form Physics-Informed Neural Networks for Fluid Flow in Complex Geometries"
title_zh: MUSA-PINN：用于复杂几何中流体流动的多尺度弱形式物理信息神经网络
authors: "Weizheng Zhang, Xunjie Xie, Hao Pan, Xiaowei Duan, Bingteng Sun, Qiang Du, Lin Lu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08465v2"
tags: ["query:pinn"]
score: 10.0
evidence: 用于复杂几何流体流动的多尺度弱形式PINN
tldr: "针对标准PINN在复杂几何结构（如TPMS）中因逐点残差最小化导致的收敛困难和物理守恒性差等问题，本文提出MUSA-PINN。该方法将PDE约束重构为分层球形控制体上的积分守恒定律，结合三尺度子域策略与两阶段训练方案，显著增强了复杂流道内的全局信息传播，在保持质量守恒的同时将相对误差降低了高达93%。"
selection_source: fresh_fetch
motivation: 标准PINN的逐点约束在拓扑复杂领域存在局部偏差，难以在曲折通道中传播全局信息，导致梯度不稳定和物理守恒律失效。
method: 提出多尺度弱形式框架，在分层球形控制体上强制执行通量平衡，并采用涵盖长程耦合、骨架感知及局部细化的三尺度策略。
result: "在TPMS几何结构的稳态不可压缩流实验中，MUSA-PINN显著优于现有基准，相对误差最高降低93%并实现了优异的质量守恒。"
conclusion: 通过将积分守恒定律与多尺度子域策略相结合，MUSA-PINN为复杂几何体中的流体动力学模拟提供了一种高精度且物理一致的无网格方案。
---

## 摘要
虽然物理信息神经网络 (PINNs) 为求解偏微分方程 (PDEs) 提供了一种无网格方法，但标准的逐点残差最小化在拓扑结构复杂的域（如三重周期最小表面，TPMS）中面临收敛病态问题。逐点约束的局部偏差无法通过曲折的通道传播全局信息，导致梯度不稳定和违反守恒律。为了解决这一问题，我们提出了多尺度弱形式 PINN (MUSA-PINN)，它将 PDE 约束重新表述为分层球形控制体上的积分守恒律。我们通过控制面上的通量平衡残差来强制执行连续性和动量守恒。我们的方法采用了一种三尺度子域策略——包括用于长程耦合的大体积、与传输路径对齐的骨架感知中尺度体积，以及用于局部细化的小体积——并配合优先考虑连续性的两阶段训练计划。在 TPMS 几何结构中的稳态不可压缩流实验表明，MUSA-PINN 优于最先进的基准模型，将相对误差降低了高达 93%，并保持了质量守恒。

## Abstract
While Physics-Informed Neural Networks (PINNs) offer a mesh-free approach to solving PDEs, standard point-wise residual minimization suffers from convergence pathologies in topologically complex domains like Triply Periodic Minimal Surfaces (TPMS). The locality bias of point-wise constraints fails to propagate global information through tortuous channels, causing unstable gradients and conservation violations. To address this, we propose the Multi-scale Weak-form PINN (MUSA-PINN), which reformulates PDE constraints as integral conservation laws over hierarchical spherical control volumes. We enforce continuity and momentum conservation via flux-balance residuals on control surfaces. Our method utilizes a three-scale subdomain strategy-comprising large volumes for long-range coupling, skeleton-aware meso-scale volumes aligned with transport pathways, and small volumes for local refinement-alongside a two-stage training schedule prioritizing continuity. Experiments on steady incompressible flow in TPMS geometries show MUSA-PINN outperforms state-of-the-art baselines, reducing relative errors by up to 93% and preserving mass conservation.

---

## 论文详细总结（自动生成）

这是一份关于论文《MUSA-PINN: Multi-scale Weak-form Physics-Informed Neural Networks for Fluid Flow in Complex Geometries》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：标准的物理信息神经网络（PINNs）在处理具有复杂拓扑结构的 3D 几何体（如三重周期最小表面 TPMS）中的流体流动时，往往表现不佳。
*   **研究动机**：
    *   **局部偏差**：标准 PINN 依赖于逐点残差最小化，这种局部约束在长而曲折的通道中难以传播全局物理信息。
    *   **守恒性失效**：在复杂几何中，局部微小的残差累积后会导致严重的全局质量不守恒和梯度不稳定。
    *   **网格挑战**：传统 CFD 在此类复杂几何中生成高质量网格的成本极高，而 PINN 虽然无网格，但在精度和稳定性上存在瓶颈。

### 2. 核心方法论
MUSA-PINN 提出了一种混合强-弱形式的框架，核心思想是将 PDE 约束重构为分层球形控制体上的**积分守恒定律**。
*   **弱形式重构**：利用高斯散度定理（Gauss Divergence Theorem），将体积内的连续性方程和动量方程转化为控制体表面上的通量平衡积分。
    *   **连续性残差**：计算穿过球面的速度矢量通量积分。
    *   **动量残差**：计算动量通量、压力张量和粘性应力张量在表面上的平衡。
*   **多尺度子域策略（Multi-scale Strategy）**：
    *   **大尺度（Large）**：在整个域内随机分布，用于捕捉长程耦合和全局一致性。
    *   **中尺度（Medium/Skeleton-aware）**：沿几何结构的“骨架”（Skeleton）放置，确保约束覆盖主要的传输路径。
    *   **小尺度（Small）**：在内部均匀采样，用于局部细节细化和边界行为稳定。
*   **两阶段训练计划**：
    *   **第一阶段**：仅开启连续性（质量守恒）约束进行预训练，抑制长程质量失衡。
    *   **第二阶段**：加入动量守恒约束进行精细调整。

### 3. 实验设计
*   **数据集/场景**：
    *   四种典型的 TPMS 热交换器通道：Primitive (P), Gyroid (G), Diamond (D), 和 IWP。
    *   工业级应用场景：液冷板（Liquid-cooling plate）和 DualMS 通道。
*   **Benchmark（基准）**：使用 Ansys CFX 生成的高精度 CFD 仿真结果作为 Ground Truth。
*   **对比方法**：
    *   **PINN-PE**：带位置编码的标准强形式 PINN。
    *   **RoPINN (NeurIPS 2024)**：区域优化 PINN。
    *   **CoPINN (ICML 2025)**：认知采样 PINN。
    *   **MDPINN-GD (NeurIPS 2025)**：带全局动力学约束的多域 PINN。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A40 GPU**。
*   **软件框架**：PyTorch。
*   **训练时长**：
    *   MUSA-PINN 第一阶段（7000 次迭代）约需 **7.05 小时**。
    *   第二阶段（2000 次迭代）由于涉及复杂的动量通量积分计算，约需 **18.79 小时**。
    *   总时长约 26 小时，高于标准 PINN（约 10 小时），但精度提升显著。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 种不同复杂度的 TPMS 结构上进行了系统对比。
    *   进行了针对雷诺数（Re=200, 400）的压力测试。
    *   **消融实验**：分别验证了单一尺度（仅大/中/小）、仅连续性、无阶段训练等变体，证明了多尺度和两阶段策略的必要性。
*   **客观性**：所有方法使用统一的网络架构（MLP + 傅里叶特征编码）和优化器（SOAP），确保了对比的公平性。

### 6. 主要结论与发现
*   **精度大幅提升**：在最困难的 Diamond 和 IWP 结构中，MUSA-PINN 将速度场相对误差降低了 **85% - 93%**。
*   **物理一致性强**：相比基准方法严重的质量泄漏（质量流率随长度大幅波动），MUSA-PINN 几乎完美保持了沿程的质量守恒（误差降低 97% 以上）。
*   **鲁棒性**：在基准方法普遍崩溃（误差 > 90%）的复杂拓扑中，MUSA-PINN 依然能收敛并捕捉到复杂的二次流和边界层特征。

### 7. 优点
*   **物理结构化监督**：通过积分形式引入了比逐点残差更强的物理约束。
*   **拓扑感知**：骨架感知采样解决了复杂细长通道中的覆盖空洞问题。
*   **无网格特性**：虽然使用了控制体，但通过蒙特卡洛表面采样实现了完全无网格化，避免了复杂的体网格划分。

### 8. 不足与局限
*   **计算开销**：由于需要在球面上进行蒙特卡洛采样并计算高阶导数（动量通量涉及 Hessian 项），训练时间显著增加。
*   **参数启发式**：控制体的半径选择（rL, rM, rS）目前依赖于几何启发式规则，尚未实现完全的任务自适应学习。
*   **稳态限制**：目前仅针对稳态不可压缩流，尚未扩展到瞬态流动或可压缩流。

（完）
