---
title: Physics-informed neural networks for solving strong-field saddle-point equations in strong-field physics with tailored fields
title_zh: 用于求解定制场强场物理中强场鞍点方程的物理信息神经网络
authors: "Jiakang Chen, Sufia Hashim, Carla Figueira de Morisson Faria"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15786v1"
tags: ["query:pinn"]
score: 10.0
evidence: 用于求解鞍点方程的物理信息神经网络
tldr: 本研究开发了一种无监督的物理信息神经网络（PINN），用于求解强场物理中描述阈上电离（ATI）的鞍点方程。该方法通过最小化方程残差进行训练，并引入窗口参数化策略以引导网络寻找物理相关的复数解。相比传统牛顿法，PINN在处理定制激光场时表现出更强的鲁棒性，能自动追踪电离事件的变化，为半经典强场计算提供了一种高效且稳定的新工具。
selection_source: fresh_fetch
motivation: 传统的牛顿迭代法在求解高度非线性的强场鞍点方程时对初始值极度敏感，难以在复杂激光参数空间内进行系统性研究。
method: 提出一种基于物理信息神经网络的无监督求解框架，结合窗口参数化策略将输出映射至复时间平面的特定区域以提高收敛稳定性。
result: 实验证明该网络能准确恢复各种定制场下的主导电离时间，并成功模拟了反映激光场对称性的光电子动量分布。
conclusion: PINN为解决强场物理中的半经典计算难题提供了稳健的方案，并为未来扩展至库仑校正模型或更复杂的再散射过程奠定了基础。
---

## 摘要
我们开发了一种无监督物理信息神经网络（PINN），用于求解强场近似下描述直接阈上电离（ATI）的鞍点方程（SPEs）。该设置提供了一个易于理解的测试平台，其中定制驱动场的鞍点结构是已知的，从而能够对所提出的求解器进行系统验证。在该方法中，网络通过最小化 SPEs 的残差进行训练，仅需定义驱动场形状及其参数，如强度、载波包络相位、椭圆率和相对相位。我们引入了一种窗口参数化策略，将网络输出映射到复时间平面的指定区域，引导优化趋向具有物理意义的解，并提高收敛稳定性。我们在多种场下将 PINN 与传统求解器进行了基准测试，证明了在广泛的参数范围内能够稳健地恢复主导的复电离时间。随着激光参数的变化，网络能够追踪电离事件主导地位的变化，从而能够探索传统求解器需要重复手动初始化的机制。利用 PINN 导出的解，我们计算了相干 ATI 光电子动量分布，并展示了驱动场的对称性体现在鞍点结构和所得光谱中。这些结果确立了 PINN 作为半经典强场计算的一个有前景的框架，并为将机器学习求解器扩展到库仑校正模型或更复杂的过程（如再散射 ATI）奠定了基础。对于再散射 ATI，其 SPEs 具有高度非线性，且存在多个紧密间隔的解，这使得传统的牛顿型求根方法对初始猜测高度敏感，从而阻碍了跨激光参数空间的系统研究，特别是在定制场的情况下。

## Abstract
We develop an unsupervised physics-informed neural network to solve saddle-point equations governing direct above-threshold ionization within the strong-field approximation. This setting provides a well-understood testbed in which the saddle-point structure is known for tailored driving fields, enabling systematic validation of the proposed solver. In this approach, the network is trained by minimizing the residual of the SPEs and requires only the definition of the driving-field shape and its parameters, such as intensity, carrier-envelope phase, ellipticity, and relative phase. We introduce a window parametrization strategy that maps network outputs to prescribed regions of the complex-time plane, guiding the optimization toward physically relevant solutions and improving convergence stability. We benchmark the PINN against a conventional solver for a range of fields, demonstrating robust recovery of the dominant complex ionization times over wide parameter ranges. The network tracks changes in ionization event dominance as laser parameters are varied, enabling exploration of regimes where conventional solvers require repeated manual initialization. Using the PINN-derived solutions, we compute coherent ATI photoelectron momentum distributions and show the symmetries of the driving fields are reflected in both the saddle-point structure and the resulting spectra. These results establish PINNs as a promising framework for semiclassical strong-field calculations and provide a foundation for extending machine-learning solvers to Coulomb-corrected models or to more complex processes, such as rescattered ATI for which the SPEs are highly nonlinear and the presence of multiple closely-spaced solutions makes conventional Newton-type root-finding highly sensitive to initial guesses, which hinders systematic investigations across laser-parameter spaces, particularly for tailored fields.

---

## 论文详细总结（自动生成）

以下是对论文《Physics-informed neural networks for solving strong-field saddle-point equations in strong-field physics with tailored fields》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在强场物理中，评估量子跃迁振幅通常涉及高度振荡的积分。通过强场近似（SFA）和鞍点法，这些积分被简化为求解非线性复数方程——鞍点方程（SPEs）。
*   **核心问题**：传统的数值求解器（如牛顿迭代法）对初始猜测值极度敏感。当激光参数（如强度、相位、偏振）变化时，解可能会移动、合并或分叉，导致求解器收敛到错误的分支或不收敛。
*   **研究意义**：本文旨在开发一种基于物理信息神经网络（PINN）的稳健求解框架，以自动、连续地追踪各种定制激光场下的物理有效解，解决半经典强场计算中的计算瓶颈。

### 2. 方法论
*   **核心思想**：采用**无监督学习**的 PINN 框架。网络不依赖预先计算的标签数据，而是通过最小化鞍点方程的残差（Residual）来学习从动量和激光参数到复数电离时间的映射。
*   **关键技术细节**：
    *   **输入与输出**：输入为电子动量（$p_\parallel, p_\perp$）和激光场参数（强度 $I$、相位 $\phi$、椭圆率 $\epsilon$ 等）；输出为复数电离时间 $t$ 的实部和虚部。
    *   **物理损失函数**：$L_{physics} = |[p + A(t, \gamma)]^2 + 2I_p|^2$，其中 $A$ 是矢量势，$I_p$ 是电离能。
    *   **窗口参数化策略（Window Parametrization）**：这是本文的关键创新。为了解决多解问题（防止网络坍缩到单一解），研究者引入了窗口函数，将网络输出映射到复时间平面的特定物理区域。这相当于为网络提供了粗略的物理先验，引导其寻找特定的电离事件分支。
*   **算法流程**：定义场形状 -> 设置窗口范围 -> 最小化残差训练网络 -> 提取全动量网格的鞍点解 -> 计算光电子动量分布（PMD）。

### 3. 实验设计
*   **测试场景**：涵盖了多种复杂的定制激光场：
    1.  单色场（Monochromatic fields）。
    2.  少周期脉冲（Few-cycle pulses，改变载波包络相位 CEP）。
    3.  双色线性偏振场（Bichromatic fields，如 $\omega-2\omega$ 和 $\omega-3\omega$）。
    4.  椭圆偏振场（Elliptically polarized fields）。
    5.  反转双圆场（Bicircular fields，具有离散旋转对称性）。
*   **Benchmark（基准）**：传统的基于梯度的局部求根算法（牛顿法）计算出的“地面真值”（Ground Truth）。
*   **对比指标**：复数解的准确性（实部与虚部）、训练损失（MSE）、以及最终生成的光电子动量分布（PMD）的对称性和干涉图样。

### 4. 资源与算力
*   **硬件设备**：单台笔记本电脑，配备 **NVIDIA GeForce RTX 3080 Laptop GPU**。
*   **训练时长**：
    *   对于 3 个输入参数（动量+强度）的模型，平均训练时间约为 **210-224 秒**。
    *   对于包含额外参数（如 CEP 或椭圆率）的 4 输入模型，训练时间增加至 **1000-1500 秒**。
*   **推理速度**：一旦训练完成，网络可以在毫秒级时间内预测整个动量网格的解。

### 5. 实验数量与充分性
*   **消融实验**：对网络深度（L=2, 3）、宽度（W=128, 256）、激活函数及窗口约束进行了详细对比，验证了窗口策略对收敛稳定性的决定性作用。
*   **参数覆盖**：实验涵盖了从简单对称场到复杂非对称场的多种配置，并在广泛的强度范围（$0.5$ 到 $2.0 \times 10^{14} W/cm^2$）内进行了验证。
*   **充分性评价**：实验设计非常系统且客观。通过对比不同对称性下的 PMD 图谱，证明了 PINN 能够准确捕捉物理对称性的破缺与恢复，实验结果具有高度的说服力。

### 6. 主要结论与发现
*   **稳健性**：PINN 能够成功恢复所有测试场下的主导复电离时间，且对参数变化的适应性远强于传统方法。
*   **自动追踪**：网络能够自然地追踪电离事件随激光参数变化的主导地位切换，无需像传统方法那样手动调整初始猜测。
*   **对称性保持**：PINN 导出的解完美保留了驱动场的动力学对称性（如半周期对称、旋转对称等），并准确反映在光电子光谱中。
*   **泛化能力**：通过增加输入维度，单个模型即可泛化到整个参数空间（如不同强度和相位的组合）。

### 7. 优点
*   **无需标签**：无监督特性使其在缺乏已知解的新物理场景下极具价值。
*   **解决多解歧义**：创新的窗口参数化方法有效解决了神经网络在处理多值函数时的“模式坍缩”问题。
*   **高效性**：相比于在每个网格点重复运行牛顿迭代，PINN 在处理大规模参数扫描时具有显著的计算优势。

### 8. 不足与局限
*   **物理模型简化**：目前仅应用于直接 ATI 过程，尚未考虑库仑势校正（如 CQSFA 模型）。
*   **复杂过程挑战**：对于再散射 ATI 或高次谐波产生（HHG），鞍点方程更加非线性且存在解的合并（Caustics），目前的架构可能需要进一步调整以处理近凝聚鞍点。
*   **先验依赖**：窗口策略虽然有效，但仍需要对解的大致分布区域有初步的物理了解。

（完）
