---
title: "pADAM: A Plug-and-Play All-in-One Diffusion Architecture for Multi-Physics Learning"
title_zh: pADAM：一种用于多物理场学习的即插即用一体化扩散架构
authors: "Amirhossein Mollaali, Bongseok Kim, Christian Moya, Guang Lin"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16757v1"
tags: ["query:pinn"]
score: 9.0
evidence: 用于多物理场PDE学习的统一生成框架
tldr: pADAM是一个统一的生成式多物理场学习框架，旨在解决深度学习求解器在跨物理定律泛化方面的局限。它通过扩散模型学习异构偏微分方程（PDE）族的共享概率先验，实现了在单一架构下无需重新训练即可进行前向预测和反向推理。该模型在从标量扩散到非线性纳维-斯托克斯方程的多种基准测试中表现出色，并结合保形预测提供可靠的不确定性量化，展现了其在统一且具有不确定性意识的科学推理中的潜力。
selection_source: fresh_fetch
motivation: 现有的深度学习物理求解器大多局限于单一方程，难以在不同的物理机制和推理任务之间进行迁移。
method: 提出一种基于扩散模型的即插即用全能架构，通过学习系统状态和物理参数的联合分布来构建跨异构PDE族的共享概率先验。
result: pADAM在多种复杂物理场景下实现了高精度的前向预测和反向推理，并能通过极少量的观测快照进行准确的概率模型选择。
conclusion: 该研究证明了生成式多物理场建模在实现统一、鲁棒且具备不确定性量化能力的科学推理方面的巨大潜力。
---

## 摘要
在不同的物理定律之间进行泛化仍然是科学人工智能面临的一个根本挑战。现有的深度学习求解器很大程度上局限于单方程设置，限制了在不同物理机制和推理任务之间的迁移。在这里，我们介绍了 pADAM，这是一个统一的生成框架，它在异构偏微分方程族之间学习共享的概率先验。通过学习系统状态以及（在适用情况下）物理参数的联合分布，pADAM 在单一架构内支持正向预测和反向推理，且无需重新训练。在从标量扩散到非线性 Navier-Stokes 方程的基准测试中，即使在稀疏观测下，pADAM 也能实现准确的推理。结合共形预测，它还提供了具有覆盖保证的可靠不确定性量化。此外，pADAM 仅通过两个稀疏快照即可执行概率模型选择，通过其学习到的生成表示来识别控制方程。这些结果突显了生成式多物理场建模在统一且具有不确定性感知能力的科学推理方面的潜力。

## Abstract
Generalizing across disparate physical laws remains a fundamental challenge for artificial intelligence in science. Existing deep-learning solvers are largely confined to single-equation settings, limiting transfer across physical regimes and inference tasks. Here we introduce pADAM, a unified generative framework that learns a shared probabilistic prior across heterogeneous partial differential equation families. Through a learned joint distribution of system states and, where applicable, physical parameters, pADAM supports forward prediction and inverse inference within a single architecture without retraining. Across benchmarks ranging from scalar diffusion to nonlinear Navier--Stokes equations, pADAM achieves accurate inference even under sparse observations. Combined with conformal prediction, it also provides reliable uncertainty quantification with coverage guarantees. In addition, pADAM performs probabilistic model selection from only two sparse snapshots, identifying governing laws through its learned generative representation. These results highlight the potential of generative multi-physics modeling for unified and uncertainty-aware scientific inference.

---

## 论文详细总结（自动生成）

以下是对论文《pADAM: A Plug-and-Play All-in-One Diffusion Architecture for Multi-Physics Learning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的深度学习偏微分方程（PDE）求解器（如 FNO、DeepONet、PINNs）大多遵循“一模型一方程”的范式，缺乏跨物理机制的知识迁移能力。此外，这些模型多为确定性输出，难以在现实世界常见的稀疏观测场景下提供可靠的不确定性量化。
*   **核心问题**：如何构建一个统一的生成式架构，使其能够跨越异构物理定律（如从纯耗散的扩散过程到非线性的流体动力学）进行学习，并在无需重新训练的情况下，灵活处理前向预测、反向推理及物理模型选择任务。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：提出 **pADAM** 框架，利用扩散模型（Diffusion Models）学习异构 PDE 族的共享概率先验。它将系统状态和物理参数映射到一个统一的生成空间中。
*   **关键技术细节**：
    *   **统一联合表示**：将不同物理系统转化为统一的 3 通道变量 $x$。对于标量 PDE，$x = [\Phi, u_0, u_T]$（参数、初态、末态）；对于矢量 PDE（如 Navier-Stokes），则包含耦合的流速分量。
    *   **参数提升（Parameter Lifting）**：为了解决物理参数（标量或低维向量）与空间场（高维图像）之间的维度差异，通过提升算子 $L$ 将参数广播为空间场 $\Phi$，使其与卷积架构兼容。
    *   **类条件扩散先验**：基于 EDM（Elucidating the Design Space of Diffusion-Based Generative Models）框架，学习条件分数函数 $\nabla_x \log p_t(x|c)$，其中 $c$ 是 PDE 类别标签。
    *   **即插即用推理（Plug-and-Play Guidance）**：利用贝叶斯引导（Bayesian Guidance），在推理阶段通过观测算子 $A$ 修正分数函数，实现对稀疏观测数据的后验采样，无需针对特定任务（前向或反向）进行微调。
    *   **保形预测（Conformal Prediction）**：集成保形校准技术，为生成的预测区间提供分布无关的有限样本覆盖保证，解决了稀疏观测下贝叶斯集合往往覆盖不足的问题。

### 3. 实验设计
*   **数据集与场景**：
    *   涵盖 7 种代表性 PDE：扩散方程（Diffusion）、平流方程（Advection）、平流-扩散方程（AD）、平流-扩散-反应方程（ADR）、Allen-Cahn 方程、Burgers' 方程、不可压缩 Navier-Stokes 方程。
    *   数据生成：使用有限差分法或傅里叶谱方法。
*   **Benchmark 与对比方法**：
    *   **对比基准**：专门针对单一 PDE 训练的扩散模型（如 DiffusionPDE）。
    *   **评估任务**：前向状态预测、反向初态重构、物理参数识别、零样本（Zero-shot）外推、概率模型选择。
    *   **观测条件**：对比了全场观测（100%）与极稀疏观测（30% 甚至 10%）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **参数效率**：文中提到 pADAM 使用的参数量与单个专用基准模型相当，但能同时处理 3 到 6 种不同的物理机制，实现了显著的“算子压缩”和参数效率提升。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多维度的评估：包括 3-PDE 统一学习、6-PDE 结构扩展、多变量参数扩展。
    *   针对每种设置，在 50 个独立测试实例上取平均值。
    *   包含消融性质的分析：如注意力机制的内部表示共享分析、保形校准前后的覆盖率对比。
*   **充分性与客观性**：实验设计较为全面，不仅验证了精度，还重点测试了模型在“物理失配”（ADR 外推）和“极度稀疏”下的鲁棒性。通过对比单方程专家模型，客观地展示了通用模型在保持精度的同时具备更强的泛化能力。

### 6. 主要结论与发现
*   **统一性**：单一模型可以同时掌握多种物理规律，且通过共享“物理词汇表”，其表现优于或等同于独立训练的专用模型。
*   **稀疏观测下的鲁棒性**：在仅有 10%-30% 观测数据的情况下，pADAM 仍能通过引导采样准确恢复全场状态和物理参数。
*   **模型发现能力**：pADAM 仅需两个稀疏的时间快照，就能从候选库中以概率形式准确识别出控制方程类别及其参数。
*   **可靠性**：保形预测将反向推理的经验覆盖率从约 36% 提升至接近 100%，确保了科学推理的严谨性。

### 7. 优点（亮点）
*   **全能性（All-in-One）**：一个架构解决前向、反向、参数识别和模型选择四类问题。
*   **即插即用**：推理时无需重新训练或微调，适应性极强。
*   **不确定性感知**：不仅提供点估计，还能量化预测风险并提供统计保证。
*   **零样本外推**：展示了在面对未见过的物理项（如反应项）时，利用观测数据修正先验偏差的能力。

### 8. 不足与局限
*   **采样效率**：作为扩散模型，推理过程涉及迭代去噪，采样速度慢于 FNO 等单次前向传播的模型。
*   **模型选择限制**：目前的概率模型选择依赖于预定义的候选 PDE 库，尚不能发现完全未知的数学结构。
*   **维度限制**：实验主要集中在 2D 空间，尚未讨论在 3D 或更高维系统中的扩展性及计算开销。
*   **离散化依赖**：虽然表现优异，但目前尚未采用函数空间扩散（Function-space Diffusion），可能存在分辨率依赖风险。

（完）
