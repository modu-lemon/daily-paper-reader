---
title: Computing the Gross-Pitaevskii Ground State via Wasserstein Gradient Flow in Diffeomorphism Space
title_zh: 通过微分同胚空间中的 Wasserstein 梯度流计算 Gross-Pitaevskii 基态
authors: "Xiangxiong Zhang, Haomin Zhou"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13579v1"
tags: ["query:pinn"]
score: 7.0
evidence: 基于神经微分方程的Gross-Pitaevskii方程无网格求解器
tldr: 本研究提出了一种在微分同胚空间中通过 Wasserstein 梯度下降计算 Gross-Pitaevskii 方程（GPE）基态的新方法。该方法利用保边界神经常微分方程（Neural ODE）参数化传输映射，将密度表示为参考测度的前推。通过将 Wasserstein 梯度流提升为参数空间中的自然梯度下降，实现了无网格且自动满足单位质量约束的计算。实验表明，该方法在 1D、2D 和 3D 中表现优异，能显著优化 Sobolev 梯度流的初始值，大幅降低初始能量差。
selection_source: fresh_fetch
motivation: 旨在解决 Gross-Pitaevskii 方程基态计算中对网格的依赖以及维持单位质量约束的复杂性。
method: 采用保边界 Neural ODE 参数化传输映射，将 Wasserstein 梯度流转化为参数空间内的自然梯度下降。
result: 该方法实现了完全无网格的计算，并在 2D 和 3D 实验中分别将 Sobolev 梯度流的初始能量差距降低了 7 倍和 4.5 倍。
conclusion: 参数化 Wasserstein 梯度流为多维 GPE 基态计算提供了一种高效、保质量且无需网格的初始化与求解方案。
---

## 摘要
我们通过微分同胚空间中的 Wasserstein 梯度下降法计算 Gross-Pitaevskii 方程 (GPE) 的基态 $u$。我们将密度 $ρ=u^2$ 表示为固定参考度量通过参数化传输映射 $T_θ$ 的前推（push-forward），该映射由保持边界的神经常微分方程 (Neural ODE) 实现。概率密度上的 Wasserstein 梯度流随后被提升到有限维参数空间中的自然梯度下降，其度量张量由 Wasserstein 度量的拉回 (pullback) 给出。该方法完全无网格，且无需归一化即可保持单位质量约束。我们展示了在 $d=1,2,3$ 维下的数值实验，并证明参数化 Wasserstein 梯度流 (PWGF) 的输出可用于初始化 $H^1$ Sobolev 梯度流，与平凡初始条件相比，在二维和三维中分别将初始能量间隙降低了 7 倍和 4.5 倍。

## Abstract
We compute the ground state $u$ of the Gross--Pitaevskii equation (GPE) via Wasserstein gradient descent in diffeomorphism space. We represent the density $ρ=u^2$ as the push-forward of a fixed reference measure through a parameterized transport map $T_θ$, realized by a boundary-preserving Neural ODE. The Wasserstein gradient flow on probability densities then lifts to natural gradient descent in the finite-dimensional parameter space, with metric tensor given by the pullback of the Wasserstein metric. The method is entirely mesh-free and preserves the unit-mass constraint without normalization. We present numerical experiments in dimensions $d=1,2,3$ and demonstrate that the parameterized Wasserstein gradient flow (PWGF) output can be used to initialize the $H^1$ Sobolev gradient flow, reducing the initial energy gap by a factor of $7$ in 2D and $4.5$ in 3D compared to trivial initial conditions.