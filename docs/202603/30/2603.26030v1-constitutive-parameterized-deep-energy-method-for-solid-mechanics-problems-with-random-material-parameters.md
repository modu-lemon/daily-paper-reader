---
title: Constitutive parameterized deep energy method for solid mechanics problems with random material parameters
title_zh: 针对具有随机材料参数的固体力学问题的本构参数化深度能量法
authors: "Zhangyong Liang, Huanhuan Gao"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26030v1"
tags: ["query:pinn"]
score: 8.0
evidence: 固体力学的物理信息框架
tldr: 针对固体力学中材料参数随机波动导致的计算效率瓶颈，本文提出了一种本构参数化深度能量法（CPDEM）。该方法将材料参数与空间坐标共同嵌入神经网络，通过最小化参数域内的期望能量泛函进行无监督训练。CPDEM实现了对未知材料参数的零样本、实时位移场推理，无需重复训练或依赖高保真数据集，在处理连续多参数变化方面具有显著优势。
selection_source: fresh_fetch
motivation: 传统数值方法和数据驱动模型在处理具有随机波动的材料参数时，面临计算成本高昂或对海量数据依赖性强的问题。
method: 提出CPDEM框架，通过将随机本构参数编码进神经网络并利用物理驱动的期望能量最小化进行训练。
result: 在有限应变超弹性和复杂非线性接触力学等多个基准测试中，该方法实现了对未知参数的零样本实时推理。
conclusion: CPDEM是首个能同时高效处理固体力学中连续多参数变化的纯物理驱动深度学习范式。
---

## 摘要
在实际结构设计和固体力学模拟中，材料属性在有限区间内固有地表现出随机变化。然而，评估连续材料不确定性下的力学响应仍然是一个持续的挑战。传统的数值方法，如有限元法 (FEM)，由于需要对每个参数实现进行重复的网格剖分和方程求解，会产生极高的计算成本。类似地，数据驱动的代理模型严重依赖海量的高保真数据集，而标准的物理信息框架（如深度能量法）在材料参数发生变化时，严格要求从头开始进行完整的重新训练。为了弥补这一关键空白，我们提出了本构参数化深度能量法 (CPDEM)。在这个纯物理驱动的框架中，通过对随机本构参数的潜在表示进行编码，重新构建了应变能密度泛函。通过将材料参数与空间坐标一起直接嵌入神经网络，CPDEM 将传统的空间配置点转换为参数感知的材料点。该预训练模型通过在参数域上进行期望能量最小化的无监督方式进行训练，从而持续学习解流形。因此，它能够对未知材料参数实现位移场的零样本、实时推理，而无需任何数据集生成或模型重训。所提方法在多种基准测试中得到了严格验证，包括线性弹性、有限应变超弹性以及复杂的强非线性接触力学。据我们所知，CPDEM 是首个能够同时且高效地处理固体力学中连续多参数变化的纯物理驱动深度学习范式。

## Abstract
In practical structural design and solid mechanics simulations, material properties inherently exhibit random variations within bounded intervals. However, evaluating mechanical responses under continuous material uncertainty remains a persistent challenge. Traditional numerical approaches, such as the Finite Element Method (FEM), incur prohibitive computational costs as they require repeated mesh discretization and equation solving for every parametric realization. Similarly, data-driven surrogate models depend heavily on massive, high-fidelity datasets, while standard physics-informed frameworks (e.g., the Deep Energy Method) strictly demand complete retraining from scratch whenever material parameters change. To bridge this critical gap, we propose the Constitutive Parameterized Deep Energy Method (CPDEM). In this purely physics-driven framework, the strain energy density functional is reformulated by encoding a latent representation of stochastic constitutive parameters. By embedding material parameters directly into the neural network alongside spatial coordinates, CPDEM transforms conventional spatial collocation points into parameter-aware material points. Trained in an unsupervised manner via expected energy minimization over the parameter domain, the pre-trained model continuously learns the solution manifold. Consequently, it enables zero-shot, real-time inference of displacement fields for unknown material parameters without requiring any dataset generation or model retraining. The proposed method is rigorously validated across diverse benchmarks, including linear elasticity, finite-strain hyperelasticity, and complex highly nonlinear contact mechanics. To the best of our knowledge, CPDEM represents the first purely physics-driven deep learning paradigm capable of simultaneously and efficiently handling continuous multi-parameter variations in solid mechanics.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：针对具有随机材料参数的固体力学问题的本构参数化深度能量法（CPDEM）

#### 1. 核心问题与整体含义（研究动机和背景）
在固体力学模拟和结构设计中，材料属性（如杨氏模量 $E$、泊松比 $\nu$）由于制造公差或环境因素，往往在一定区间内呈现随机波动。
*   **传统方法的局限**：有限元法（FEM）在参数变化时需重新划分网格并求解刚度矩阵，计算成本极高；纯数据驱动模型（如算子学习）依赖海量高保真模拟数据；现有的物理信息神经网络（如 PINN 或 DEM）在材料参数改变时通常需要从头重训。
*   **研究目标**：开发一种纯物理驱动的框架，能够学习一个通用的“解流形”，从而在材料参数发生变化时实现**零样本（Zero-shot）实时推理**，无需重复计算或依赖预存数据。

#### 2. 论文提出的方法论
论文提出了**本构参数化深度能量法（CPDEM）**，其核心思想是将材料参数作为神经网络的显式输入，并利用变分原理进行训练。
*   **核心架构**：采用模块化设计，包含三个部分：
    1.  **空间编码器 ($g_{\theta c}$)**：提取空间坐标 $X$ 的特征。
    2.  **参数编码器 ($g_{\theta p}$)**：将随机本构参数 $\eta$（如 $E, \nu$）编码为潜在表示（Latent Representation）。
    3.  **流形子空间网络 ($g_{\theta g}$)**：融合上述两种特征，输出位移场 $u(X, \eta)$。
*   **损失函数（期望能量最小化）**：不同于标准 DEM 最小化单个配置的势能，CPDEM 在参数空间 $P$ 上最小化总势能的**期望值**：
    $$J(u) = \mathbb{E}_{\eta \sim p(\eta)} [\Pi(u(X, \eta); \eta)]$$
    这使得网络能够同时学习整个参数域内的物理规律。
*   **算法流程**：
    *   **预训练阶段**：在参数空间内均匀采样，通过 Adam 和 L-BFGS 优化器进行无监督训练，构建通用的解流形。
    *   **推理阶段**：对于给定的任意参数，直接通过前向传播获取位移场。
    *   **快速微调**：针对特定极端精度需求，可冻结编码器，仅对流形网络进行极少量迭代。

#### 3. 实验设计
论文通过多个由简入繁的基准测试验证了 CPDEM 的性能：
*   **线性弹性问题**：1D 弹性杆（对比解析解）、2D 悬臂梁弯曲（对比高精度 FEM）。
*   **超弹性问题**：1D 超弹性杆、2D/3D 几何非线性梁（采用 Neo-Hookean 和 Mooney-Rivlin 模型）。
*   **复杂非线性问题**：半圆柱橡胶体与刚性平面的**接触摩擦力学（Ironing problem）**，涉及大变形和接触约束。
*   **Benchmark**：主要对比对象为解析解和经过精细网格划分的 FEM 结果。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **优化细节**：提到了使用 Adam 优化器进行初步快速下降，随后切换到 L-BFGS 优化器以确保物理平衡的严格收敛。训练通常涉及 50 到 120 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：论文共设计了约 6 组主要实验，涵盖了从 1D 到 3D、从线性到强非线性、从单体到接触问题的完整梯度。
*   **充分性评价**：
    *   **参数覆盖**：对杨氏模量和泊松比在连续区间内的变化进行了密集采样验证。
    *   **泛化性测试**：进行了分布内（ID）的零样本推理实验和分布外（OOD）的外推实验。
    *   **客观性**：通过与 FEM 结果的逐点误差分析（Pointwise Error）和相对 $L_2$ 误差对比，证明了模型在整个参数域内的一致性。实验设计较为全面且客观。

#### 6. 论文的主要结论与发现
*   **高效推理**：CPDEM 训练完成后，对新参数的推理速度比 FEM 快几个数量级，且无需重新组装刚度矩阵。
*   **零样本能力**：模型能够准确预测训练期间未见过的材料参数组合下的力学响应。
*   **物理一致性**：由于损失函数基于能量泛函，预测结果严格遵守运动学约束和本构方程，即使在 OOD 情况下也能保持结构完整性。
*   **微调优势**：通过快速微调策略，可以在不到 1 秒的时间内将 OOD 预测精度提升 95% 以上。

#### 7. 优点（亮点）
*   **纯物理驱动**：完全不需要标记数据（Label-free），解决了力学数据获取成本高的问题。
*   **参数化创新**：首次在深度能量法中引入本构参数编码，实现了从“单点求解”到“流形学习”的跨越。
*   **处理复杂性**：成功处理了极具挑战性的 3D 大变形和非线性接触问题，展示了极强的鲁棒性。

#### 8. 不足与局限
*   **参数同质性假设**：目前实验主要针对全局均匀的随机参数，尚未深入探讨空间非均匀（如功能梯度材料或随机场）的复杂情况。
*   **路径依赖性**：尚未涵盖弹塑性等具有历史相关性（Path-dependent）的材料行为。
*   **算力细节缺失**：缺乏具体的硬件开销和训练时间对比，难以直观评估其在工业级大规模模型上的预训练成本。

（完）
