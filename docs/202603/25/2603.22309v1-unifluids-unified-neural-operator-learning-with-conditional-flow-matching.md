---
title: "UniFluids: Unified Neural Operator Learning with Conditional Flow-matching"
title_zh: UniFluids：基于条件流匹配的统一神经算子学习
authors: "Haosen Li, Qi Meng, Jiahao Li, Rui Zhang, Ruihua Song, Liang Ma, Zhi-Ming Ma"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.22309v1"
tags: ["query:pinn"]
score: 10.0
evidence: 针对多种PDE的统一神经算子学习
tldr: UniFluids是一个基于条件流匹配（Conditional Flow-matching）的统一神经算子学习框架，旨在解决不同维度和物理变量的偏微分方程（PDE）模拟问题。该模型利用扩散Transformer的可扩展性，通过统一的四维时空表示处理异构数据集，并采用并行序列生成替代传统的自回归方式。此外，针对PDE数据的低维特性引入了x-prediction策略。实验证明，UniFluids在1D、2D和3D PDE任务中均表现出高精度、强扩展性和优异的跨场景泛化能力。
selection_source: fresh_fetch
motivation: 现有的PDE基础模型多采用自回归方式，难以高效且统一地处理具有不同维度和物理变量的异构偏微分方程。
method: 提出UniFluids框架，结合条件流匹配与扩散Transformer，利用统一的四维时空表示和x-prediction策略实现并行化的算子学习。
result: 在涵盖1D、2D和3D空间的多个PDE数据集上，UniFluids展现了卓越的预测精度、良好的可扩展性以及跨场景的泛化性能。
conclusion: UniFluids证明了流匹配在统一神经算子学习中的有效性，为大规模、多维度的科学计算模拟提供了一种高效的新范式。
---

## 摘要
偏微分方程（PDE）模拟在科学研究中具有广泛的意义。目前，利用深度神经网络学习 PDE 的解算子展现出了巨大的潜力。在本文中，我们提出了 UniFluids，这是一个条件流匹配框架，它利用扩散 Transformer 的可扩展性，实现了对具有不同维度和物理变量的多种 PDE 解算子的统一学习。与自回归 PDE 基础模型不同，UniFluids 采用流匹配来实现并行序列生成，使其成为首个用于统一算子学习的此类方法。具体而言，为异构 PDE 数据集引入统一的四维时空表示，实现了联合训练和条件编码。此外，我们发现 PDE 数据集的有效维度远低于其 patch 维度。因此，我们在流匹配算子学习中采用了 $x$-预测（$x$-prediction），经验证这能显著提高预测精度。我们在涵盖 1D、2D 和 3D 空间维度的多个 PDE 数据集上对 UniFluids 进行了大规模评估。实验结果表明，UniFluids 取得了极高的预测精度，并展现出良好的可扩展性和跨场景泛化能力。代码将在稍后发布。

## Abstract
Partial differential equation (PDE) simulation holds extensive significance in scientific research. Currently, the integration of deep neural networks to learn solution operators of PDEs has introduced great potential. In this paper, we present UniFluids, a conditional flow-matching framework that harnesses the scalability of diffusion Transformer to unify learning of solution operators across diverse PDEs with varying dimensionality and physical variables. Unlike the autoregressive PDE foundation models, UniFluids adopts flow-matching to achieve parallel sequence generation, making it the first such approach for unified operator learning. Specifically, the introduction of a unified four-dimensional spatiotemporal representation for the heterogeneous PDE datasets enables joint training and conditional encoding. Furthermore, we find the effective dimension of the PDE dataset is much lower than its patch dimension. We thus employ $x$-prediction in the flow-matching operator learning, which is verified to significantly improve prediction accuracy. We conduct a large-scale evaluation of UniFluids on several PDE datasets covering spatial dimensions 1D, 2D and 3D. Experimental results show that UniFluids achieves strong prediction accuracy and demonstrates good scalability and cross-scenario generalization capability. The code will be released later.

---

## 论文详细总结（自动生成）

以下是对论文《UniFluids: Unified Neural Operator Learning with Conditional Flow-matching》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的偏微分方程（PDE）数值模拟计算成本高。虽然神经算子（Neural Operators）展现了潜力，但现有方法存在三大局限：
    1.  **误差累积**：大多采用自回归（Autoregressive）步进预测，导致小误差随时间放大。
    2.  **确定性偏差**：确定性模型容易产生过度平滑（Over-smoothing）和频谱偏差，忽略了物理系统中的不确定性。
    3.  **缺乏统一性**：现有模型通常针对特定维度（如仅2D）或特定物理变量设计，难以在统一框架下处理1D、2D、3D及异构物理变量的联合训练。
*   **核心目标**：构建一个基于**条件流匹配（Conditional Flow-matching）**的统一生成式神经算子框架，实现跨维度、跨物理场景的并行序列生成。

### 2. 方法论：核心思想与关键技术
*   **统一四维时空表示**：
    *   将所有PDE轨迹映射到统一的4D张量接口 $(t, h, w, d)$。
    *   对于低维数据（如1D或2D），在缺失维度上进行零填充（Zero-padding），并使用通道掩码（Channel Mask）处理异构的物理变量集。
    *   引入**4D旋转位置嵌入（4D RoPE）**，使模型能灵活处理不同分辨率和维度的Token。
*   **条件流匹配算子**：
    *   **条件编码器**：使用Transformer提取历史状态的密集特征（Dense tokens）和通过时空聚合得到的紧凑特征（Compact vector）。
    *   **流匹配生成**：不同于自回归，UniFluids通过定义数据与噪声之间的线性插值路径，学习概率流ODE。在推理时，通过ODE求解器（如Euler法）一次性并行生成未来多个时间步的状态。
*   **x-prediction 策略**：
    *   **关键发现**：论文通过特征值分析发现，PDE数据的有效维度远低于其Patch维度（尤其在3D场景下）。
    *   **技术细节**：在流匹配训练中，让模型直接预测原始状态 $x$（即 $x$-pred），而非预测速度 $v$ 或噪声 $\epsilon$。这种参数化方式能让模型更专注于低维流形上的结构信息，显著提升收敛速度和精度。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 **PDEBench** 基准测试集，涵盖：1D（平流方程、Burgers方程、压缩NS方程）、2D（压缩/不可压缩NS方程、浅水方程、反应扩散方程）、3D（压缩NS方程）。
*   **对比方法（Baselines）**：
    *   **任务特定模型**：PINNs, U-Net, FNO。
    *   **统一预训练模型**：MPP, DPOT, OmniArch。
*   **评估指标**：主要使用相对 $L_2$ 误差（nRMSE）。

### 4. 资源与算力
*   **模型规模**：提供了四种规模：S (22.7M), M (60.5M), L (135.3M), XL (512.3M)。
*   **算力细节**：
    *   **文中未明确说明**具体的GPU型号（如A100或H100）及总训练时长。
    *   提到了训练配置：使用 AdamW 优化器，学习率 $10^{-4}$，采用混合精度训练（Mixed-precision）和梯度裁剪。
    *   批大小（Batch Size）：1D为16，2D为8，3D为4。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在7大类PDE数据集上进行了全面测试。
    *   **Scaling 实验**：验证了模型性能随参数量增加（从S到XL）的持续提升。
    *   **泛化实验**：包括域内微调（In-domain adaptation）和针对未见场景（如激波管、KH不稳定性）的**零样本（Zero-shot）**泛化实验。
    *   **消融实验**：重点对比了 $x$-pred、$v$-pred 和 $\epsilon$-pred 的效果。
*   **充分性评价**：实验设计较为充分，涵盖了从基础精度到大规模扩展性及跨域泛化的全方位评估。对比基准包含了当前最先进的PDE基础模型，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **精度突破**：UniFluids 在多个任务上刷新了纪录，相比现有最强基准（如OmniArch），在2D不可压缩流体任务中误差降低了 **86.7%**。
*   **并行优势**：流匹配框架实现了未来序列的并行生成，有效缓解了自回归模型的误差累积问题。
*   **x-pred 的必要性**：实验证明在处理高维、强相关性的物理数据时，预测状态 $x$ 比预测噪声 $\epsilon$ 更加稳定且精确，后者在复杂场景下甚至无法收敛。
*   **扩展性**：模型展现了类似大语言模型的 Scaling Law 特征，随着参数量增大，验证损失平稳下降。

### 7. 优点（亮点）
*   **真正的统一性**：通过4D表示和4D RoPE，首次在生成式框架下实现了1D/2D/3D PDE的无缝联合训练。
*   **理论与实践结合**：不仅提出了模型，还深入分析了PDE数据的“有效维度”退化问题，并据此优化了训练策略（x-pred）。
*   **零样本能力**：在未见过的复杂物理现象（如Orszag-Tang涡旋）上展现了极强的零样本推理能力。

### 8. 不足与局限
*   **采样成本**：虽然避免了自回归，但流匹配在推理时仍需多次ODE求解迭代（如4D步），相比单步确定性预测，推理延迟较高。
*   **分辨率敏感性**：虽然支持多分辨率，但在测试分辨率远超训练分布（如极高雷诺数的湍流）时，模型捕捉尖锐结构（如激波）的能力仍有待加强。
*   **算力透明度**：论文未公开具体的训练资源消耗，难以评估其训练成本的经济性。

（完）
