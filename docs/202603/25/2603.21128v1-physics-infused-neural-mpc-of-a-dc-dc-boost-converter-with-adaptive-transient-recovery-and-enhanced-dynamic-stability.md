---
title: Physics-Infused Neural MPC of a DC-DC Boost Converter with Adaptive Transient Recovery and Enhanced Dynamic Stability
title_zh: 具有自适应瞬态恢复与增强动态稳定性的直流-直流升压变换器物理注入神经模型预测控制
authors: Tahmin Mahmud
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21128v1"
tags: ["query:pinn"]
score: 9.0
evidence: 用于升压转换器的混合物理信息神经网络
tldr: 针对DC-DC升压斩波器在变负载下的效率与稳定性挑战，本文提出一种结合物理信息神经网络（PINN）与有限控制集模型预测控制（FCS-MPC）的混合控制框架。该方法通过PINN嵌入物理规律实现精确状态预测，并利用FCS-MPC进行多目标优化，实现了自适应瞬态恢复与增强的动态稳定性。实验证明，该方案在降低电压纹波和提升瞬态响应方面表现优异，为电力电子实时控制提供了兼顾物理一致性与计算效率的新思路。
selection_source: fresh_fetch
motivation: 传统MPC和纯数据驱动方法在处理DC-DC转换器控制时存在计算复杂度高及难以保证物理约束的问题。
method: 提出一种将物理规律嵌入神经网络训练的PINN模型，并结合FCS-MPC实现具备自适应瞬态恢复能力的混合控制框架。
result: 实验结果显示，该方法显著改善了瞬态响应速度，减小了输出电压纹波，并在不同导通模式下均表现出极强的鲁棒性。
conclusion: 该物理注入的神经MPC框架为电力电子设备提供了一种计算高效且符合物理一致性的实时控制解决方案。
---

## 摘要
直流-直流（DC-DC）升压变换器需要先进的控制技术，以确保在变负载条件下的效率和稳定性。传统的模型预测控制（MPC）和数据驱动神经网络方法面临着计算复杂度高以及物理约束执行受限等挑战。本文针对升压变换器提出了一种结合物理信息神经网络（PINN）与有限控制集模型预测控制（FCS-MPC）的混合方法。PINN 将物理定律嵌入到神经网络训练中，提供准确的状态预测，而 FCS-MPC 则确保了约束满足和多目标优化。该方法具有自适应瞬态恢复、显式占空比控制以及增强的动态稳定性等特点。在商用升压模块上的实验结果表明，该方法改善了瞬态响应，减小了电压纹波，并在不同导通模式下实现了鲁棒运行。所提出的框架为电力电子领域的实时控制提供了一种计算高效且物理一致的解决方案。

## Abstract
DC-DC boost converters require advanced control to ensure efficiency and stability under varying loads. Traditional model predictive control (MPC) and data-driven neural network methods face challenges such as high complexity and limited physical constraint enforcement. This paper proposes a hybrid physics-informed neural network (PINN) combined with finite control set MPC (FCS-MPC) for boost converters. The PINN embeds physical laws into neural training, providing accurate state predictions, while FCS-MPC ensures constraint satisfaction and multi-objective optimization. The method features adaptive transient recovery, explicit duty-ratio control, and enhanced dynamic stability. Experimental results on a commercial boost module demonstrate improved transient response, reduced voltage ripple, and robust operation across conduction modes. The proposed framework offers a computationally efficient, physically consistent solution for real-time control in power electronics.

---

## 论文详细总结（自动生成）

以下是对论文《Physics-Infused Neural MPC of a DC-DC Boost Converter with Adaptive Transient Recovery and Enhanced Dynamic Stability》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：DC-DC 升压变换器（Boost Converter）具有显著的非线性、非理想特性（如死区时间、寄生振荡）和单向功率流特征。传统的模型预测控制（MPC）计算复杂度高且难以处理非线性约束，而纯数据驱动的神经网络（NN）方法虽然推理快，但缺乏物理一致性，在处理安全关键的电力电子应用时存在泛化性差和误差累积的问题。
*   **研究动机**：开发一种既能利用神经网络的快速推理能力，又能严格遵守物理定律的控制框架，以提高变换器在变负载条件下的瞬态响应速度、动态稳定性和效率。

### 2. 核心方法论
论文提出了一种**混合 PINN + FCS-MPC 框架**，其核心思想是将物理约束嵌入神经网络训练，并结合有限控制集模型预测控制进行实时优化。
*   **物理信息神经网络 (PINN)**：
    *   采用轻量级多层感知器（MLP）架构。
    *   **损失函数设计**：由数据损失（$L_{data}$，拟合测量值）和物理损失（$L_{phys}$，基于变换器的常微分方程 ODE）组成。
    *   **功能**：预测下一时刻的状态变量（电感电流 $i_L$、电容电压 $v_C$）和在线估计物理参数（如电感、电容、电阻等）。
*   **有限控制集模型预测控制 (FCS-MPC)**：
    *   利用 PINN 提供的精确预测模型，在每个采样周期内枚举有限的开关状态。
    *   **自适应瞬态恢复**：通过在线估计负载电阻 $R$，计算稳态占空比 $d_{ss}$ 作为前馈项。
    *   **多目标代价函数**：综合考虑电压/电流跟踪误差、开关频率约束以及占空比偏差，通过最小化代价函数选择最优开关信号。

### 3. 实验设计
*   **实验平台**：使用商用 TI-PMLKBOOSTEVM 升压模块，包含 TPS55340 和 LM5122 开关稳压器。控制器采用 TI C2000 TMS320F28379D 微控制器。
*   **测试场景**：
    *   **稳态运行**：验证在连续导通模式（CCM）和断续导通模式（DCM）下的稳定性。
    *   **负载瞬态响应**：负载电流在 1A 和 0.2A 之间突变；电阻负载在 200Ω 和 500Ω 之间切换。
*   **Benchmark（基准方法）**：对比了芯片内置的传统电压模式 PWM 控制（带工厂预设 PI 补偿网络）以及不带 PINN 辅助的控制方案。

### 4. 资源与算力
*   **硬件设备**：训练使用 NVIDIA GeForce RTX 4090 GPU (24GB VRAM) 和 Intel Core i9-14900HX CPU。
*   **训练细节**：
    *   **数据集规模**：仅需 3000 个样本（70%训练，15%测试，15%验证）。
    *   **训练时长**：在 Adam 优化器下，5000 个 epoch 的训练约耗时 **4 分钟**。
    *   **推理延迟**：PINN 推理时间小于 0.5 ms/step，模型参数量约 3200 个，适合实时部署。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了从仿真到硬件实验的完整流程。硬件实验涵盖了重载、中载、轻载以及 CCM/DCM 模式切换。
*   **充分性评价**：实验设计较为充分，通过对比不同负载阶跃下的波形（如电压跌落、恢复时间），直观展示了改进效果。使用了 95% 置信区间来量化 PINN 的预测精度，增加了实验的客观性。

### 6. 主要结论与发现
*   **瞬态性能显著提升**：在负载电阻从 200Ω 切换到 500Ω 时，恢复时间从 520μs 缩短至 **120μs**。
*   **纹波与稳定性**：相比传统 PI 控制，该方法显著减小了输出电压纹波，并实现了更快的稳定过程。
*   **物理一致性**：PINN 能够以极少的数据量（3000 样本）实现高精度预测（RMSE 降至 $10^{-3}$ 级别），证明了物理约束在减少数据依赖方面的有效性。

### 7. 优点与亮点
*   **轻量化设计**：模型参数少，计算效率高，解决了深度学习模型难以在低功耗 MCU 上实时运行的痛点。
*   **自适应能力**：通过在线负载估计和稳态占空比前馈，增强了系统对参数漂移和负载波动的鲁棒性。
*   **混合驱动**：结合了数据驱动的灵活性和物理模型的严谨性，有效处理了 Boost 变换器的非线性动态。

### 8. 不足与局限
*   **参数依赖性**：虽然能在线估计负载，但对于电感/电容的等效串联电阻（ESR）等微小参数的准确性仍有一定依赖。
*   **拓扑局限性**：实验仅限于单相 Boost 变换器，尚未验证在更复杂的电力电子系统（如多相交错或多电平变换器）中的扩展性。
*   **环境适应性**：未讨论极端温度或长期老化导致的元器件参数剧烈变化对 PINN 预测精度的长期影响。

（完）
