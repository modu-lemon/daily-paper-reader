---
title: Verifiable Error Bounds for Physics-Informed Neural KKL Observers
title_zh: 物理信息神经网络 KKL 观测器的可验证误差界
authors: "Hannah Berin-Costain, Harry Wang, Kirsten Morris, Jun Liu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20434v1"
tags: ["query:pinn"]
score: 9.0
evidence: 使用物理信息神经网络（PINN）学习KKL变换映射
tldr: 本文针对基于学习的KKL观测器缺乏可计算误差界限的问题，提出了一种结合物理信息神经网络（PINN）与神经网络验证技术的方法。研究者推导出了一个仅依赖于可在指定区域内验证的量的状态估计误差界，并将其扩展至包含有界测量噪声的情形。该方法在非线性基准系统上得到了验证，为神经观测器提供了可靠的理论保障。
selection_source: fresh_fetch
motivation: 现有的基于PINN的KKL观测器虽然能学习非线性系统的变换映射，但缺乏可量化的状态估计误差界限。
method: 利用神经网络验证工具计算特定区域内的可验证指标，从而推导出状态估计误差的理论上界。
result: 实现了对状态估计误差的严谨界定，并证明了该界限在存在有界测量噪声时依然有效。
conclusion: 该方法为非线性系统的神经观测器提供了可计算的性能保证，增强了学习型观测器在实际应用中的可靠性。
---

## 摘要
本文为基于学习的 Kazantzis--Kravaris/Luenberger (KKL) 观测器提出了一种可计算的状态估计误差界。近期的研究工作利用物理信息神经网络 (PINN) 学习 KKL 变换映射，并利用常规神经网络学习相应的左逆映射。然而，目前该方法尚无现成的可计算状态估计误差界。我们推导出了一个状态估计误差界，该误差界仅取决于可以通过神经网络验证在指定区域内进行认证的量。我们进一步将该结果扩展到有界加性测量噪声的情况，并在非线性基准系统上展示了这些保证。

## Abstract
This paper proposes a computable state-estimation error bound for learning-based Kazantzis--Kravaris/Luenberger (KKL) observers. Recent work learns the KKL transformation map with a physics-informed neural network (PINN) and a corresponding left-inverse map with a conventional neural network. However, no computable state-estimation error bounds are currently available for this approach. We derive a state-estimation error bound that depends only on quantities that can be certified over a prescribed region using neural network verification. We further extend the result to bounded additive measurement noise and demonstrate the guarantees on nonlinear benchmark systems.

---

## 论文详细总结（自动生成）

以下是对论文《Verifiable Error Bounds for Physics-Informed Neural KKL Observers》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何为基于学习的非线性观测器（特别是 KKL 观测器）提供**可计算且可验证**的状态估计误差上界。
*   **研究背景**：Kazantzis–Kravaris/Luenberger (KKL) 观测器通过将非线性系统映射到线性稳定动力学空间来实现状态估计。虽然物理信息神经网络 (PINN) 已被用于学习这种复杂的映射及其逆映射，但现有的误差分析通常依赖于不可计算的量（如未知的理想映射或最坏情况下的近似间隙）。
*   **整体含义**：在安全关键型应用中，仅有“平均误差小”是不够的。本文旨在通过形式化验证技术，为神经观测器提供严谨的性能保证。

### 2. 论文提出的方法论
核心思想是将 Lyapunov 稳定性分析与神经网络验证工具（$\alpha, \beta$-CROWN）相结合。
*   **核心步骤**：
    1.  **定义 PDE 残差**：定义 $R_\theta(x)$ 为学习到的变换映射 $\hat{T}_\theta$ 与理论 KKL 偏微分方程之间的偏差。
    2.  **Lyapunov 误差界推导**：利用 Lyapunov 函数证明，观测器坐标系下的误差 $e_z$ 的渐近上界取决于 PDE 残差的最大值 $\bar{R}$。
    3.  **状态空间误差转换**：通过三角不等式，将状态估计误差 $\| \hat{x} - x \|$ 分解为两部分：由 $e_z$ 引起的误差（受逆映射的 Lipschitz 常数 $L_\eta$ 缩放）和重建误差 $E_{\hat{T}}$（即 $\hat{T}^*_\eta(\hat{T}_\theta(x)) - x$）。
    4.  **形式化验证**：使用 $\alpha, \beta$-CROWN 工具在预设的紧致区域 $X$ 上计算三个关键量的可验证上界：最大 PDE 残差 $\bar{R}$、逆映射的 Lipschitz 常数 $L_\eta$、以及最大重建误差 $E_{\hat{T}}$。
    5.  **噪声扩展**：将该框架扩展到存在有界测量噪声 $v(t)$ 的情况，推导出包含噪声项的增强误差界。

### 3. 实验设计
*   **实验场景**：
    *   **反向 Duffing 振子 (Reverse Duffing Oscillator)**：一个具有守恒能量特性的二阶非线性系统。
    *   **Van der Pol 振子**：具有稳定极限环的典型非线性系统。
*   **Benchmark 与对比**：
    *   实验主要对比了**实际观测到的误差轨迹**与**理论计算的验证界限**。
    *   验证了在 1.0% 峰值输出幅度的加性测量噪声下的鲁棒性。
*   **训练流程**：采用 PINN 框架，结合 Adam 优化器进行初步训练，并使用 L-BFGS 进行精细化调整（Fine-tuning），同时引入“硬点挖掘”（Hard-point mining）策略来降低最大残差。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 $\alpha, \beta$-CROWN 验证框架，该框架通常在现代 GPU 上运行以实现并行化分支定界计算。训练参数包括：Duffing 系统使用 8 层 100 单元网络，Van der Pol 使用 7 层 128 单元网络，激活函数为 `tanh`。

### 5. 实验数量与充分性
*   **实验规模**：针对两个经典的非线性基准系统进行了深入实验。
*   **充分性评价**：
    *   **覆盖面**：实验涵盖了无噪声和有噪声两种情况，并展示了误差随时间演化的轨迹。
    *   **客观性**：通过形式化验证得到的界限确实包络了所有随机初始条件的实际误差轨迹，证明了界限的严谨性。
    *   **局限性**：实验仅限于二阶系统（状态维度 $n_x=2$），对于更高维度的复杂系统，验证工具的计算开销和界限的紧致性（Tightness）尚待进一步验证。

### 6. 论文的主要结论与发现
*   **可计算性**：成功推导出了一个完全由可验证量组成的误差界公式。
*   **有效性**：实验证明，计算出的验证界限虽然具有一定的保守性（高于实际误差），但能够为神经观测器提供可靠的“最坏情况”保证。
*   **噪声鲁棒性**：证明了 KKL 神经观测器在面对有界测量噪声时，其估计误差依然保持最终有界（Ultimate Boundedness）。

### 7. 优点
*   **理论严谨**：填补了学习型观测器缺乏可计算性能保证的空白。
*   **工具集成**：巧妙地将控制理论（Lyapunov 分析）与现代 AI 验证工具（$\alpha, \beta$-CROWN）结合。
*   **实用性**：提出的界限不依赖于未知的解析解，只需对训练好的神经网络进行离线分析即可获得。

### 8. 不足与局限
*   **维数灾难**：神经网络验证（如分支定界法）在处理高维输入空间时计算复杂度呈指数增长，限制了该方法向大型系统的扩展。
*   **界限保守性**：由于 Lipschitz 常数和 PDE 残差的验证通常给出的是过估计（Over-approximation），最终得到的误差界可能比实际误差大一个数量级。
*   **区域限制**：验证仅在预定义的紧致区域内有效，若系统状态脱离该区域，则保证失效。
*   **系统限制**：目前仅针对自治系统（无控制输入）进行了详细推导，虽提及可扩展至受控系统，但未给出具体实验。

（完）
