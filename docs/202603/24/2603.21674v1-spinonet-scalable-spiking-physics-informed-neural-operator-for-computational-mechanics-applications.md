---
title: "SPINONet: Scalable Spiking Physics-informed Neural Operator for Computational Mechanics Applications"
title_zh: SPINONet：用于计算力学应用的可扩展脉冲物理信息神经算子
authors: "Shailesh Garg, Luis Mandl, Somdatta Goswami, Souvik Chakraborty"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21674v1"
tags: ["query:pinn"]
score: 10.0
evidence: 可扩展的脉冲物理信息神经算子
tldr: 针对计算力学中物理信息算子学习模型的高能耗问题，本文提出SPINONet框架。该框架结合类脑脉冲神经元与可分离架构，通过稀疏、事件驱动的计算模式显著降低能耗，同时保留了计算时空导数所需的连续坐标微分能力。在多种偏微分方程任务中，SPINONet在保持预测精度的同时，展现了卓越的计算效率和在复杂场景下的鲁棒性。
selection_source: fresh_fetch
motivation: 传统物理信息算子学习在边缘设备等功耗受限场景下，因密集网络的重复评估而面临巨大的计算和能源开销。
method: 提出一种架构感知的类脑脉冲算子网络，利用回归友好的脉冲神经元实现稀疏事件驱动计算，并支持物理信息的自动微分。
result: 在多种偏微分方程测试中，SPINONet取得了与传统模型相当的预测精度，并有效减少了计算负载。
conclusion: SPINONet证明了类脑脉冲机制在科学计算中的可行性，为低功耗、高效率的物理信息机器学习提供了新路径。
---

## 摘要
能效仍然是在计算力学和科学计算中部署物理信息算子学习模型的一个关键挑战，特别是在边缘和嵌入式设备等功耗受限的环境中，密集网络中重复的算子评估会产生巨大的计算和能量成本。为了应对这一挑战，我们引入了可分离物理信息类脑算子网络（SPINONet），这是一个受神经科学启发的框架，在保持与物理信息训练兼容的同时，减少了重复评估中的冗余计算。SPINONet 通过架构感知设计结合了回归友好的类脑脉冲神经元，实现了稀疏、事件驱动的计算，在提高能效的同时，保留了计算时空导数所需的连续、坐标可微路径。我们在代表计算力学问题的一系列偏微分方程上评估了 SPINONet，这些方程在时变和稳态设置中具有空间、时间和参数依赖性，并证明了尽管引入了稀疏通信，其预测性能仍可与传统的物理信息算子学习方法相媲美。此外，研究表明，在混合设置中，有限的数据监督可以提高在纯物理信息训练可能收敛到伪解的挑战性情况下的性能。最后，我们提供了一个分析性讨论，将 SPINONet 的架构组件和设计选择与计算负载和能耗的降低联系起来。

## Abstract
Energy efficiency remains a critical challenge in deploying physics-informed operator learning models for computational mechanics and scientific computing, particularly in power-constrained settings such as edge and embedded devices, where repeated operator evaluations in dense networks incur substantial computational and energy costs. To address this challenge, we introduce the Separable Physics-informed Neuroscience-inspired Operator Network (SPINONet), a neuroscience-inspired framework that reduces redundant computation across repeated evaluations while remaining compatible with physics-informed training. SPINONet incorporates regression-friendly neuroscience-inspired spiking neurons through an architecture-aware design that enables sparse, event-driven computation, improving energy efficiency while preserving the continuous, coordinate-differentiable pathways required for computing spatio-temporal derivatives. We evaluate SPINONet on a range of partial differential equations representative of computational mechanics problems, with spatial, temporal, and parametric dependencies in both time-dependent and steady-state settings, and demonstrate predictive performance comparable to conventional physics-informed operator learning approaches despite the induced sparse communication. In addition, limited data supervision in a hybrid setup is shown to improve performance in challenging regimes where purely physics-informed training may converge to spurious solutions. Finally, we provide an analytical discussion linking architectural components and design choices of SPINONet to reductions in computational load and energy consumption.

---

## 论文详细总结（自动生成）

这是一份关于论文《SPINONet: Scalable Spiking Physics-informed Neural Operator for Computational Mechanics Applications》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**物理信息神经算子（PINO）在计算力学应用中的能效与可扩展性瓶颈**。
*   **背景**：虽然物理信息学习（如 PINNs 和 DeepONet）在处理偏微分方程（PDE）方面表现出色，但其密集的计算模式（每个神经元在每次评估时都处于激活状态）导致在边缘设备或嵌入式系统上部署时能耗极高。
*   **动机**：受生物大脑启发，脉冲神经网络（SNN）通过稀疏的“事件驱动”通信实现极低功耗。然而，脉冲神经元的离散/不连续特性与物理信息学习中所需的“连续坐标求导”存在天然冲突。
*   **核心目标**：开发一种既能利用脉冲神经元的稀疏节能特性，又能保持物理约束（残差计算）所需的高阶导数计算能力的神经算子架构。

### 2. 方法论：核心思想与关键技术
论文提出了 **SPINONet（可分离物理信息类脑算子网络）**，其核心思想是通过**架构解耦**解决脉冲动力学与物理求导的冲突：
*   **架构解耦**：将算子分为 **Branch（分支）网络**和 **Trunk（躯干）网络**。
    *   **Branch 网络**：引入**可变脉冲神经元（VSN）**。它负责处理输入函数（如初始条件或参数），输出稀疏的脉冲信号。由于输入函数在物理残差计算中通常是代数进入的，不涉及对坐标的求导，因此可以使用离散脉冲。
    *   **Trunk 网络**：保持为**连续、可微的传统人工神经网络（ANN）**。它负责处理空间和时间坐标，确保可以通过自动微分计算 PDE 所需的时空导数。
*   **可分离构造（Separable Structure）**：采用坐标轴分解技术，将 $d$ 维问题的 Trunk 网络分解为 $d$ 个 1 维网络，通过外积组合。这使得计算量从网格规模的乘法关系变为加法关系，极大提升了扩展性。
*   **关键技术细节**：
    *   **VSN 模型**：采用支持连续值脉冲的 VSN，使其更适合回归任务。
    *   **代理梯度（Surrogate Gradient）**：在训练反向传播时，使用平滑函数替代不可微的脉冲阈值函数。
    *   **前向模式自动微分（FAD）**：利用可分离架构的特性，采用 FAD 显著降低高维残差计算的内存和计算开销。

### 3. 实验设计
论文在三个具有代表性的计算力学 PDE 场景中进行了评估：
*   **场景 E-I：1D 粘性 Burgers 方程**（非线性对流扩散）。
*   **场景 E-II：2D 参数化热传导方程**（高维时空+参数依赖，总维度 $d=4$）。
*   **场景 E-III：2D 参数化边界 Eikonal 方程**（几何输入表示，符号距离函数学习）。
*   **Benchmark 与对比方法**：
    *   **PI-DeepONet**：标准的物理信息 DeepONet（非脉冲、非分离）。
    *   **Vanilla Baseline**：可分离的物理信息 DeepONet（非脉冲）。
    *   **SPINONet**：本文提出的脉冲+可分离架构。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明具体的 GPU 型号和数量**。
*   **性能指标**：论文详细记录了不同网格分辨率下的 **GPU 显存占用（GB）**和**每轮训练时长（秒/Epoch）**。
*   **训练时长**：Burgers 和 Eikonal 方程训练了 40k 轮，热传导方程训练了 100k 轮。实验结果显示，SPINONet 在高分辨率网格下的显存和时间开销远低于 PI-DeepONet。

### 5. 实验数量与充分性
*   **实验组数**：针对三个 PDE 场景分别进行了精度对比、效率对比（随网格规模变化）以及脉冲活动率分析。
*   **充分性**：实验涵盖了稳态与瞬态、低维与高维、物理参数化与几何参数化，维度最高达到 4 维。
*   **客观性**：论文不仅展示了精度（L2 误差），还客观地分析了脉冲神经元的平均活动率（28% - 65%），并讨论了 Eikonal 方程中纯物理训练可能出现的“伪解”现象，提出了混合监督（少量数据辅助）的解决方案，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **精度保持**：SPINONet 在引入稀疏脉冲通信后，预测精度与全激活的密集网络（Vanilla Baseline）相当（误差在同一数量级）。
*   **显著节能潜力**：Branch 网络的神经元活动率显著降低（平均稀疏度在 35%-72% 之间），这意味着在神经形态硬件上可实现大幅能耗削减。
*   **卓越的扩展性**：得益于可分离架构和 FAD，SPINONet 能够处理 PI-DeepONet 因显存溢出而无法处理的大规模高维网格问题。
*   **稳定性提升**：在 Eikonal 方程等复杂任务中，通过引入少量数据监督，SPINONet 能有效避免物理信息学习常见的收敛至伪解（如符号翻转）的问题。

### 7. 优点
*   **架构创新**：巧妙地利用 DeepONet 的解耦特性，将脉冲神经元限制在不影响求导的 Branch 路径，解决了 SNN 与 PINN 结合的根本矛盾。
*   **效率极高**：结合了“可分离架构”和“脉冲稀疏性”双重优势，是目前已知最节能的算子学习框架之一。
*   **理论支撑**：提供了关于 FAD 效率和 SNN 能耗的硬件无关性分析，具有较强的理论指导意义。

### 8. 不足与局限
*   **硬件依赖性**：虽然理论上节能，但目前的实验仍是在传统 GPU 上模拟 SNN，其实际节能效果需在专门的神经形态芯片（如 Intel Loihi）上才能完全体现。
*   **超参数敏感性**：脉冲神经元的阈值、泄漏率以及代理梯度的斜率等超参数可能需要精细调优。
*   **复杂几何限制**：对于极度复杂的几何边界，单纯依靠 Branch 网络的脉冲编码可能存在表达力瓶颈，且 Eikonal 方程的实验表明其对纯物理训练的鲁棒性仍有待提高。

（完）
