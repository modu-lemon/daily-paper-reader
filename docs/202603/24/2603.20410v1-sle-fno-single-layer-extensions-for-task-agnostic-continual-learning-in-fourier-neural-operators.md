---
title: "SLE-FNO: Single-Layer Extensions for Task-Agnostic Continual Learning in Fourier Neural Operators"
title_zh: SLE-FNO：用于傅里叶神经算子中任务无关持续学习的单层扩展
authors: "Mahmoud Elhadidy, Roshan M. D'Souza, Amirhossein Arzani"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20410v1"
tags: ["query:pinn"]
score: 10.0
evidence: 傅里叶神经算子的持续学习框架
tldr: 本研究针对科学机器学习中傅里叶神经算子（FNO）在处理分布偏移时面临的灾难性遗忘问题，提出了一种名为SLE-FNO的单层扩展架构。该方法通过在FNO中结合单层扩展（SLE）实现高效的持续学习，在动脉瘤血流模拟任务中表现优异，实现了零遗忘和极低参数增量，为流体力学等领域的模型外推提供了稳健的解决方案。
selection_source: fresh_fetch
motivation: 传统的科学机器学习模型在面对流体力学中几何或边界条件变化导致的分布偏移时，容易出现灾难性遗忘且难以有效外推。
method: 提出SLE-FNO架构，通过在傅里叶神经算子中引入单层扩展（SLE）机制，实现任务无关的持续学习。
result: 在脉动动脉瘤血流模拟的回归任务中，SLE-FNO在保持高精度的同时实现了零遗忘，且新增参数量极小，优于LoRA和重放类等多种基准方法。
conclusion: SLE-FNO在塑性和稳定性之间达到了最佳平衡，是解决科学计算中模型外推和持续适应问题的有效策略。
---

## 摘要
科学机器学习越来越多地用于构建代理模型，但大多数模型是在一个限制性假设下训练的，即未来数据遵循与训练集相同的分布。在实践中，新的实验条件或模拟方案可能存在显著差异，需要在不重新访问先前数据的情况下进行外推和模型更新。这产生了对持续学习（CL）框架的需求，这些框架能够适应分布偏移，同时防止灾难性遗忘。这种挑战在流体动力学中尤为突出，其中几何形状、边界条件或流态的变化会导致解发生显著变化。在此，我们介绍了一种新的基于架构的方法（SLE-FNO），它将单层扩展（SLE）与傅里叶神经算子（FNO）相结合，以支持高效的持续学习。在图像到图像的回归设置中，我们将 SLE-FNO 与一系列成熟的持续学习方法进行了比较，包括弹性权重整合（EWC）、不忘学习（LwF）、基于重放的方法、正交梯度下降（OGD）、梯度情景记忆（GEM）、PiggyBack 和低秩近似（LoRA）。模型被训练用于在脉动动脉瘤血流中将瞬态浓度场映射到时间平均壁面剪切应力（TAWSS）。任务源自 230 个计算流体动力学模拟，这些模拟被分为四个顺序且分布外（OOD）的配置。结果表明，基于重放的方法和基于架构的方法（PiggyBack、LoRA 和 SLE-FNO）实现了最佳的记忆保持，其中 SLE-FNO 在可塑性和稳定性之间提供了最强的整体平衡，以极少的额外参数实现了零遗忘的准确性。我们的研究结果强调了持续学习算法之间的关键差异，并介绍了 SLE-FNO 作为一种在需要外推时调整基准模型的极具前景的策略。

## Abstract
Scientific machine learning is increasingly used to build surrogate models, yet most models are trained under a restrictive assumption in which future data follow the same distribution as the training set. In practice, new experimental conditions or simulation regimes may differ significantly, requiring extrapolation and model updates without re-access to prior data. This creates a need for continual learning (CL) frameworks that can adapt to distribution shifts while preventing catastrophic forgetting. Such challenges are pronounced in fluid dynamics, where changes in geometry, boundary conditions, or flow regimes induce non-trivial changes to the solution. Here, we introduce a new architecture-based approach (SLE-FNO) combining a Single-Layer Extension (SLE) with the Fourier Neural Operator (FNO) to support efficient CL. SLE-FNO was compared with a range of established CL methods, including Elastic Weight Consolidation (EWC), Learning without Forgetting (LwF), replay-based approaches, Orthogonal Gradient Descent (OGD), Gradient Episodic Memory (GEM), PiggyBack, and Low-Rank Approximation (LoRA), within an image-to-image regression setting. The models were trained to map transient concentration fields to time-averaged wall shear stress (TAWSS) in pulsatile aneurysmal blood flow. Tasks were derived from 230 computational fluid dynamics simulations grouped into four sequential and out-of-distribution configurations. Results show that replay-based methods and architecture-based approaches (PiggyBack, LoRA, and SLE-FNO) achieve the best retention, with SLE-FNO providing the strongest overall balance between plasticity and stability, achieving accuracy with zero forgetting and minimal additional parameters. Our findings highlight key differences between CL algorithms and introduce SLE-FNO as a promising strategy for adapting baseline models when extrapolation is required.

---

## 论文详细总结（自动生成）

以下是对论文《SLE-FNO: Single-Layer Extensions for Task-Agnostic Continual Learning in Fourier Neural Operators》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：科学机器学习（SciML）模型（如代理模型）通常假设测试数据与训练数据分布一致。但在实际应用中，新的几何形状、边界条件或流态会导致“分布偏移”（OOD）。
*   **核心问题**：直接对新数据进行微调会导致模型对旧知识的“灾难性遗忘”。现有的持续学习（CL）方法多针对分类任务，在处理复杂的科学计算回归问题（如流体动力学中的函数到函数映射）时表现不足。
*   **研究目标**：为傅里叶神经算子（FNO）开发一种高效、任务无关且能完全消除遗忘的持续学习框架。

### 2. 论文提出的方法论：SLE-FNO
*   **核心思想**：采用基于架构的扩展策略。保持预训练的 FNO 主干网络（Backbone）完全冻结，仅为每个新任务添加一个轻量级的“单层扩展”（Single-Layer Extension, SLE）。
*   **关键技术细节**：
    *   **冻结主干**：利用预训练模型捕获的基础物理特征。
    *   **单层扩展（SLE）**：每个新任务对应一个独立的 FNO 层，该层学习残差修正，将其输出与主干网络的输出相加。
    *   **任务无关路由（Task-Agnostic Routing）**：引入基于**核主成分分析（Kernel PCA, KPCA）**的分布检测器。在推理时，KPCA 根据输入数据的重构误差自动识别所属任务，并路由至对应的 SLE 分支，无需人工提供任务标签。
    *   **参数效率**：每个新任务仅增加总参数量的 1.5% 到 4.4%。

### 3. 实验设计
*   **应用场景**：脉动动脉瘤血流模拟。任务是将瞬态浓度场（5个时间步）映射到时间平均壁面剪切应力（TAWSS）分布图。
*   **数据集**：基于 230 个 CFD 模拟，分为四个顺序任务（A → B → C → D）。
    *   **任务 A**：基准任务（160个训练样本）。
    *   **任务 B/C/D**：小样本 OOD 任务（每个仅 8 个训练样本），涉及几何参数和雷诺数的显著变化。
*   **对比方法（Baselines）**：涵盖了 CL 的主要家族：
    *   正则化类：EWC, LwF。
    *   重放类：随机重放（Reservoir）、K-means 重放。
    *   优化类：OGD, GEM。
    *   架构类：PiggyBack, LoRA。
    *   基准：朴素微调（Naive Fine-tuning）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练配置**：提到基准任务训练 3000 轮（epochs），后续 CL 阶段各训练 1500 轮。学习率在 $10^{-2}$ 到 $10^{-4}$ 之间，使用了 Sobolev 范数损失函数以增强空间正则性。

### 5. 实验数量与充分性
*   **实验规模**：进行了 4 个阶段的纵向持续学习实验，对比了 10 种不同的算法。
*   **充分性评价**：实验设计较为充分。不仅提供了定量指标（相对 L2 误差、平均准确度、遗忘度），还提供了定性的 TAWSS 误差图分析。通过跨越不同流态（雷诺数变化）和几何形状的任务序列，验证了模型在极端分布偏移下的鲁棒性。

### 6. 主要结论与发现
*   **SLE-FNO 表现最优**：在保持高精度的同时实现了**零遗忘**，在最终的平均准确度上超过了所有对比方法。
*   **重放类方法有效但受限**：基于重放的方法（尤其是 K-means 重放）在缓解遗忘方面表现稳健，但需要存储历史数据，存在隐私和内存压力。
*   **正则化与优化类方法在 SciML 中受挫**：EWC 和 LwF 在面对剧烈的物理分布偏移时保护效果有限；OGD 和 GEM 随着任务增加，约束变得过于僵硬，导致新任务学习能力下降。
*   **任务识别准确**：基于 KPCA 的路由机制在实验中达到了 100% 的任务识别准确率。

### 7. 优点
*   **零遗忘特性**：通过参数隔离彻底解决了灾难性遗忘问题。
*   **任务无关推理**：集成的 OOD 检测器使得模型在部署时不需要知道当前属于哪个任务，增强了实用性。
*   **参数极其高效**：相比于复制整个网络，SLE 仅增加极少量参数，适合长期持续学习。
*   **物理一致性**：结合了 FNO 的谱偏置特性，能够用简单的线性层修正捕获复杂的高频物理变化。

### 8. 不足与局限
*   **分支线性增长**：虽然单层扩展很轻量，但随着任务数量无限增加，分支数量仍会线性增长，可能在极端长期场景下产生管理开销。
*   **KPCA 依赖性**：路由的准确性高度依赖于 KPCA 对输入分布的区分度，若不同任务的输入空间高度重叠，路由可能失效。
*   **实验场景单一**：目前仅在动脉瘤血流这一特定生物流体问题上验证，尚未在更广泛的物理场（如湍流、结构力学）中测试。

（完）
