---
title: Real-time control of multiphase processes with learned operators
title_zh: 基于学习算子的多相过程实时控制
authors: "Paolo Guida, Didier Barradas-Bautista"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25308v1"
tags: ["query:pinn"]
score: 9.0
evidence: 学习算子用于多相流的实时控制
tldr: 针对多相流过程控制中数值模型计算量大、难以实时应用的问题，本文提出了一种基于学习算子的代理辅助模型预测控制（MPC）框架。该框架利用傅里叶神经算子（FNO）预测相指示场的时空演化，并将其嵌入优化过程以识别最佳控制变量。在双相气泡塔液位控制任务中，该方法实现了高效的闭环优化，为快速多相单元操作的实时控制提供了可行路径。
selection_source: fresh_fetch
motivation: 多相流动力学具有强非线性且数值模拟计算成本极高，限制了其在实时控制算法中的应用。
method: 提出一种基于傅里叶神经算子（FNO）的代理模型，用于快速预测时空演化并辅助模型预测控制（MPC）进行优化。
result: 在双相气泡塔液位跟踪任务中，FNO 代理模型能够以较低的计算成本准确预测系统状态并实现有效的闭环控制。
conclusion: 学习算子为复杂多相流过程的实时模型预测控制提供了一种高效且实用的解决方案。
---

## 摘要
多相流在自然界和制造设备中频繁出现。由于强非线性动力学、快速相变以及现有传感器有限的时空分辨率，控制此类现象极具挑战性，这可能导致在预测和管理这些流动时出现显著的不准确性。在大多数情况下，数值模型是获取高时空分辨率数据以实现精细控制的唯一途径。虽然在控制算法中嵌入数值模型可以实现对多相过程的精细控制，但巨大的计算负担目前限制了它们的实际应用。本研究提出了一种利用学习算子调节多相过程的代理辅助模型预测控制（MPC）框架。训练傅里叶神经算子（FNO），根据近期状态的短期历史和候选驱动信号，预测有限时域内相指示场（体积分数）的时空演化。然后在优化过程中迭代调用神经算子代理，以确定最佳控制变量。为了说明该方法，我们解决了双相欧拉气泡柱上的最优控制问题（OCP）。在此，控制器通过调节引入系统的气体流速来跟踪分段常数液位设定点。研究结果表明，利用 FNO 进行场级预测非常适合闭环优化，因为它们的评估成本相对较低。后者为快速多相单元操作的 MPC 提供了一条实用路径，并为未来扩展到部分可观测性和物理信息算子学习奠定了基础。

## Abstract
Multiphase flows frequently occur naturally and in manufactured devices. Controlling such phenomena is extremely challenging due to the strongly non-linear dynamics, rapid phase transitions, and the limited spatial and temporal resolution of available sensors, which can lead to significant inaccuracies in predicting and managing these flows. In most cases, numerical models are the only way to access high spatial and temporal resolution data to an extent that allows for fine control. While embedding numerical models in control algorithms could enable fine control of multiphase processes, the significant computational burden currently limits their practical application. This work proposes a surrogate-assisted model predictive control (MPC) framework for regulating multiphase processes using learned operators. A Fourier Neural Operator (FNO) is trained to forecast the spatiotemporal evolution of a phase-indicator field (the volume fraction) over a finite horizon from a short history of recent states and a candidate actuation signal. The neural operator surrogate is then iteratively called during the optimisation process to identify the optimal control variable. To illustrate the approach, we solve an optimal control problem (OCP) on a two-phase Eulerian bubble column. Here, the controller tracks piecewise-constant liquid level setpoints by adjusting the gas flow rate introduced into the system. The results we obtained indicate that field-level forecasting with FNOs are well suited for closed-loop optimization since they have relatively low evaluation cost. The latter provide a practical route toward MPC for fast multiphase unit operations and a foundation for future extensions to partial observability and physics-informed operator learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Real-time control of multiphase processes with learned operators》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：多相流（如气泡塔、增材制造、电池产气等）在工业中无处不在，但其动力学具有强非线性、快速瞬态和复杂的相变特征。
*   **核心痛点**：传统的计算流体力学（CFD）模型虽然精确，但计算成本极高，无法直接嵌入需要高频迭代的实时模型预测控制（MPC）回路中。此外，物理传感器往往存在时空分辨率不足的问题。
*   **研究目标**：开发一种基于深度学习算子的快速代理模型，替代高昂的 CFD 模拟，实现对复杂多相流过程的实时、闭环精细控制。

### 2. 方法论
*   **核心思想**：将**傅里叶神经算子（FNO）**作为代理模型嵌入 MPC 框架中，利用其在函数空间学习映射的能力，快速预测未来时空的场演化。
*   **关键技术细节**：
    *   **FNO 代理模型**：输入为过去 $K$ 个时刻的体积分数场快照、空间坐标通道以及候选控制信号（气流速度）；输出为未来 $H$ 个时刻的预测场。
    *   **算子学习**：利用 FFT（快速傅里叶变换）在谱域捕捉全局空间依赖关系，使模型具有准实时的推理速度。
    *   **神经模型预测控制（NMPC）**：在每个采样时刻，控制器调用 FNO 进行多次“滚动预测”（Rollouts），评估不同控制动作下的系统响应。
    *   **优化算法**：由于从体积分数场提取液位（Level Extraction）涉及阈值操作，导致目标函数非光滑且不可微，因此采用了**贝叶斯优化（BO）**来寻找最优控制输入，而非梯度下降法。

### 3. 实验设计
*   **实验场景**：二维气泡塔反应器（Bubble Column Reactor）。目标是通过调节底部的气体输入流速，使液体高度（液位）跟踪预设的参考曲线。
*   **数据集生成**：使用 OpenFOAM 的 `twoPhaseEulerFoam` 求解器生成训练数据。通过随机改变输入速度序列，模拟并记录了体积分数 $\alpha$ 的时空演化。
*   **Benchmark 与对比**：
    *   验证了 FNO 在不同学习率下的收敛性。
    *   在验证集和测试集上对比了 MSE（均方误差）、MAE（平均绝对误差）和 SSIM（结构相似性指数）。
    *   闭环控制实验中，对比了测量液位与分段常数设定点（Setpoint）的跟踪精度。

### 4. 资源与算力
*   **硬件环境**：单台 **NVIDIA Quadro GV100 (32 GB)** GPU。
*   **软件框架**：Python 3.11, PyTorch 2.10.0, CuPy 13.6.0, CUDA 12.2。
*   **性能表现**：
    *   **训练加速**：通过 GPU 驻留预加载和 CuPy 预处理，训练速度比基准提升了 **3.09 倍**。
    *   **推理效率**：推理吞吐量达到 **19,711–24,508 样本/秒**，单个样本推理耗时仅约 **0.04-0.05 毫秒**。

### 5. 实验数量与充分性
*   **数据规模**：生成了包含 11,900 个样本的窗口化数据集。
*   **实验覆盖**：
    *   进行了多组不同学习率的消融实验（图 2）。
    *   展示了 5 步预测时域内的场演化对比（图 4）。
    *   进行了长达 200 秒的连续闭环控制实验，涵盖了多个液位设定点的切换。
*   **评价**：实验设计较为客观，充分展示了模型从离线训练到在线推理再到闭环控制的全过程。但实验场景仅限于单一的气泡塔模型，缺乏对不同几何拓扑或更高维度（3D）场景的泛化性验证。

### 6. 主要结论与发现
*   **实时性验证**：FNO 代理模型能够以毫秒级的延迟提供准确的场预测，完全满足实时控制的需求。
*   **控制效果**：基于 FNO 的 MPC 能够稳定跟踪液位设定点，在设定点切换时响应迅速且超调较小。
*   **稳态误差**：在极端设定点（如极高或极低液位）处存在微小的系统性偏差，这与执行器（气流速度）的边界限制有关。

### 7. 优点
*   **场级预测（Field-level Forecasting）**：不同于仅预测标量的模型，该方法保留了完整的空间结构信息，为未来控制复杂的空间分布（如催化剂分布）奠定了基础。
*   **极高的计算效率**：相比直接运行 CFD，FNO 的推理速度提升了数个数量级，使 MPC 在多相流领域的应用成为可能。
*   **框架灵活性**：结合了算子学习的泛化能力和贝叶斯优化的鲁棒性，能够处理非光滑的控制目标。

### 8. 不足与局限
*   **细节解析度**：FNO 在预测时倾向于平滑场数据，导致液相内部的一些细微气泡结构（Fine-scale structures）无法被完全还原。
*   **全场观测假设**：实验假设可以获得完整的体积分数场作为输入，但在实际工业应用中，通常只能获得稀疏的传感器数据（部分可观测性）。
*   **优化维度限制**：由于使用了贝叶斯优化，当控制变量（Action Space）维度显著增加时，可能会遇到维度灾难，计算成本将大幅上升。

（完）
