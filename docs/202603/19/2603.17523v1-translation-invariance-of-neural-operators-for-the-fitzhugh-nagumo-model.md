---
title: Translation Invariance of Neural Operators for the FitzHugh-Nagumo Model
title_zh: FitzHugh-Nagumo 模型中神经算子的平移不变性
authors: Luca Pellegrini
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17523v1"
tags: ["query:pinn"]
score: 10.0
evidence: 用于FitzHugh-Nagumo偏微分方程的神经算子
tldr: 本研究探讨了神经算子（NOs）在捕捉FitzHugh-Nagumo模型复杂时空动力学中的平移不变性。通过一种新颖的训练策略，即在固定时间改变电流位置和强度进行训练，并在时空平移的分布外场景下进行测试，评估了包括CNO、FNO和DON在内的七种架构。研究发现CNO在处理平移测试动态方面表现出色，而FNO虽然训练误差最低但推理较慢且泛化性较差，为复杂离子模型建模提供了重要的基准参考。
selection_source: fresh_fetch
motivation: 旨在评估神经算子在处理FitzHugh-Nagumo模型中具有挑战性的刚性时空动力学及平移不变性方面的能力。
method: 采用一种新颖的训练策略，在固定时间改变输入电流的空间位置和强度，并对比评估了七种主流神经算子架构的性能。
result: CNO在平移动态测试中表现最佳但训练成本高，FNO训练误差最低但推理慢且泛化差，而DON系列虽然高效但泛化能力有限。
conclusion: 研究揭示了不同神经算子在捕捉复杂离子模型动力学方面的优劣，并为涉及平移动态的应用场景提供了全面的基准测试。
---

## 摘要
神经算子（Neural Operators, NOs）是一种强大的深度学习框架，旨在学习偏微分方程产生的解算子。本研究探讨了 NOs 捕捉 FitzHugh-Nagumo 模型中刚性时空动力学的能力，该模型用于描述可兴奋细胞。本工作的一个关键贡献是利用一种新颖的训练策略评估了平移不变性。NOs 在固定时间的具有不同空间位置和强度的外加电流下进行训练，而测试集则引入了更具挑战性的分布外（out-of-distribution）场景，其中外加电流在时间和空间上均发生了平移。这种方法显著降低了数据集生成的计算成本。此外，我们对七种 NOs 架构进行了基准测试：卷积神经算子（CNOs）、深度算子网络（DONs）、带有 CNN 编码器的 DONs（DONs-CNN）、本征正交分解 DONs（POD-DONs）、傅里叶神经算子（FNOs）、Tucker 张量化 FNOs（TFNOs）以及局部神经算子（LocalNOs）。我们根据训练和测试精度、效率以及推理速度对这些模型进行了评估。我们的结果表明，CNOs 在平移后的测试动力学上表现良好。然而，尽管它们在训练集上的表现与所考虑的其他架构相似，但其训练成本较高。相比之下，FNOs 实现了最低的训练误差，但推理时间最长。关于平移后的动力学，FNOs 及其变体提供的预测准确性较低。最后，DONs 及其变体在训练和推理中均表现出高效率，但它们在测试集上的泛化能力较差。这些发现突出了 NOs 在捕捉复杂离子模型动力学方面的当前能力和局限性，并提供了一个全面的基准，包括它们在涉及平移动力学场景中的应用。

## Abstract
Neural Operators (NOs) are a powerful deep learning framework designed to learn the solution operator that arise from partial differential equations. This study investigates NOs ability to capture the stiff spatio-temporal dynamics of the FitzHugh-Nagumo model, which describes excitable cells. A key contribution of this work is evaluating the translation invariance using a novel training strategy. NOs are trained using an applied current with varying spatial locations and intensities at a fixed time, and the test set introduces a more challenging out-of-distribution scenario in which the applied current is translated in both time and space. This approach significantly reduces the computational cost of dataset generation. Moreover we benchmark seven NOs architectures: Convolutional Neural Operators (CNOs), Deep Operator Networks (DONs), DONs with CNN encoder (DONs-CNN), Proper Orthogonal Decomposition DONs (POD-DONs), Fourier Neural Operators (FNOs), Tucker Tensorized FNOs (TFNOs), Localized Neural Operators (LocalNOs). We evaluated these models based on training and test accuracy, efficiency, and inference speed. Our results reveal that CNOs performs well on translated test dynamics. However, they require higher training costs, though their performance on the training set is similar to that of the other considered architectures. In contrast, FNOs achieve the lowest training error, but have the highest inference time. Regarding the translated dynamics, FNOs and their variants provide less accurate predictions. Finally, DONs and their variants demonstrate high efficiency in both training and inference, however they do not generalize well to the test set. These findings highlight the current capabilities and limitations of NOs in capturing complex ionic model dynamics and provide a comprehensive benchmark including their application to scenarios involving translated dynamics.

---

## 论文详细总结（自动生成）

这篇论文对神经算子（Neural Operators, NOs）在解决生物电生理学中经典的 FitzHugh-Nagumo (FHN) 模型时的表现进行了深入的基准测试，特别关注了模型对“平移不变性”的捕捉能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：评估不同神经算子架构在处理具有**强刚性（Stiff）**时空动力学的 FHN 模型时的准确性、效率及泛化能力。
*   **研究背景**：FHN 模型是描述神经元或心肌细胞电活动的基础模型，由反应扩散方程和刚性常微分方程耦合而成。传统的数值解法计算成本高，而神经算子虽能加速求解，但其在面对**分布外（OOD）平移场景**（即刺激发生时间和位置改变）时的表现尚不明确。
*   **研究动机**：利用物理系统中的“平移不变性”来减少训练数据的生成成本，并测试模型是否能真正学习到算子的本质属性而非简单的模式匹配。

### 2. 方法论与核心思想
*   **核心思想**：提出一种**基于平移不变性的训练策略**。
    *   **训练集**：固定刺激发生的时间段，仅改变刺激的强度和空间位置。
    *   **测试集**：引入时间平移和空间平移，构建分布外（OOD）场景。
*   **关键技术细节**：
    *   **算子定义**：学习从外加电流 $I_{app}(x, t)$ 到跨膜电位 $V(x, t)$ 和恢复变量 $w(x, t)$ 的映射。
    *   **架构对比**：涵盖了基于卷积的 **CNO**、基于基函数的 **DON** 及其变体（CNN 编码器、POD 分解）、以及基于频谱的 **FNO** 及其变体（Tucker 张量化、局部化算子）。
    *   **损失函数**：采用数据驱动的相对 $L^2$ 范数。

### 3. 实验设计
*   **数据集/场景**：1D FitzHugh-Nagumo 模型。
    *   **训练集**：2000 个样本（固定时间刺激）。
    *   **验证/测试集**：500 个样本（时空随机平移刺激）。
*   **Benchmark 对象**：对比了 7 种主流神经算子：
    1.  CNO (Convolutional Neural Operators)
    2.  DON (Deep Operator Networks)
    3.  DON-CNN
    4.  POD-DON
    5.  FNO (Fourier Neural Operators)
    6.  TFNO (Tucker Tensorized FNO)
    7.  LocalNO (Localized Neural Operators)
*   **评估指标**：训练/测试误差（$L^1, L^2$）、参数量、训练时长、推理延迟、综合成本函数（$C$ 和 $C_{log}$）。

### 4. 资源与算力
*   **硬件环境**：
    *   **自动调参**：使用 2 个节点，每个节点配备 2 块 **NVIDIA A16 GPU**。
    *   **单次训练**：使用 1 块 **NVIDIA A16 GPU**。
    *   **推理测试**：使用配备 **NVIDIA GeForce RTX 4070** 的笔记本电脑。
*   **训练时长**：
    *   **CNO**：约 10.6 小时（最慢）。
    *   **FNO**：约 4 小时。
    *   **DON 系列**：15 至 30 分钟（最快）。

### 5. 实验数量与充分性
*   **实验规模**：每种架构均进行了 100 次分布式的自动超参数搜索（使用 HyperNOs 库和 Ray 框架）。
*   **重复性**：最终结果基于 5 次不同随机种子的运行取平均值和标准差。
*   **公平性**：通过自动超参调优（ASHA 调度器和 TPE 算法）确保了每种架构都处于其最优配置下进行对比，实验设计客观且严谨。

### 6. 主要结论与发现
*   **CNO 的优越泛化性**：CNO 是唯一能够有效捕捉平移不变性并在测试集（平移动态）上保持良好精度的模型（中位 $L^2$ 误差约 0.09），但其训练成本最高。
*   **FNO 的训练优势与推理劣势**：FNO 在训练集上精度最高（$L^2$ 误差达 $10^{-3}$ 级别），但推理速度最慢，且在平移测试集上泛化能力较差。
*   **DON 的高效性**：DON 及其变体在训练和推理速度上具有压倒性优势，但几乎无法处理平移后的 OOD 场景。
*   **权衡关系**：研究揭示了精度、推理速度与泛化能力之间的显著权衡（Trade-off）。

### 7. 优点与亮点
*   **策略创新**：通过物理属性（平移不变性）设计训练/测试分割，这比随机分割更具挑战性，也更能反映模型对 PDE 本质的理解。
*   **全面基准**：不仅对比了精度，还引入了考虑参数量和训练时间的综合成本函数，为实际应用提供了选型指南。
*   **自动化程度高**：利用 HyperNOs 进行大规模并行调参，消除了人工调参带来的偏差。

### 8. 不足与局限
*   **阈值效应（Threshold Effect）**：所有 NOs 架构在处理“刺激是否足以触发动作电位”这一非线性临界点时都表现不佳，容易产生离群值误差。
*   **维度限制**：实验目前仅限于 1D 空间，尚未在更复杂的 2D/3D 几何结构或真实心脏几何中验证。
*   **CNO 的计算开销**：虽然 CNO 泛化性好，但其极高的训练耗时限制了其在大规模问题上的快速迭代。

（完）
