---
title: "RHYME-XT: A Neural Operator for Spatiotemporal Control Systems"
title_zh: RHYME-XT：一种用于时空控制系统的神经算子
authors: "Marijn Ruiter, Miguel Aguiar, Jake Rap, Karl H. Johansson, Amritam Das"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17867v1"
tags: ["query:pinn"]
score: 9.0
evidence: 用于时空控制系统的神经算子框架
tldr: RHYME-XT 是一种针对具有局部节律行为的时空控制系统（由输入仿射非线性偏积分微分方程 PIDE 描述）的算子学习框架。它通过 Galerkin 投影将高维 PIDE 映射到学习到的有限维子空间，并直接学习其流映射，从而避免了复杂的数值积分，实现了连续时间且离散不变的代理建模。实验证明其在神经场 PIDE 任务中优于现有技术，并具备出色的跨模型迁移学习能力。
selection_source: fresh_fetch
motivation: 旨在解决具有局部节律特征的时空控制系统在代理建模中计算成本高且难以实现离散不变性的挑战。
method: 利用 Galerkin 投影将 PIDE 投影到由神经网络参数化的空间基函数子空间，并直接学习投影后 ODE 系统的流映射。
result: 在神经场 PIDE 实验中，RHYME-XT 的性能超越了最先进的神经算子，并展示了高效的微调迁移能力。
conclusion: 该框架为复杂时空动力系统的实时控制和跨任务知识迁移提供了一种高效且离散不变的神经算子解决方案。
---

## 摘要
我们提出了 RHYME-XT，这是一个算子学习框架，用于对受具有局部节律行为的输入仿射非线性偏积分微分方程（PIDE）支配的时空控制系统进行代理建模。RHYME-XT 使用 Galerkin 投影，在由神经网络参数化的空间基函数构成的学习到的有限维子空间上，近似无限维 PIDE。这产生了一个由投影输入驱动的投影常微分方程（ODE）系统。我们没有对该非自治系统进行积分，而是使用一种用于学习流函数的架构直接学习其流映射，在避免昂贵计算的同时，获得了一种连续时间且离散不变的表示。在神经场 PIDE 上的实验表明，RHYME-XT 的性能优于最先进的神经算子，并且能够通过微调过程在不同数据集上训练的模型之间有效地迁移知识。

## Abstract
We propose RHYME-XT, an operator-learning framework for surrogate modeling of spatiotemporal control systems governed by input-affine nonlinear partial integro-differential equations (PIDEs) with localized rhythmic behavior. RHYME-XT uses a Galerkin projection to approximate the infinite-dimensional PIDE on a learned finite-dimensional subspace with spatial basis functions parameterized by a neural network. This yields a projected system of ODEs driven by projected inputs. Instead of integrating this non-autonomous system, we directly learn its flow map using an architecture for learning flow functions, avoiding costly computations while obtaining a continuous-time and discretization-invariant representation. Experiments on a neural field PIDE show that RHYME-XT outperforms a state-of-the-art neural operator and is able to transfer knowledge effectively across models trained on different datasets, through a fine-tuning process.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RHYME-XT** 的新型神经算子框架，旨在为受非线性偏积分微分方程（PIDE）支配的时空控制系统提供高效的代理模型。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：复杂的时空动力系统（如流体动力学、神经场模型）通常由非线性 PIDE 或 PDE 描述。传统的数值求解方法（如有限元法）在处理具有“兴奋性”或局部节律行为的系统时，面临计算成本高、对离散化方案敏感（网格越细越慢，越粗越不准）的问题。
*   **研究动机**：现有的神经算子（如 DeepONet）或物理信息神经网络（PINN）在处理随时间变化的外部输入、实现离散不变性以及长时预测方面仍存在局限。作者希望开发一种既能处理输入仿射非线性系统，又能实现连续时间表示且不依赖特定网格的通用学习框架。

### 2. 方法论
RHYME-XT 的核心思想是将 **Galerkin 投影**与**深度学习**相结合，分为三个关键组件：
*   **空间基函数学习**：使用前馈神经网络（FNN）$h_\phi$ 参数化空间基函数。为了捕捉高频特征，输入坐标 $x$ 先通过**随机傅里叶特征（RFF）**映射。
*   **流函数架构（Flow Function）**：
    *   通过 Galerkin 投影将无限维 PIDE 简化为有限维 ODE 系统。
    *   不直接进行数值积分，而是利用循环神经网络（RNN/LSTM）学习该系统的“流映射”。
    *   该架构包含编码器（映射初始状态）、RNN 单元（处理随时间变化的投影输入）和解码器（生成时间系数 $\hat{a}(t)$）。
*   **非线性输出重构**：不同于传统的线性基函数组合，RHYME-XT 使用另一个 FNN $h_u$ 对时间系数和空间基函数的阿达马积（Hadamard product）进行非线性映射，以增强表达能力。
*   **训练流程**：采用两步法。首先通过本征正交分解（POD）获取快照数据训练基函数网络；随后固定或联合微调流函数和重构网络。

### 3. 实验设计
*   **数据集/场景**：单层神经场模型（Neural Field Equation），这是一种典型的具有兴奋性和振荡行为的 PIDE 系统。
*   **Benchmark（基准）**：
    *   **DeepONet**：作为主要的对比基准，并调整其参数量与 RHYME-XT 相当。
    *   **数值真值**：使用前向欧拉法配合高精度时空离散化生成的轨迹数据。
*   **对比维度**：
    *   预测精度（相对 $\ell_2$ 误差）。
    *   长时外推能力（在 $T=50$ 的数据上训练，在 $T=100$ 和 $T=250$ 上测试）。
    *   迁移学习能力（在不同核函数的模型间进行微调）。

### 4. 资源与算力
*   **算力支持**：论文提到使用了瑞典国家超级计算学术基础设施（NAISS）的资源。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到了优化器使用 Adam，Batch Size 为 128，学习率为 $1.2 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：生成了 1000 条轨迹数据（70/20/10 分割）。
*   **实验类型**：
    *   基础精度对比实验。
    *   时间跨度外推实验（验证误差累积情况）。
    *   迁移学习实验（仅用 2% 的新数据进行微调）。
*   **充分性评价**：实验设计较为客观，通过控制参数量确保了与基准模型的公平对比。外推实验和迁移实验证明了模型的鲁棒性和实用性。不过，实验仅限于一维空间的神经场模型，尚未在更高维（如 2D/3D 流体）或更多样化的 PDE 上进行验证。

### 6. 主要结论与发现
*   **精度优势**：RHYME-XT 的相对 $\ell_2$ 误差（0.1284）显著低于 DeepONet（0.7115），能更好地捕捉轨迹中的细微节律特征。
*   **长时稳定性**：模型在训练时长 5 倍的时间跨度上仍能保持较低误差，未出现明显的误差爆炸，证明了流函数架构的稳定性。
*   **高效迁移**：通过预训练模型进行微调，仅需极少量数据即可快速适配具有不同物理参数的新系统。

### 7. 优点
*   **离散不变性**：由于采用了连续的基函数和流函数表示，模型可以在任意时空点进行推理。
*   **处理控制输入**：能够有效处理随时间变化的外部刺激（输入仿射项），这在控制系统中至关重要。
*   **无需先验知识**：不需要预先知道控制方程的具体形式或选择特定的基函数（如正弦或多项式）。

### 8. 不足与局限
*   **输入假设**：流函数架构假设输入在小时间间隔内是分段常数，这可能限制其在极高频连续变化输入下的表现。
*   **两步训练**：依赖 POD 预处理和两步训练法，增加了流程的复杂性。
*   **维度局限**：目前的数值验证仅限于 1D 空间，对于高维复杂几何区域的适用性有待进一步实验支撑。
*   **计算开销**：虽然推理快，但 RNN 结构的训练在处理极长序列时可能存在梯度挑战或计算瓶颈。

（完）
