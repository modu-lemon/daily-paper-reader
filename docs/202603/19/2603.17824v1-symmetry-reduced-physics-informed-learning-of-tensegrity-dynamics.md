---
title: Symmetry-Reduced Physics-Informed Learning of Tensegrity Dynamics
title_zh: 张拉整体动力学的对称性约化物理信息学习
authors: "Jing Qin, Muhao Chen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17824v1"
tags: ["query:pinn"]
score: 9.0
evidence: 用于张拉整体动力学的对称性简化PINN
tldr: 本研究针对张拉整体结构动力学建模中忽略几何对称性导致的高复杂度和优化不稳定问题，提出了对称性约简物理信息神经网络（SymPINN）。该框架利用群论将对称性嵌入网络架构和解的表达中，通过轨道分解和对称基表示实现降维建模，并结合傅里叶特征与两阶段优化，显著提升了动力学预测的精度与效率。
selection_source: fresh_fetch
motivation: 现有的物理信息神经网络在处理具有固有几何对称性的张拉整体结构时，因未利用对称性而面临计算复杂度高和优化不稳定的挑战。
method: 提出SymPINN框架，通过群论将节点分解为对称轨道并利用对称基构建约简坐标表示，同时结合硬约束初始条件和傅里叶特征编码。
result: 在对称T型杆和着陆器结构上的数值实验表明，该方法在预测精度和计算效率上均显著优于标准的物理信息模型。
conclusion: 将几何对称性显式嵌入神经网络架构中，是实现张拉整体结构高效、保结构动力学建模的有效途径。
---

## 摘要
张拉整体结构具有支配其动力学行为的内在几何对称性。然而，现有的多数用于张拉整体动力学的物理信息神经网络（PINN）方法并未显式利用这些对称性，导致计算复杂度高且优化不稳定。在这项工作中，我们提出了一种对称性约化物理信息神经网络（SymPINN）框架，该框架将基于群论的对称性直接嵌入到解的表达式和神经网络架构中，用于预测张拉整体动力学。通过将节点分解为对称轨道并使用对称基表示自由节点坐标，该方法构建了一种保留结构几何对称性的约化坐标表示。随后，通过对网络学习到的约化解进行对称变换来恢复完整坐标，从而确保预测的构型自动满足对称性约束。在该框架中，通过基于轨道的坐标生成、对称一致的消息传递以及物理残差约束来强制实现等变性。此外，SymPINN 通过将初始条件编码为硬约束、引入傅里叶特征编码以增强动态运动的表示，并采用两阶段优化策略，提高了训练效果。在对称 T 型杆和着陆器结构上进行的大量数值实验表明，与标准物理信息模型相比，该方法的预测精度和计算效率显著提高，表明了对称感知学习在张拉整体动力学保结构建模方面的巨大潜力。

## Abstract
Tensegrity structures possess intrinsic geometric symmetries that govern their dynamic behavior. However, most existing physics-informed neural network (PINN) approaches for tensegrity dynamics do not explicitly exploit these symmetries, leading to high computational complexity and unstable optimization. In this work, we propose a symmetry-reduced physics-informed neural network (SymPINN) framework that embeds group-theory-based symmetry directly into both the solution expression and the neural network architecture to predict tensegrity dynamics. By decomposing nodes into symmetry orbits and representing free nodal coordinates using a symmetry basis, the proposed method constructs a reduced coordinate representation that preserves geometric symmetry of the structure. The full coordinates are then recovered via symmetry transformations of the reduced solution learned by the network, ensuring that the predicted configurations automatically satisfy the symmetry constraints. In this framework, equivariance is enforced through orbit-based coordinate generation, symmetry-consistent message passing, and physics residual constraints. In addition, SymPINN improves training effectiveness by encoding initial conditions as hard constraints, incorporating Fourier feature encoding to enhance the representation of dynamic motions, and employing a two-stage optimization strategy. Extensive numerical experiments on symmetric T-bars and lander structures demonstrate significantly improved prediction accuracy and computational efficiency compared to standard physics-informed models, indicating the great potential of symmetry-aware learning for structure-preserving modeling of tensegrity dynamics.

---

## 论文详细总结（自动生成）

以下是对论文《Symmetry-Reduced Physics-Informed Learning of Tensegrity Dynamics》（张拉整体动力学的对称性约化物理信息学习）的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：张拉整体（Tensegrity）结构由受压杆件和受拉索网组成，具有轻质、高强和易部署等优点，广泛应用于航空航天和机器人领域。
*   **核心问题**：张拉整体动力学具有高度非线性、几何约束复杂和维度高的特点。传统的物理信息神经网络（PINN）在处理此类问题时面临三大挑战：
    1.  **计算负担重**：变量维度随节点数增加而激增。
    2.  **优化不稳定**：强耦合的非线性导致动力学行为“僵硬”（stiff）。
    3.  **忽略对称性**：大多数张拉整体结构具有内在的几何对称性（如循环对称或二面体对称），标准 PINN 无法利用这一先验知识，导致表示冗余。

### 2. 方法论：SymPINN 框架
*   **核心思想**：利用群论（Group Theory）识别结构的几何对称性，将高维构型空间投影到低维的对称不变子空间中，仅在约化空间内进行物理信息学习。
*   **关键技术细节**：
    1.  **对称性识别与基向量构建**：通过节点置换矩阵 $P$ 和空间旋转/反射矩阵 $R$ 定义对称算子 $S$。通过求解 $S$ 的零空间（nullspace）获得对称基矩阵 $U$。
    2.  **坐标约化**：将自由节点坐标 $n_a$ 表示为 $n_a(t) = U z(t)$，其中 $z(t)$ 是维度远低于原始空间的约化坐标。
    3.  **约化动力学方程**：将原始二阶微分方程投影到对称基上，推导出关于 $z(t)$ 的约化控制方程，从而降低物理残差计算的复杂度。
    4.  **硬约束与特征增强**：
        *   **硬约束初始条件**：构造解的形式为 $z(t) = z_0 + t \dot{z}_0 + \rho t^2 z_\theta(t)$，强制网络输出在 $t=0$ 时精确满足初始位移和速度。
        *   **傅里叶特征编码**：对时间输入进行正弦/余弦变换，以克服神经网络的“光谱偏差”，更好地捕捉高频振荡。
    5.  **两阶段优化**：先使用 Adam 优化器进行快速收敛，再切换至 L-BFGS 优化器进行精细微调。

### 3. 实验设计
*   **数据集/场景**：
    1.  **T-bar 结构**：包含 4 个自由节点，具有 $C_2$ 循环对称性，变量维度从 12 维约化至 6 维。
    2.  **Lander（着陆器）结构**：包含 12 个自由节点，具有 $C_2$ 对称性，变量维度从 36 维约化至 18 维。
*   **Benchmark（基准）**：标准的物理信息神经网络（Vanilla PINN）。
*   **对比指标**：均方误差（MSE）、相对误差（RE）以及计算耗时（Runtime）。
*   **采样策略**：测试了 0.1 到 0.5 不同的训练数据采样率。

### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**: Intel Core i9-9960X (3.10 GHz, 16 cores)。
    *   **RAM**: 64 GB。
    *   **GPU**: 双 NVIDIA Quadro RTX 5000。
*   **软件环境**：Python 3，数据生成使用 MATLAB 的 TsgFEM 软件包。
*   **训练时长**：SymPINN 在 T-bar 上约为 48-152 秒，在 Lander 上约为 113-515 秒；而标准 PINN 在 Lander 上需耗时 2400-3000 秒以上。

### 5. 实验数量与充分性
*   **实验规模**：针对两个代表性结构进行了多组采样率下的重复实验，并绘制了误差包络线（Min/Max/Mean）。
*   **充分性评价**：实验设计较为充分，涵盖了从简单到复杂的对称结构。通过对比不同采样率下的表现，验证了模型在小样本数据下的鲁棒性。对比实验在相同的网络架构和优化设置下进行，保证了公平性。

### 6. 主要结论与发现
*   **精度提升**：SymPINN 的预测误差（RE）通常比标准 PINN 低 1-2 个数量级，尤其在捕获长期周期性运动方面表现更优。
*   **效率飞跃**：由于变量维度减半且自动微分（autograd）计算量减少，SymPINN 的训练速度比标准 PINN 快 5-20 倍。
*   **稳定性**：通过硬约束嵌入初始条件和对称性限制，SymPINN 有效消除了训练过程中可能出现的“对称性破缺”数值模态。

### 7. 优点
*   **理论严谨**：将群论与深度学习有机结合，为具有几何特征的结构动力学提供了保结构（Structure-preserving）的建模方案。
*   **计算高效**：通过维度约化显著降低了 PINN 处理复杂系统时的计算门槛。
*   **泛化性强**：硬约束和傅里叶特征的引入解决了标准 PINN 在处理二阶振荡系统时的常见顽疾。

### 8. 不足与局限
*   **对称性依赖**：该方法高度依赖于预先识别的几何对称性，若结构在运动过程中发生剧烈大变形导致对称性丧失（如屈曲或局部损坏），模型效力可能下降。
*   **对称群范围**：目前主要验证了 $C_n$ 和 $D_n$ 对称群，对于更复杂的空间对称群或非完美对称结构的适用性尚待研究。
*   **规模限制**：虽然比标准 PINN 强，但实验中的节点数（最高 12 节点）仍属于中小型规模，对于成百上千节点的超大型张拉整体阵列，其扩展性有待验证。

（完）
