---
title: A Family of Adaptive Activation Functions for Mitigating Failure Modes in Physics-Informed Neural Networks
title_zh: 一种用于缓解物理信息神经网络失效模式的自适应激活函数族
authors: Krishna Murari
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18328v1"
tags: ["query:pinn"]
score: 10.0
evidence: 自适应激活函数以减轻PINN的失效模式
tldr: 针对物理信息神经网络（PINNs）在处理复杂偏微分方程时易出现的训练不稳定和失效模式，本文提出了一系列基于小波变换的自适应激活函数。通过将可训练的小波函数与传统的双曲正切或Softplus函数结合，增强了模型的表达能力和训练稳定性。在多种偏微分方程上的实验表明，该方法在鲁棒性和精度上均优于传统PINNs及Transformer架构模型。
selection_source: fresh_fetch
motivation: 旨在解决标准PINNs在训练过程中常见的失效模式，并利用小波函数强大的函数逼近能力提升模型性能。
method: 开发了五种将可训练小波函数与固定或可训练的tanh及softplus函数相结合的自适应激活函数。
result: 在四类代表性偏微分方程的评估中，该方法在准确性和鲁棒性方面显著优于传统激活函数及PINNsFormer等先进架构。
conclusion: 引入自适应小波激活函数是提升PINNs处理科学与工程问题效能的一种通用且有效的策略。
---

## 摘要
物理信息神经网络（PINNs）是一种强大且灵活的学习框架，近年来受到了广泛关注。它在广泛的科学和工程问题中展现出了强大的性能。与此同时，小波因其强大的逼近能力而被广泛用作高效的计算工具。受标准 PINNs 中观察到的常见失效模式的启发，本研究引入了一系列新型的基于小波的自适应激活函数。所提出的激活函数通过将可训练的小波函数与可训练或固定的双曲正切及 softplus 函数相结合，显著提高了训练稳定性和表达能力。在 PINN 框架内开发了五种不同的激活函数，并在四类具有代表性的偏微分方程（PDEs）中进行了系统评估。使用柱状图进行的全面对比表明，与传统激活函数相比，该方法在鲁棒性和准确性方面均有所提高。此外，通过与基准 PINNs、基于 Transformer 的架构（如 PINNsFormer）以及其他深度学习模型的直接对比，验证了所提方法的有效性和通用性。

## Abstract
Physics-Informed Neural Networks(PINNs) are a powerful and flexible learning framework that has gained significant attention in recent years. It has demonstrated strong performance across a wide range of scientific and engineering problems. In parallel, wavelets have been extensively used as efficient computational tools due to their strong approximation capabilities. Motivated by the common failure modes observed in standard PINNs, this work introduces a novel family of adaptive wavelet-based activation functions. The proposed activation functions significantly improve training stability and expressive power by combining trainable wavelet functions with either trainable or fixed hyperbolic tangent and softplus functions. Five distinct activation functions are developed within the PINN framework and systematically evaluated across four representative classes of partial differential equations (PDEs). Comprehensive comparisons using bar plots demonstrate improved robustness and accuracy compared to traditional activation functions. Furthermore, the proposed approach is validated through direct comparisons with baseline PINNs, transformer-based architectures such as PINNsFormer, and other deep learning models, highlighting its effectiveness and generality.

---

## 论文详细总结（自动生成）

### 论文总结：一种用于缓解物理信息神经网络失效模式的自适应激活函数族

#### 1. 论文的核心问题与整体含义（研究动机和背景）
物理信息神经网络（PINNs）在解决科学与工程中的偏微分方程（PDEs）方面展现了巨大潜力，但在处理具有**振荡模式、高频特征和多尺度特性**的复杂问题时，标准 PINNs（通常使用 `tanh` 或 `ReLU` 激活函数）经常会出现训练不稳定或完全失效的情况。研究动机在于利用**小波函数（Wavelets）**强大的局部化逼近能力，结合自适应学习机制，设计一套新型激活函数，以增强神经网络的表达能力，解决梯度流不均和时间演化信息缺失导致的预测偏差。

#### 2. 论文提出的方法论
论文提出了一系列基于小波变换的自适应激活函数，核心思想是将**可训练的小波函数**与**双曲正切（tanh）**及 **Softplus** 函数相结合。
*   **核心公式结构**：激活函数通常表示为 $\psi(x) = \text{tanh}(\beta x) \cdot \text{Wavelet}(x, \alpha, \dots)$。
*   **关键技术细节**：
    *   **参数自适应**：小波函数中的缩放参数（$\alpha$）、平移参数以及 `tanh` 的增益参数（$\beta$）均设为可训练变量。
    *   **正性约束**：使用 `Softplus` 函数处理这些参数，确保它们在训练过程中保持严格正值，从而维持激活函数的数学特性。
    *   **函数族成员**：开发了五种主要函数：**SoftMexTanh**（墨西哥帽）、**SoftMorTanh**（Morlet）、**SoftGaussTanh**（高斯）、**SoftGaborTanh**（Gabor）和 **SoftHermTanh**（埃尔米特）。
    *   **变体设计**：区分了全可训练版本和固定 `tanh` 参数的版本（后缀为 W），以应对不同 PDE 的稳定性需求。

#### 3. 实验设计
*   **实验场景（PDEs）**：
    1.  **1D 反应方程**（Reaction）：测试非线性双曲型问题的处理能力。
    2.  **1D 波方程**（Wave）：测试对波动和振荡特征的捕捉。
    3.  **1D 对流方程**（Convection）：测试在高对流系数（$\beta'=50$）下的稳定性。
    4.  **2D 纳维-斯托克斯方程**（Navier-Stokes）：测试复杂流体动力学系统的泛化能力。
*   **Benchmark 与对比方法**：
    *   **基准**：标准 PINN（使用 `tanh` 激活函数）。
    *   **对比模型**：PINNsFormer（基于 Transformer）、QRes（二次残差网络）、FLS（首层正弦方法）、PINN-Mamba 及 ML-PINN。
*   **评估指标**：相对平均绝对误差（rMAE）、相对均方根误差（rRMSE）以及训练损失（Loss）。

#### 4. 资源与算力
*   **硬件**：单台 **NVIDIA A100 GPU**，配置 32 GB 显存。
*   **软件框架**：PyTorch。
*   **优化器设置**：使用 L-BFGS 优化器（带 Strong Wolfe 线搜索），固定训练迭代次数为 1000 次。
*   **计算资源支持**：由印度国家超级计算任务（NSM）的“PARAM RUDRA”超级计算机提供支持。

#### 5. 实验数量与充分性
*   **实验规模**：针对 4 类不同的 PDE 进行了详尽测试，每类方程都对比了 5 种以上的新型激活函数及其变体。
*   **充分性**：实验涵盖了从简单标量方程到复杂矢量系统（NS 方程）的跨度。通过消融实验（如固定 $\beta$ 参数的 W 变体）验证了不同组件的贡献。
*   **客观性**：使用了统一的随机种子（Seed 5）以保证可复现性，并采用了学术界通用的公开基准数据集进行对比，实验设计较为客观公平。

#### 6. 论文的主要结论与发现
*   **性能提升**：所提出的自适应小波激活函数在所有测试案例中均显著优于标准 `tanh` 激活函数。
*   **缓解失效**：在标准 PINN 容易失效的对流方程和高频波方程中，新函数（尤其是 **SoftGaborTanh**）表现出了极强的鲁棒性。
*   **效率优势**：相比于 PINNsFormer 等复杂架构，该方法在保持高精度的同时，训练速度更快（如在反应方程中，其迭代速度约为 PINNsFormer 的 1.4 倍），且内存占用更低。
*   **参数敏感性**：发现移除 `tanh` 组件的可训练参数（即使用 W 变体）在某些复杂问题（如 NS 方程）中能获得更优的相对误差。

#### 7. 优点
*   **数学启发性强**：将小波分析的局部化特性与深度学习的自适应性完美结合。
*   **即插即用**：无需改变 PINN 的整体架构，仅通过更换激活函数即可获得显著的性能提升。
*   **计算高效**：在提升精度的同时，避免了 Transformer 架构带来的巨大计算开销。
*   **鲁棒性**：对初始化不敏感，能有效处理 PINN 常见的梯度消失或不平衡问题。

#### 8. 不足与局限
*   **超参数依赖**：虽然参数是自适应的，但初始值的设定（如 Gabor 函数的初始频率）仍可能影响最终收敛效果。
*   **实验维度限制**：目前实验主要集中在 1D 和 2D 问题，尚未在更高维（如 3D 或更高）或极度不规则几何域上进行验证。
*   **优化策略单一**：主要依赖 L-BFGS 优化器，对于超大规模数据集，可能需要探索与 Adam 等优化器的混合使用策略。

（完）
