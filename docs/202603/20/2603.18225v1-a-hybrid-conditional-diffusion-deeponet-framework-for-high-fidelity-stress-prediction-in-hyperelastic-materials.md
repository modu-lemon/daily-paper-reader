---
title: A Hybrid Conditional Diffusion-DeepONet Framework for High-Fidelity Stress Prediction in Hyperelastic Materials
title_zh: 一种用于超弹性材料高保真应力预测的混合条件扩散-DeepONet 框架
authors: "Purna Vindhya Kota, Meer Mehran Rashid, Somdatta Goswami, Lori Graham-Brady"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18225v1"
tags: ["query:pinn"]
score: 9.0
evidence: 混合扩散-DeepONet用于材料应力预测
tldr: 针对超弹性材料复杂微结构下的应力预测难题，本文提出一种结合条件扩散模型（cDDPM）与深度算子网络（DeepONet）的混合框架 cDDPM-DeepONet。该框架通过解耦应力形态与幅值，利用 cDDPM 生成归一化应力场，并由 DeepONet 预测全局缩放参数，有效解决了传统模型在捕捉局部应力集中和全局物理尺度方面的局限性，显著提升了预测精度。
selection_source: fresh_fetch
motivation: 传统深度学习模型在处理超弹性材料时，难以同时兼顾高频应力集中捕捉和全局物理尺度的准确还原。
method: 提出一种混合框架，利用条件扩散模型生成归一化应力形态，并结合 DeepONet 预测极值参数以实现物理尺度的精确重构。
result: 在非线性超弹性数据集上的实验表明，该模型在预测精度上比 UNet 和 DeepONet 等基准模型提高了一到两个数量级。
conclusion: 该混合框架成功克服了谱偏差和尺度漂移问题，能够高保真地还原复杂微结构中的局部应力集中和全局力学行为。
---

## 摘要
预测具有复杂微结构的超弹性材料中的应力场对于传统的深度学习代理模型来说仍然具有挑战性，这些模型难以同时捕捉尖锐的应力集中和应力幅值的宽动态范围。诸如 UNet 之类的卷积架构往往会过度平滑高频梯度，而诸如 DeepONet 之类的神经算子则表现出谱偏差，且对局部极值的预测不足。扩散模型可以恢复细微尺度结构，但通常会引入低频幅度漂移，从而降低物理缩放的准确性。为了解决这些局限性，我们提出了一种混合代理框架 cDDPM-DeepONet，该框架将应力形态与幅值解耦。基于 UNet 骨干网络的条件去噪扩散概率模型（cDDPM）根据几何形状和载荷生成归一化的 von Mises 应力场。与此同时，一个改进的 DeepONet 预测全局缩放参数（最小和最大应力），从而能够重建全分辨率的物理应力图。这种分离使得扩散模型能够专注于空间结构，而算子网络则修正全局幅度，从而减轻了谱偏差和缩放偏差。我们在具有单个和多个多边形孔隙的非线性超弹性数据集上评估了该框架。所提出的模型在性能上始终优于 UNet、DeepONet 和独立的 cDDPM 基准模型，提升达一到两个数量级。频谱分析显示，该模型在所有波数上都与有限元解高度一致，同时保留了全局行为和局部应力集中。

## Abstract
Predicting stress fields in hyperelastic materials with complex microstructures remains challenging for traditional deep learning surrogates, which struggle to capture both sharp stress concentrations and the wide dynamic range of stress magnitudes. Convolutional architectures such as UNet tend to oversmooth high-frequency gradients, while neural operators like DeepONet exhibit spectral bias and underpredict localized extremes. Diffusion models can recover fine-scale structure but often introduce low-frequency amplitude drift, degrading physical scaling. To address these limitations, we propose a hybrid surrogate framework, cDDPM-DeepONet, that decouples stress morphology from magnitude. A conditional denoising diffusion probabilistic model (cDDPM), built on a UNet backbone, generates normalized von Mises stress fields conditioned on geometry and loading. In parallel, a modified DeepONet predicts global scaling parameters (minimum and maximum stress), enabling reconstruction of full-resolution physical stress maps. This separation allows the diffusion model to focus on spatial structure while the operator network corrects global amplitude, mitigating spectral and scaling biases. We evaluate the framework on nonlinear hyperelastic datasets with single and multiple polygonal voids. The proposed model consistently outperforms UNet, DeepONet, and standalone cDDPM baselines by one to two orders of magnitude. Spectral analysis shows strong agreement with finite element solutions across all wavenumbers, preserving both global behavior and localized stress concentrations.

---

## 论文详细总结（自动生成）

以下是对论文《A Hybrid Conditional Diffusion-DeepONet Framework for High-Fidelity Stress Prediction in Hyperelastic Materials》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在计算力学中，准确预测具有复杂微结构的超弹性材料的应力场至关重要，但传统深度学习代理模型存在局限性：
    *   **UNet（卷积架构）**：倾向于过度平滑高频梯度，难以捕捉孔隙边缘的尖锐应力集中。
    *   **DeepONet（神经算子）**：存在“谱偏差（Spectral Bias）”，往往低估局部极值。
    *   **扩散模型（DDPM）**：虽能恢复精细结构，但常出现低频幅值漂移，导致物理尺度（绝对数值）不准确。
*   **核心问题**：如何构建一个既能捕捉高频局部应力集中，又能保持全局物理尺度准确性的高保真代理模型？

### 2. 论文提出的方法论
*   **核心思想：解耦策略**。将应力场的“空间形态（Morphology）”与“物理幅值（Magnitude）”分开处理。
*   **关键技术细节**：
    *   **条件扩散模型 (cDDPM)**：采用基于 UNet 的骨干网络，输入几何形状和载荷条件的嵌入向量（Embedding），生成**归一化**的 von Mises 应力场（数值范围在 [0, 1]）。它专注于学习复杂的空间频率分布。
    *   **深度算子网络 (DeepONet)**：包含分支网络（Branch Net，处理几何和载荷函数）和主干网络（Trunk Net，处理坐标）。在此框架中，DeepONet 被简化为预测每个样本的**全局最小值和最大值**。
    *   **混合重构**：将 cDDPM 生成的归一化场通过 DeepONet 预测的极值进行线性缩放，还原为具有物理意义的应力图。
*   **算法流程**：
    1.  前向过程：对训练数据进行加噪。
    2.  反向过程：cDDPM 在几何/载荷条件下迭代去噪，生成归一化应力图。
    3.  并行预测：DeepONet 预测该工况下的应力极值。
    4.  后处理：通过公式 $\sigma_{vM} = \sigma_{norm} \times (max - min) + min$ 完成重构。

### 3. 实验设计
*   **数据集/场景**：
    *   **单孔隙数据集**：包含 19,900 个训练样本和 3,317 个测试样本，具有随机形状、位置和取向的多边形孔隙。
    *   **多孔隙数据集**：包含 20,000 个训练样本和 3,000 个测试样本，模拟更复杂的微结构交互。
    *   **物理模型**：使用 Neo-Hookean 超弹性本构模型，通过 FEniCS 有限元软件生成地面真值（Ground Truth）。
*   **基准模型 (Benchmark)**：
    *   标准 **UNet**。
    *   独立运行的 **cDDPM**（直接预测物理数值，不进行解耦）。
*   **评估指标**：MAE、RMSE、相对 MAE、峰值绝对误差 (PAE)、局部应力梯度 (LSG) 以及频谱分析（1D/2D 傅里叶变换）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了扩散步数 $T=100$ 是性能与效率的平衡点，并给出了训练样本量（约 2 万个），这暗示了该模型需要中等规模的计算资源进行训练。

### 5. 实验数量与充分性
*   **实验规模**：针对单孔隙和多孔隙两个复杂度不同的数据集进行了全面测试。
*   **消融与敏感性分析**：
    *   研究了扩散步数 $T$ 对预测精度的影响。
    *   进行了数据效率分析（使用 25% 到 100% 的数据量进行训练），证明了模型在 50%-66% 数据量时即可达到性能饱和。
*   **客观性**：通过频谱分析（Spectral Analysis）从频域角度验证了模型对高频细节的捕捉能力，这比单纯的像素级误差指标更具说服力。

### 6. 论文的主要结论与发现
*   **精度飞跃**：cDDPM-DeepONet 混合模型在所有指标上均优于基准模型，误差降低了 **1 到 2 个数量级**。
*   **克服偏差**：成功解决了扩散模型的尺度漂移问题和神经算子的谱偏差问题。
*   **频谱一致性**：频谱分析显示，该模型在全波数范围内与有限元参考解高度吻合，能够完美还原孔隙边缘的应力梯度。
*   **泛化能力**：模型在未见过的几何形状和载荷条件下表现出极强的鲁棒性。

### 7. 优点（亮点）
*   **架构创新**：巧妙地利用 DeepONet 预测标量极值来辅助扩散模型，这种“形态与幅值解耦”的思想非常适合处理具有宽动态范围的物理场。
*   **物理保真度高**：不仅关注像素误差，还通过 LSG 指标和频谱分析证明了其在力学特征捕捉上的优越性。
*   **数据效率**：实验表明模型不需要极端庞大的数据集即可收敛，具有较好的实用性。

### 8. 不足与局限
*   **维度限制**：目前仅在 2D 平面应力问题上进行了验证，尚未扩展到更复杂的 3D 空间。
*   **推理成本**：扩散模型需要多次迭代去噪，虽然论文提到 $T=100$ 步即可，但相比于 UNet 的单次前向推理，其计算开销仍然较大。
*   **硬件信息缺失**：缺乏具体的算力消耗报告，不便于其他研究者评估部署成本。
*   **本构单一性**：主要针对 Neo-Hookean 模型，对于具有路径依赖性（如弹塑性）或更复杂各向异性的材料，其表现尚待验证。

（完）
