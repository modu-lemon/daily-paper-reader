---
title: Incorporating Continuous Dependence Qualifies Physics-Informed Neural Networks for Operator Learning
title_zh: 引入连续依赖性使物理信息神经网络适用于算子学习
authors: "Guojie Li, Wuyue Yang, Liu Hong"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25122v1"
tags: ["query:pinn"]
score: 9.0
evidence: 通过连续依赖性将PINN扩展到算子学习
tldr: 针对物理信息神经网络（PINN）在算子学习中泛化能力差的问题，本文受偏微分方程（PDE）适定性理论启发，提出了一种融合解对参数及初边值连续依赖性的改进模型cd-PINN。该方法通过引入连续依赖性信息，显著提升了模型在有限标注数据下的泛化性能，在测试误差上比DeepONet和FNO降低了1-3个数量级，为PINN应用于算子学习提供了新途径。
selection_source: fresh_fetch
motivation: 传统PINN在实际应用中受限于较差的泛化性能，难以有效处理算子学习任务。
method: 提出cd-PINN，通过在模型中引入PDE解对参数、初始值和边界值的连续依赖性信息来增强约束。
result: 在有限标注数据下，cd-PINN的测试均方误差比DeepONet和FNO等主流算子学习模型低1到3个数量级。
conclusion: 结合PDE解的连续依赖性是提升PINN泛化能力并使其胜任算子学习任务的一种简单且有效的方法。
---

## 摘要
物理信息神经网络 (PINNs) 已被证明是求解各种偏微分方程 (PDEs) 的一种极具前景的方法，特别是高维方程和具有不规则边界的方程。然而，它们在实际应用中的能力受到其较差泛化性能的严重限制。受偏微分方程适定性严谨数学表述的启发，我们通过引入关于 PDE 解对参数和初/边值连续依赖性的额外信息，开发了 PINNs 的一种新型扩展（简称为 cd-PINN）。大量的数值实验表明，在有限的标注数据下，cd-PINN 的测试均方误差 (MSE) 比 DeepONet 和 FNO 低 1 到 3 个数量级。因此，引入 PDE 解的连续依赖性为使 PINNs 适用于算子学习提供了一种简单的方法。

## Abstract
Physics-informed neural networks (PINNs) have been proven as a promising way for solving various partial differential equations, especially high-dimensional ones and those with irregular boundaries. However, their capabilities in real applications are highly restricted by their poor generalization performance. Inspired by the rigorous mathematical statements on the well-posedness of PDEs, we develop a novel extension of PINNs by incorporating the additional information on the continuous dependence of PDE solutions with respect to parameters and initial/boundary values (abbreviated as cd-PINN). Extensive numerical experiments demonstrate that, with limited labeled data, cd-PINN achieves 1-3 orders of magnitude lower in test MSE than DeepONet and FNO. Therefore, incorporating the continuous dependence of PDE solutions provides a simple way for qualifying PINNs for operator learning.

---

## 论文详细总结（自动生成）

以下是对论文《Incorporating Continuous Dependence Qualifies Physics-Informed Neural Networks for Operator Learning》的深度结构化总结：

### 1. 核心问题与整体含义
该论文针对**物理信息神经网络（PINN）在算子学习（Operator Learning）任务中泛化性能差**的核心问题展开研究。
*   **研究背景**：传统的PINN通常只能解决单一配置下的PDE，改变参数或初边值后需重新训练；而现有的算子学习方法（如DeepONet、FNO）虽然能处理多种配置，但往往依赖海量的标注数据。
*   **研究动机**：受偏微分方程（PDE）数学理论中“适定性”（Well-posedness）的启发，特别是解对参数和初值的**连续依赖性**，作者试图将这一数学先验引入神经网络，使PINN能够以极高的数据效率学习解算子。

### 2. 方法论
论文提出了 **cd-PINN（Continuous Dependence PINN）**，其核心思想是将解对配置参数的敏感性信息直接嵌入损失函数。
*   **核心思想**：假设PDE的解 $u$ 不仅是时空坐标 $(t, x)$ 的函数，也是编码参数 $c$（如扩散系数、初值函数的系数）的连续函数。通过强制模型满足解对 $c$ 的导数约束，提升模型的泛化能力。
*   **关键技术细节**：
    *   **参数编码**：将方程的物理参数或初边值的特征编码为向量 $c$，作为神经网络的额外输入。
    *   **损失函数设计**：在标准PINN的残差损失（$L_{res}$）和数据损失（$L_{data}$）基础上，引入了**连续依赖损失项（$L_{cd}$）**。
    *   **公式逻辑**：$L_{cd}$ 包含了对编码 $c$ 的偏导数约束，例如 $\frac{\partial^2 \hat{u}}{\partial c \partial t} - \frac{\partial P}{\partial \hat{u}}\frac{\partial \hat{u}}{\partial c} - \frac{\partial P}{\partial c} = 0$。这种高阶导数约束强制网络学习解空间在参数维度上的平滑演化规律。
*   **流程**：模型在离线阶段针对一定范围的参数进行训练，在线推理阶段对于未见过的参数配置，无需任何微调或重新训练即可直接输出高精度解。

### 3. 实验设计
作者设计了极其丰富的数值实验来验证 cd-PINN 的性能：
*   **实验场景**：
    1.  **基础PDE**：1D扩散方程、2D波动方程、2D泊松方程。
    2.  **高维与非线性**：2D/5D/8D扩散反应方程、1D Burgers方程（含冲击波）、2D Navier-Stokes方程。
    3.  **实际应用**：阿尔茨海默症中的 Tau 蛋白聚集动力学（基于真实 ADNI 临床数据）。
*   **Benchmark（基准）**：DeepONet、FNO、PI-DeepONet、传统PINN、以及有限差分法（FDM）。
*   **对比指标**：测试均方误差（MSE）、负对数平均绝对误差（NLMAE）、相对均方根误差（NRMSE）以及计算耗时。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：提到了部分实验的训练轮数（如 20,000 轮 Adam + L-BFGS 优化）。在与 FDM 的对比中，作者指出当需要求解的参数配置超过 30 个时，cd-PINN 的总耗时（训练+推理）将低于传统数值方法。

### 5. 实验数量与充分性
*   **实验数量**：论文涵盖了从简单线性方程到复杂非线性方程、从低维到高维（达8维）、从合成数据到真实临床数据的全方位测试，实验组数超过 10 组。
*   **充分性与公平性**：
    *   **消融实验**：对比了有无 $L_{cd}$ 项、有无显式编码（cd-PINN #）的效果。
    *   **极端测试**：专门测试了在不满足连续依赖性理论保证（如负扩散系数的病态问题）下模型的表现。
    *   **公平性**：在相同且极少量的标注数据（如仅 20 个点）下对比 DeepONet 和 FNO，证明了 cd-PINN 的数据效率优势。整体实验设计非常严谨且具有说服力。

### 6. 主要结论与发现
*   **精度飞跃**：在有限标注数据下，cd-PINN 的测试 MSE 比 DeepONet 和 FNO 低 **1 到 3 个数量级**。
*   **数据效率**：cd-PINN 仅需极少量的标注样本即可实现对整个参数空间的精准覆盖。
*   **泛化能力**：引入 $L_{cd}$ 项能显著改善模型在参数空间边缘或复杂区域（如小 $\sigma$ 值的扩散）的预测精度。
*   **逆问题优势**：在参数估计任务中，cd-PINN 提供的损失函数景观（Loss Landscape）更平滑，更容易通过梯度下降找到全局最优解。

### 7. 优点
*   **理论驱动**：将深厚的数学分析（PDE 适定性）与深度学习结合，而非纯粹的黑盒调优。
*   **零样本推理**：训练完成后，对新参数的推理是瞬时的，无需像元学习（Meta-learning）方法那样进行二阶梯度微调。
*   **高维鲁棒性**：在 8 维扩散反应方程中依然保持了较好的精度提升。
*   **实用性强**：在阿尔茨海默症临床数据上的成功应用展示了其处理复杂生物医学问题的潜力。

### 8. 不足与局限
*   **计算开销**：$L_{cd}$ 项涉及对参数的高阶混合偏导数，这会显著增加训练阶段自动微分（Auto-diff）的内存占用和计算时间。
*   **维数灾难的残余**：虽然比传统方法好，但在极高维度下，残差点的采样密度依然是制约精度的瓶颈。
*   **理论边界**：对于本质上不具备连续依赖性的物理过程（如某些极端的湍流或奇异性问题），该方法的约束可能会失效或引入偏差。
*   **编码依赖**：模型性能依赖于对输入配置（如初值函数）的有效编码，若编码方式不当，可能无法充分发挥连续依赖性的优势。

（完）
