---
title: Physics-Informed Neural Operator for Electromagnetic Inverse Scattering Problems
title_zh: 物理信息神经算子在电磁逆散射问题中的应用
authors: "Q. C. Dong, Zi-Xuan Su, Qing Huo Liu, Wen Chen, Zhizhang, Chen"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25404v1"
tags: ["query:pinn"]
score: 10.0
evidence: 使用傅里叶神经算子的物理信息神经算子 (PINO)
tldr: 本研究提出了一种物理信息神经算子（PINO）框架，用于解决电磁逆散射问题。该框架将介电特性表示为可学习张量，利用神经算子预测感应电流，并通过结合状态损失、数据损失和全变分正则化的混合损失函数进行联合优化。PINO在多频率和无相位反演等复杂场景下表现出极高的准确性和鲁棒性，优于传统的对比源反演（CSI）方法，为电磁逆散射提供了一种高效、统一的解决方案。
selection_source: fresh_fetch
motivation: 旨在克服传统逆散射方法在处理多频率和无相位等复杂测量条件时建模复杂且计算效率低下的局限性。
method: 提出一种基于PINO的框架，利用混合损失函数联合优化网络参数与介电特性张量，并集成了多种先进的神经算子架构。
result: 实验证明PINO在含噪声的单频、多频及无相位测量场景下均实现了高精度重建，性能显著优于传统CSI方法。
conclusion: 该框架为电磁逆散射问题提供了一个通用、高效且稳健的统一解决方案，具有极强的实际应用潜力。
---

## 摘要
本文提出了一种物理信息神经算子（PINO）框架用于解决逆散射问题，能够在多种测量条件下实现快速且准确的重建。在该方法中，介电特性被表示为一个可学习的张量，而神经算子则用于预测感应电流分布。通过构建由状态损失、数据损失和全变分（TV）正则化组成的混合损失函数，建立了一个完全可微的公式，以实现网络参数和介电特性的联合优化。为了展示该框架的通用性和灵活性，本文利用三种代表性的神经算子实现了 PINO，即傅里叶神经算子（FNO）、增强型傅里叶神经算子（U-FNO）和分解傅里叶神经算子（F-FNO）。与传统方法相比，所提框架具有更简洁的公式和通用的建模能力，使其能够轻松应用于包括多频和无相位反演在内的各种测量场景。数值模拟表明，在存在噪声的情况下，所提 PINO 在单频和多频设置下，对包含或不包含相位信息的样本均能实现高精度且鲁棒的重建。结果证明，PINO 的性能始终优于传统的对比源反演（CSI）方法，为复杂的电磁逆散射问题提供了一种高效、统一的解决方案。

## Abstract
This paper proposes a physics-informed neural operator (PINO) framework for solving inverse scattering problems, enabling rapid and accurate reconstructions under diverse measurement conditions. In the proposed approach, the dielectric property is represented as a learnable tensor, while a neural operator is employed to predict the induced current distribution. A hybrid loss function, consisting of the state loss, data loss and total-variation (TV) regularization, is constructed to establish a fully differentiable formulation for a joint optimization of network parameters and dielectric property. To demonstrate the framework's generality and flexibility, PINO is implemented using three representative neural operators, i.e., the Fourier Neural Operator (FNO), the enhanced Fourier Neural Operator (U-FNO) and the Factorized Fourier Neural Operator (F-FNO). Compared with conventional approaches, the proposed framework offers a simpler formulation and universal modeling capability, making it readily applicable to various measurement scenarios, including multi-frequency and phaseless inversion. Numerical simulations demonstrate that the proposed PINO achieves high accuracy and robust reconstruction across samples with and without phase information, under single-frequency and multi-frequency settings in the presence of noise. The results demonstrate that PINO consistently outperforms conventional contrast-source inversion (CSI) methods and provides an efficient, unified solution to complex electromagnetic inverse-scattering problems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PINO（Physics-Informed Neural Operator，物理信息神经算子）** 的框架，旨在解决复杂的电磁逆散射问题（Inverse Scattering Problems, ISP）。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：电磁逆散射旨在从测量的散射场数据中重建目标的介电特性（如相对介电常数）。这是一个典型的**非线性、严重病态（ill-posed）**问题。
*   **研究动机**：
    *   **传统方法（如CSI）**：计算成本高、耗时长，且在噪声环境下稳定性较差。
    *   **纯数据驱动方法**：虽然推理快，但泛化能力差，高度依赖训练数据的分布，且往往缺乏物理一致性。
    *   **研究目标**：开发一个既能利用物理规律（麦克斯韦方程组的积分形式），又能发挥神经算子强大拟合能力的统一框架，以应对多频率、无相位（phaseless）及高噪声等复杂测量场景。

### 2. 方法论
*   **核心思想**：将逆散射问题转化为一个联合优化问题。其中，**介电对比度（Contrast Function）**被建模为一个可学习的张量（类似于NeRF中的表示），而**感应电流（Induced Current）**则由神经算子预测。
*   **关键技术细节**：
    *   **输入编码**：对空间坐标 $(X, Y)$ 进行频率编码（Frequency Encoding），以增强网络对高频细节的表征能力。
    *   **神经算子架构**：采用了三种变体：**FNO**（傅里叶神经算子）、**U-FNO**（结合U-Net增强）和**F-FNO**（因子化张量以减少参数）。
    *   **混合损失函数**：
        1.  **状态损失（State Loss）**：基于Lippmann-Schwinger积分方程，确保预测的电流与介电特性符合物理规律。
        2.  **数据损失（Data Loss）**：确保预测的散射场与实际测量数据一致。
        3.  **正则化项**：引入全变分（TV）正则化以保持重建图像的边缘平滑。
    *   **无相位反演**：通过引入 `Softplus` 激活函数确保介电常数为正，并修改数据损失函数为仅对比场强幅值，实现了无需相位信息的重建。

### 3. 实验设计
*   **实验场景**：
    *   **目标形状**：Austria（经典测试形状）、圆柱体组合、MNIST手写数字。
    *   **测量配置**：16个线源，32个接收器，分布在半径2米的圆周上。
*   **Benchmark与对比方法**：
    *   **传统方法**：CSI（对比源反演）、CSI+TV、BP（反向传播算法）。
    *   **深度学习方法**：BPS（基于U-Net的精细化方法）、Physics-Net、CS-Net。
*   **测试变量**：单频（3 GHz）与多频（3/4/5 GHz）；含相位与无相位数据；不同噪声水平（5% 到 50%）。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **18核 AMD EPYC 9754 处理器** 和 **单张 NVIDIA RTX 4090D GPU** 的云服务器。
*   **训练/优化细节**：使用 Adam 优化器，运行 2000 个 epoch。
*   **计算效率**：文中给出了每轮迭代的时间，FNO 约为 **11-13 ms/iteration**，U-FNO 约为 **17-22 ms/iteration**。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从简单几何体到复杂数字形状的多种目标，并针对 10%、30%、50% 的极高噪声水平进行了鲁棒性测试。
*   **消融与对比**：对比了三种不同的神经算子骨干网络（FNO, U-FNO, F-FNO），验证了框架的**算子无关性（Backbone-agnostic）**。
*   **充分性评价**：实验设计较为全面，不仅对比了重建精度（SSIM, RMSE），还对比了计算参数量和收敛曲线，能够客观反映 PINO 在极端噪声和信息缺失（无相位）下的优势。

### 6. 主要结论与发现
*   **性能优越**：PINO 在所有测试场景下的 SSIM（结构相似性）和 RMSE（均方根误差）均优于传统 CSI 和现有的深度学习基准方法。
*   **高鲁棒性**：在 50% 的超高噪声水平下，PINO 仍能保持稳定的重建质量，而传统方法往往失效。
*   **多频优势**：多频率数据的引入显著提升了重建精度，尤其是在无相位反演中，频率多样性有效缓解了问题的病态性。
*   **灵活性**：该框架无需针对无相位或多频场景重新推导公式，仅需修改损失函数即可适配。

### 7. 优点
*   **物理一致性**：通过状态损失将物理方程嵌入优化过程，避免了纯黑盒模型的不确定性。
*   **统一框架**：一个架构同时解决单频/多频、有相位/无相位问题，具有极强的通用性。
*   **分辨率无关性**：受益于神经算子的特性，该方法在不同网格分辨率下具有良好的表现。
*   **无需预训练**：该方法可以从零开始（from scratch）针对特定测量数据进行优化，不依赖大规模预训练数据集。

### 8. 不足与局限
*   **迭代耗时**：虽然单次迭代快，但作为一种基于优化的方法，它仍需要数百到数千次迭代才能收敛，实时性不如端到端的推理网络。
*   **超参数敏感性**：混合损失函数中各项（状态、数据、TV）的权重系数 $\lambda$ 需要手动调优，这在不同场景下可能影响结果。
*   **计算复杂度**：对于大规模三维问题，存储和计算格林函数矩阵（$G_D, G_S$）可能会面临内存瓶颈。

（完）
