---
title: A deep backward regression-based scheme for high-dimensional nonlinear partial differential equations
title_zh: 一种求解高维非线性偏微分方程的深度后向回归方案
authors: "Qiang Han, Shaolin Ji, Yunzhang Li"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14721v1"
tags: ["query:pinn"]
score: 9.0
evidence: 高维非线性偏微分方程的深度学习方案
tldr: 本文提出了一种基于深度后向回归（DBR）的方案，用于求解高维非线性抛物型偏微分方程。该方法通过将随机优化问题重构为确定性函数逼近任务，并引入条件期望表示，有效降低了数值方差并增强了训练稳定性。实验证明，DBR在处理高维（最高200维）及复杂无界解问题上显著优于现有DBDP1方法，且具有严谨的理论收敛性保证。
selection_source: fresh_fetch
motivation: 旨在解决高维非线性PDE求解过程中数值方差大、训练不稳定以及在处理无界解时泛化能力不足的挑战。
method: 通过将后向随机差分方程重构为条件期望形式，将随机优化转化为具有预去噪机制的确定性函数逼近任务。
result: 该方案在有界解问题上支持高达200维的计算，且在复杂无界PDE中表现出远超DBDP1方法的鲁棒性和精度。
conclusion: DBR方案显著提升了高维非线性方程求解的稳定性和泛化性能，并为变分不等式问题提供了理论支撑和扩展可能。
---

## 摘要
本文提出了一种用于求解高维非线性抛物型偏微分方程的深度后向回归（DBR）方案。在已有的 DBDP 方法（Huré 等人，2020）基础上，我们的算法引入了局部损失函数的重新表述，这些损失函数在每个时间步通过后向归纳法进行顺序优化。该方法的核心涉及将模拟的后向随机差分方程重新表述为其条件期望表示，从而将基于投影的随机优化问题转化为确定性的函数逼近任务。通过显式地引入条件期望，DBR 方案在损失评估之前提供了一种去噪机制。这种架构显著减轻了数值方差，从而增强了训练稳定性并提升了泛化性能。数值结果表明，DBR 方案始终优于 DBDP1 方法，对于有界解在维度高达 d=200 时仍能保持精度（见表 1）。值得注意的是，对于 DBDP1 方法在 d>10 时失效的复杂无界 PDE，DBR 方案在 d=20 时依然保持稳健，相对误差低于 9.7%（见表 6）。在理论上，我们推导了严格的误差上界，并证明了该方案具有半阶收敛性。此外，本文还提供了对变分不等式的扩展。

## Abstract
A deep backward regression-based (DBR) scheme for solving high-dimensional nonlinear parabolic partial differential equations is proposed. Building upon the established DBDP method (Huré et al., 2020), our algorithm introduces a reformulation of the local loss functions that are sequentially optimized via backward induction at each time step. The core of this approach involves reformulating simulated backward stochastic difference equations into their conditional expectation representations, thereby recasting a projection-based stochastic optimization problem as a deterministic function-approximation task. By explicitly incorporating conditional expectations, the DBR scheme facilitates a denoising mechanism prior to loss evaluation. This architecture substantially mitigates numerical variance, resulting in enhanced training stability and superior generalization performance. Numerical results demonstrate that the DBR scheme consistently outperforms the DBDP1 method, maintaining accuracy up to d=200 for bounded solutions (see Table 1). Notably, for complex unbounded PDEs where the DBDP1 method fails beyond d=10, the DBR scheme remains robust up to $d=20$ with relative errors under 9.7% (see Table 6}). Theoretically, we derive rigorous upper error bounds and establish half-order convergence for the proposed scheme. Extensions to variational inequalities are also provided.

---

## 论文详细总结（自动生成）

这是一份关于论文《A deep backward regression-based scheme for high-dimensional nonlinear partial differential equations》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文旨在解决**高维非线性抛物型偏微分方程（PDEs）**的数值求解难题，即所谓的“维度灾难”。
*   **研究背景**：传统的数值方法（如有限差分、有限元）在维度超过4时失效。近年来，基于深度学习的概率方法（如 Deep BSDE、DBDP）利用非线性 Feynman-Kac 公式将 PDE 转化为后向随机微分方程（BSDEs）进行求解。
*   **核心问题**：现有的 DBDP（Deep Backward Dynamic Programming）方法在损失函数中直接包含随机噪声项（布朗运动增量），导致训练过程中数值方差大、易过拟合单路径噪声，且在处理**无界解**或**复杂结构**的高维 PDE 时稳定性极差（通常在维度 $d > 10$ 时失效）。

### 2. 方法论：核心思想与技术细节
论文提出了 **DBR（Deep Backward Regression-based）方案**，其核心思想是将随机优化问题重构为基于条件期望的确定性函数逼近任务。
*   **核心思想**：利用 BSDE 的 Euler 离散化方案，将解 $Y_i$ 和梯度 $Z_i$ 表示为下一时刻状态的**条件期望**。通过显式的“去噪”机制（即多路径蒙特卡洛平均），在评估损失函数前平滑掉布朗运动带来的随机波动。
*   **关键技术细节**：
    1.  **损失函数重构**：不同于 DBDP 拟合单路径残差，DBR 定义了两个回归损失函数 $F_{y,i}$ 和 $F_{z,i}$，分别用于学习 $Y$ 和 $Z$ 的条件期望表示。
    2.  **分步优化策略**：在每个时间步，先优化梯度项 $Z_i$，再利用更新后的 $Z_i$ 优化解 $Y_i$，而非同时优化，这增强了收敛稳定性。
    3.  **多路径采样**：在每个时间步 ti，通过局部蒙特卡洛模拟生成多条子路径来近似条件期望，将“投影型随机优化”转变为“平滑的确定性函数拟合”。
    4.  **理论支撑**：论文不仅给出了算法，还推导了严格的误差上界，并证明了该方案具有**半阶收敛性**（half-order convergence）。
    5.  **扩展性**：该方案被进一步扩展到处理**变分不等式**（如美式期权定价中的障碍问题），并证明了相应的收敛性。

### 3. 实验设计
论文通过两个具有代表性的高维数值实验验证了 DBR 的性能：
*   **场景 1（有界解）**：一个 $d$ 维非线性抛物型 PDE，具有显式的指数型有界解析解。
    *   **Benchmark**：DBDP1 方法。
    *   **测试维度**：$d = 50, 80, 100, 200$。
*   **场景 2（无界解与复杂结构）**：具有二次非线性项和正弦/线性组合的无界解 PDE，结构更为复杂。
    *   **Benchmark**：DBDP1 方法。
    *   **测试维度**：$d = 1, 2, 8, 15, 20$。
*   **对比指标**：均值估计值、标准差、平均绝对误差、相对误差。

### 4. 资源与算力
论文明确记录了实验环境：
*   **硬件**：桌面计算机，配备 Intel Xeon Gold 6133 CPU (2.50GHz)，64GB RAM。
*   **GPU**：**NVIDIA GeForce RTX 4090 (24GB)**。
*   **软件**：Python 3.8, PyTorch 2.0, CUDA 11.8。
*   **训练细节**：每个时间步进行 3000 到 6000 次迭代，学习率为 $5 \times 10^{-4}$，使用 Adam 优化器。

### 5. 实验数量与充分性
*   **实验组数**：针对不同维度和时间步长（N=10 到 140）进行了多组实验。
*   **客观性与公平性**：为了消除随机采样带来的偏差，**每组实验均独立重复运行 10 次**，并报告了均值和标准差。
*   **充分性**：实验涵盖了从低维到超高维（200维）、从有界解到无界解的跨度，对比了该领域的经典基准算法（DBDP1），实验设计较为全面且客观。

### 6. 主要结论与发现
*   **高维鲁棒性**：在有界解情况下，DBR 在 200 维时相对误差仅为 3.42%，而 DBDP1 在同维度下误差更高且不够稳定。
*   **突破瓶颈**：在处理无界解的复杂 PDE 时，DBDP1 在 $d=15$ 和 $d=20$ 时彻底失效（相对误差高达 50%-80%），而 **DBR 在 20 维时仍能保持低于 9.7% 的相对误差**。
*   **稳定性**：通过条件期望的去噪机制，DBR 的训练过程比 DBDP1 更加平滑，有效避免了梯度振荡和过拟合噪声。

### 7. 优点
*   **去噪机制创新**：通过多路径平均代替单路径拟合，从根本上解决了随机噪声导致的训练不稳定问题。
*   **泛化能力强**：实验证明其在复杂、无界解的高维场景下具有极强的鲁棒性。
*   **理论完备**：提供了严谨的误差分析和收敛性证明，弥补了许多深度学习数值方法缺乏理论支撑的短板。
*   **应用广泛**：支持变分不等式，使其在金融工程（如复杂衍生品定价）等领域具有应用潜力。

### 8. 不足与局限
*   **计算开销增加**：由于引入了多路径蒙特卡洛采样来近似条件期望，其每个时间步的计算量（采样成本）显著高于单路径方法。
*   **超高维无界解测试缺失**：虽然有界解测试到了 200 维，但对于最具挑战性的无界解场景，实验仅止步于 20 维，更高维度的表现尚待验证。
*   **网络架构限制**：理论分析使用了 GroupSort 网络，但实际数值实验为了降低计算成本仍使用了普通的前馈神经网络，理论与实践之间存在微小偏差。

（完）
