---
title: A Least-Squares-Based Regularity-Conforming Neural Networks (LS-ReCoNNs) for Solving Parametric Transmission Problems
title_zh: 一种用于求解参数化传输问题的基于最小二乘的正则性符合神经网络 (LS-ReCoNNs)
authors: "Shima Baharlouei, Jamie Taylor, David Pardo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09663v1"
tags: ["query:pinn"]
score: 9.0
evidence: 求解参数化传输问题的深度学习方法
tldr: 针对具有异质材料、界面不连续和交点奇异性的参数化传输问题，本文提出了一种基于最小二乘的正则性一致神经网络（LS-ReCoNN）。该方法将解分解为主成分（含平滑和梯度跳跃部分）与奇异成分，利用深度神经网络逼近空间函数，并通过最小二乘法在线求解参数系数。实验证明，该方法能有效捕捉奇异性，在多种参数下保持高精度，为复杂物理系统的建模提供了高效的数值解法。
selection_source: fresh_fetch
motivation: 旨在解决异质材料物理建模中，因界面不连续和交点奇异性导致的参数化传输问题求解难题。
method: 提出LS-ReCoNN框架，通过解的成分分解结合深度神经网络与最小二乘求解器，并引入一致的能量范数误差上界作为损失函数。
result: 在一维和二维数值实验中，该方法成功捕捉了复杂的奇异性行为，并在不同参数实例下展现出优异的计算精度。
conclusion: LS-ReCoNN通过融合物理正则性先验与数据驱动技术，为处理具有复杂几何和参数依赖性的偏微分方程提供了高效方案。
---

## 摘要
本文专注于求解一维和二维空间中的参数化传输问题。这些问题属于一类偏微分方程，产生于具有异质材料的物理系统建模中。它们通常在界面处表现出不连续性，并在界面相交点处表现出奇异性。为了解决这些问题，我们提出了一种名为“基于最小二乘的正则性符合神经网络 (LS-ReCoNN)”的新型深度学习方法。该方法提出了一种损失函数，并证明其是能量范数误差的一致上界。该方法将解表示为主成分和奇异成分之和。主成分被分解为平滑部分和梯度跳跃部分，分别捕捉一维和二维问题中的正则解行为以及跨界面的正则性降低。引入奇异成分是为了对节点奇异性进行建模，并使用从一维有限元特征值问题计算出的基函数进行近似。对于主成分，采用了分离表示法，由参数相关系数和空间相关函数组成。深度神经网络近似空间相关函数，而参数相关系数由最小二乘求解器确定，其中每个参数实例的最优系数通过在线求解低维最小二乘问题获得。一维和二维的数值实验表明，LS-ReCoNN 在广泛的参数值范围内有效地捕捉了奇异性，同时保持了求解精度。

## Abstract
This article focuses on solving parametric transmission problems in one and two spatial dimensions. These problems belong to a class of partial differential equations that arise in the modeling of physical systems with heterogeneous materials. They often exhibit discontinuities across interfaces and singularities at points where interfaces intersect. To address these problems, we propose a new deep learning approach named {\it{Least-Squares-Based Regularity-Conforming Neural Network (LS-ReCoNN)}}. This approach proposes a loss function that is shown to be a consistent upper bound for the energy-norm error. The method represents the solution as the sum of a principal component and a singular component. The principal component is decomposed into smooth and gradient-jump parts, which capture both the regular solution behavior and reduced regularity across interfaces in one- and two-dimensional problems. The singular component is introduced to model junction singularities and it is approximated using basis functions computed from a one-dimensional finite element eigenvalue problem. For the principal component, a separated representation is employed, consisting of parameter-dependent coefficients and space-dependent functions. A deep neural network approximates the space-dependent functions, while the parameter-dependent coefficients are determined by a least-squares solver, where the optimal coefficients for each parameter instance are obtained online by solving a low-dimensional least-squares problem. Numerical experiments in one and two dimensions demonstrate that LS-ReCoNN effectively captures singularities while maintaining solution accuracy across a wide range of parameter values.

---

## 论文详细总结（自动生成）

以下是对论文《A Least-Squares-Based Regularity-Conforming Neural Networks (LS-ReCoNNs) for Solving Parametric Transmission Problems》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文旨在解决**参数化传输问题（Parametric Transmission Problems）**。这类问题常见于电磁学、流体力学和材料科学，其核心难点在于：
*   **物理特性复杂**：由于材料异质性，解在界面处存在梯度跳跃（Gradient Jumps），在多个界面交汇点处存在奇异性（Singularities）。
*   **数值挑战**：传统的有限元法（FEM）在处理奇异性时需要极细的网格，计算量大；而标准的物理信息神经网络（PINN）在处理低正则性解时容易产生吉布斯现象（Gibbs-type instabilities），导致精度低下。
*   **参数化需求**：在工程设计中，需要针对不同的材料参数（如电导率、渗透率）快速求解，传统方法每变换一次参数都需要重新计算，效率极低。

### 2. 方法论：核心思想与关键技术
论文提出了 **LS-ReCoNN** 框架，结合了正则性符合神经网络（ReCoNN）和最小二乘神经网络（LS-Net）的优势：
*   **解的成分分解**：将解 $\mathfrak{u}$ 分解为三部分：
    1.  **平滑部分 ($w$)**：捕捉整体趋势。
    2.  **梯度跳跃部分 ($v$)**：通过特殊的截断函数（Cutoff functions）显式嵌入界面处的导数不连续性。
    3.  **奇异成分 ($s$)**：针对顶点奇异性，利用**一维有限元特征值求解器**计算 Sturm-Liouville 问题的特征函数作为基函数，而非让神经网络硬学。
*   **分离表示与最小二乘求解**：
    *   使用深度神经网络（DNN）学习与空间相关的基函数（参数无关）。
    *   对于给定的参数实例 $p$，解表示为基函数的线性组合。
    *   **在线求解**：通过求解一个低维的线性最小二乘问题来确定组合系数，极大地提高了参数化扫描的速度。
*   **一致性损失函数**：设计了一个基于残差的二次损失函数，并从理论上证明了该损失函数是能量范数（$H^1$ 误差）的一致上界，确保了训练的收敛性。

### 3. 实验设计
论文设计了三个主要数值实验场景：
*   **一维传输问题**：包含 5 个子区域，参数 $p_i$ 在 $[0.01, 50]$ 范围内随机取值。
*   **二维 $2 \times 2$ 材料配置**：中心有一个奇异点，参数 $p_i \in [1, 10]$。
*   **二维 $4 \times 4$ 材料配置**：包含 9 个内部奇异点，参数空间维度更高，测试模型处理复杂几何和多奇异点的能力。
*   **Benchmark（基准）**：主要对比了高精度**有限元法（FEM）**结果和半解析解。
*   **对比维度**：对比了参数化训练与单实例（非参数化）训练的精度差异。

### 4. 资源与算力
*   **算力说明**：文中未明确提及具体的 GPU 型号或数量。
*   **训练细节**：
    *   一维实验：训练 1,000 次迭代，使用 Adam 优化器。
    *   二维实验：训练 20,000 到 50,000 次迭代。
    *   **效率优势**：作者强调了该方法的计算成本主要集中在参数无关的 NN 评估上，一旦训练完成，针对新参数的求解（LS 步骤）几乎是瞬时的，且总执行时间对参数实例数量 $N_p$ 不敏感。

### 5. 实验数量与充分性
*   **实验规模**：针对每个场景，作者使用了 200 到 1000 个随机采样的参数实例进行验证。
*   **充分性**：实验涵盖了一维到二维、单奇异点到多奇异点的演进，并提供了误差分布的统计直方图。
*   **客观性**：通过与高精度 FEM 结果的绝对误差图对比，直观展示了模型在奇异点附近的表现。实验设计较为公平，考虑了离散不变性（Discretization invariance），即可以用粗积分训练，用细积分后处理。

### 6. 主要结论与发现
*   **奇异性捕捉**：LS-ReCoNN 能够精确捕捉界面跳跃和顶点奇异性，避免了标准 NN 的振荡问题。
*   **参数化优势**：参数化训练不仅能一次性解决一类问题，且由于参数空间的平滑性，其收敛表现往往优于针对单一实例的训练。
*   **精度提升**：在 1D 实验中，平均相对误差可降至 $10^{-5}\%$ 级别；在复杂的 2D 实验中，解的相对误差保持在 $0.1\%-1\%$ 左右。
*   **理论验证**：实验结果验证了损失函数作为误差上界的理论推导。

### 7. 优点（亮点）
*   **物理启发式架构**：通过数学分解将解的正则性先验（如特征函数）直接嵌入架构，降低了 NN 的学习难度。
*   **混合求解模式**：巧妙结合了特征值求解（传统数值法）、神经网络（函数逼近）和最小二乘（线性优化），取长补短。
*   **高效在线推理**：LS-Net 策略使得该方法在处理大规模参数扫描时具有极高的工业应用潜力。

### 8. 不足与局限
*   **先验依赖**：该方法依赖于对解的奇异性结构和界面位置的**预先了解**。如果界面位置是动态变化的或未知的，该方法难以直接应用。
*   **线性限制**：目前的 LS 框架主要针对线性参数化 PDE，扩展到非线性问题需要进一步研究。
*   **维度扩展**：虽然展示了 2D 结果，但扩展到 3D 需要求解 2D 特征值问题，计算复杂度会显著增加。
*   **应用范围**：主要针对传输类问题，对于波动方程或其他类型的 PDE 尚需验证。

（完）
