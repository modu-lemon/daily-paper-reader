---
title: Deep Ritz Physics-Informed Neural Network Method for Solving the Variational Inequality
title_zh: 求解变分不等式的深度 Ritz 物理信息神经网络方法
authors: "Qijia Zhou, Yiyang Wang, Shengyuan Deng, Chenliang Li"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11544v1"
tags: ["query:pinn"]
score: 10.0
evidence: 用于求解变分不等式的Deep Ritz物理信息神经网络
tldr: 本研究针对机械工程和流体渗透等领域的变分不等式问题，提出了一种结合物理信息神经网络（PINN）的Deep Ritz方法。该方法利用Ritz变分原理将变分不等式转化为优化问题，并引入贝叶斯优化调整损失函数权重及残差自适应数据集更新策略，显著提升了求解椭圆变分不等式的精度与效率，为复杂工程计算提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决传统方法在处理机械工程及流体渗透等领域椭圆变分不等式时精度和效率不足的问题。
method: 通过Ritz变分法将不等式转化为优化问题，并结合贝叶斯权重优化与残差自适应数据集更新策略进行求解。
result: 数值实验验证了该方法在逼近解析解方面的有效性，展现了较高的计算精度。
conclusion: 提出的Deep Ritz PINN方法是解决变分不等式问题的一种高效且可靠的数值模拟工具。
---

## 摘要
变分不等式广泛应用于机械工程、流体渗透、交通运输等领域。本文提出了一种基于物理信息神经网络（PINNs）的深度 Ritz 方法，旨在提高求解椭圆变分不等式的精度和效率。首先利用 Ritz 变分法将变分不等式问题转化为优化问题。随后，采用贝叶斯优化来调整损失函数的权重，并引入一种基于残差的自适应数据集更新策略，以提高模型的收敛性和精度。数值实验表明，所提方法能够有效逼近解析解。

## Abstract
Variational inequalities are widely applied in mechanical engineering, fluid penetration, transportation, and other fields. In this paper, a Deep Ritz method based on Physics-Informed Neural Networks (PINNs) is proposed to enhance the accuracy and efficiency of solving elliptic variational inequalities. The Ritz variational method is firstly utilized to transform the variational inequality problem into an optimization problem. Then Bayesian optimization is employed to tune the weights of the loss function, and a residual-based adaptive dataset update strategy is introduced to improve the convergence and accuracy of the model. Numerical experiments show that the proposed method can effectively approximate the analytical solution.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合 Deep Ritz 方法与物理信息神经网络（PINNs）的新型数值方法，专门用于求解椭圆变分不等式问题（EVIP）。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：椭圆变分不等式（EVIP）和互补问题在机械工程、流体渗透、交通运输等领域有广泛应用，但传统数值算法（如有限元法）往往面临计算成本高、处理复杂约束困难的问题。
*   **研究动机**：虽然 PINNs 在求解偏微分方程（PDEs）方面表现出色，但在变分不等式领域的应用尚未得到充分探索。本文旨在通过引入变分原理和自适应优化策略，提升 PINNs 求解此类问题的精度、收敛速度和处理约束的能力。

### 2. 论文提出的方法论：Deep Ritz-PINNs (DRPINNs)
*   **核心思想**：将变分不等式问题转化为等价的能量泛函最小化问题（Ritz 变分法），并利用神经网络作为函数逼近器进行求解。
*   **关键技术细节**：
    *   **Ritz 变分转化**：将变分不等式转化为优化模型 $J(u) = \min J(v)$，其中 $J(v)$ 包含能量项和源项。
    *   **损失函数构造**：损失函数由三部分组成：区域项（基于 Ritz 泛函）、约束项（强制解的非负性 $u \ge 0$）和边界项（强制满足 Dirichlet 边界条件）。
    *   **贝叶斯优化（BO）权重调节**：引入贝叶斯优化动态调整损失函数中各项的权重（$w_1, w_2, w_3$），解决了多任务学习中梯度消失或权重失衡的问题。
    *   **基于残差的自适应数据集更新**：不使用固定采样点，而是根据当前模型的残差分布动态生成新样本点，使模型重点学习“难拟合”的区域（如约束边界或高梯度区）。
*   **算法流程**：使用 Adam 优化器进行参数更新，每隔一定迭代次数通过 BO 更新权重，并根据残差分布重新采样训练点。

### 3. 实验设计
*   **实验场景**：
    1.  **1D 障碍问题**：经典的单维约束问题。
    2.  **2D 椭圆变分不等式**：在正方形区域内的二维问题。
    3.  **带空间相关源项的 2D 问题**：测试模型处理复杂源项的能力。
    4.  **3D 问题**：在立方体区域内，通过径向坐标转换验证高维处理能力。
*   **对比基准（Benchmarks）**：
    *   **Barrier DNN (DEQ)**：深度平衡模型。
    *   **ALDL**：增广拉格朗日深度学习方法。
*   **消融实验**：分别对比了“无数据集更新”和“无贝叶斯优化”的情况，以验证各模块的贡献。

### 4. 资源与算力
*   **硬件配置**：
    *   **GPU**：NVIDIA RTX 4090D (24GB 显存)。
    *   **CPU**：192 核，主频 1130.5 MHz。
*   **训练细节**：迭代次数设定为 10 万次（100k epochs），或直到误差低于 $10^{-4}$。学习率范围在 0.0001 到 0.01 之间。

### 5. 实验数量与充分性
*   **实验规模**：论文设计了 4 组主要的数值案例（从 1D 到 3D），每组案例均包含误差分布图、损失下降曲线和预测值与解析解的对比。
*   **充分性评价**：实验设计较为充分。通过与 ALDL 和 Barrier DNN 的对比，证明了该方法在收敛速度和最终精度上的优势。消融实验定量展示了贝叶斯优化对模型收敛的关键作用（不加 BO 时误差显著增大）。实验覆盖了不同维度和边界条件，具有较好的说服力。

### 6. 论文的主要结论与发现
*   **高精度逼近**：DRPINNs 能够精确捕捉变分不等式中解与障碍物“接触”和“分离”的特征。
*   **优越的收敛性**：相比于 ALDL 出现的剧烈震荡和 Barrier DNN 的收敛停滞，DRPINNs 在训练初期即可快速下降并保持稳定。
*   **策略有效性**：贝叶斯优化能有效平衡物理约束与边界条件之间的竞争，而自适应采样显著提升了全局拟合精度。

### 7. 优点（亮点）
*   **理论与深度学习结合**：巧妙地将 Ritz 变分原理引入 PINN 框架，将不等式约束转化为更易处理的优化目标。
*   **自动化调参**：利用贝叶斯优化解决了 PINN 训练中极其痛苦的“手动调权重”问题。
*   **动态采样**：基于残差的采样策略提高了数据效率，使模型在复杂区域表现更稳健。

### 8. 不足与局限
*   **高梯度区域波动**：在 2D 实验（Example 2）中，作者提到在解变化剧烈的区域（高梯度区），误差仍有微小波动，说明模型在处理极高正则化要求的区域时仍有提升空间。
*   **计算开销**：虽然引入贝叶斯优化和自适应采样提升了精度，但这些额外的计算步骤（如高斯过程拟合、残差排序采样）会增加单次迭代的时间成本，文中未详细对比总训练时长。
*   **应用限制**：目前仅验证了椭圆型变分不等式，对于随时间演化的抛物型变分不等式或其他更复杂的非线性约束尚未涉及。

（完）
