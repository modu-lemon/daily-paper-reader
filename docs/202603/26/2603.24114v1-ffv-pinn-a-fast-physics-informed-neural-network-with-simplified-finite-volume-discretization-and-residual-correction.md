---
title: "FFV-PINN: A Fast Physics-Informed Neural Network with Simplified Finite Volume Discretization and Residual Correction"
title_zh: FFV-PINN：一种结合简化有限体积离散与残差修正的快速物理信息神经网络
authors: "Chang Wei, Yuchen Fan, Jian Cheng Wong, Chin Chun Ooi, Heyang Wang, Pao-Hsiung Chiu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24114v1"
tags: ["query:pinn"]
score: 10.0
evidence: 集成有限体积离散化和残差修正的快速PINN框架
tldr: 针对物理信息神经网络（PINN）在处理复杂流体动力学时存在的物理约束弱、收敛慢及不稳定性问题，本文提出了一种快速有限体积PINN框架（FFV-PINN）。该方法结合了简化的有限体积法（FVM）离散化对流项和残差修正损失项，显著提升了训练效率和精度。实验证明，FFV-PINN首次在无数据情况下实现了高雷诺数和高瑞利数流场的快速求解，展现了其作为高效偏微分方程求解器的潜力。
selection_source: fresh_fetch
motivation: 传统PINN利用自动微分构建损失函数时忽略了邻域信息，导致在复杂流体问题中物理约束力弱且收敛困难。
method: 提出FFV-PINN框架，通过简化有限体积法离散化对流项并引入残差修正损失项来加速收敛并优化色散耗散行为。
result: 在Re=10000的顶盖驱动流和Ra=1e8的自然对流等挑战性问题上，该方法仅需数百秒即可实现高精度的无数据求解。
conclusion: FFV-PINN显著提高了PINN的计算速度和准确性，是解决高复杂度流体动力学问题的一种极具竞争力的神经偏微分方程求解器。
---

## 摘要
物理信息神经网络 (PINNs) 已成为一个主要的研究焦点。然而，目前的 PINNs 面临着若干局限性。首先，在使用自动微分构建损失函数时，PINNs 往往忽略了相邻点的信息，这阻碍了其施加物理约束的能力并降低了精度。此外，PINN 训练过程中存在不稳定性及收敛性差等问题，限制了其在复杂流体动力学问题中的应用。为了应对这些挑战，本文提出了一种集成简化有限体积法 (FVM) 和残差修正损失项的快速物理信息神经网络框架，称为快速有限体积 PINN (FFV-PINN)。FFV-PINN 对对流项采用了简化的 FVM 离散，并相应地改善了色散和耗散行为。与传统 FVM 不同，FFV-PINN 的输出可以直接用于近似控制面上的值，从而简化了离散过程。此外，引入了残差修正损失项，显著加速了收敛并提高了训练效率。为了验证性能，我们解决了一系列具有挑战性的问题，包括二维稳态和非稳态顶盖驱动方腔流、三维稳态顶盖驱动方腔流、后向台阶流，以及高雷诺数和高瑞利数下的自然对流。值得注意的是，FFV-PINN 在 PINN 相关文献中首次实现了 Re = 10000 的顶盖驱动方腔流和 Ra = 1e8 的自然对流的无数据求解，且分别仅需 680 秒和 231 秒。这进一步凸显了 FFV-PINN 在提高速度和精度方面的有效性，标志着 PINNs 作为具有竞争力的神经偏微分方程 (PDE) 求解器又向前迈进了一步。

## Abstract
Physics-informed neural networks (PINNs) have emerged as a major research focus. However, today's PINNs encounter several limitations. Firstly, during the construction of the loss function using automatic differentiation, PINNs often neglect information from neighboring points, which hinders their ability to enforce physical constraints and diminishes their accuracy. Furthermore, issues such as instability and poor convergence persist during PINN training, limiting their applicability to complex fluid dynamics problems. To address these challenges, a fast physics-informed neural network framework that integrates a simplified finite volume method (FVM) and residual correction loss term has been proposed, referred to as Fast Finite Volume PINN (FFV-PINN). FFV-PINN utilizes a simplified FVM discretization for the convection term, with an accompanying improvement in the dispersion and dissipation behavior. Unlike traditional FVM, the FFV-PINN outputs can be simply and directly harnessed to approximate values on control surfaces, thereby simplifying the discretization process. Moreover, a residual correction loss term is introduced to significantly accelerates convergence and improves training efficiency. To validate the performance, we solve a series of challenging problems -- including flow in the two-dimensional steady and unsteady lid-driven cavity, three-dimensional steady lid-driven cavity, backward-facing step flows, and natural convection at high Reynolds number and Rayleigh number. Notably, the FFV-PINN can achieve data-free solutions for the lid-driven cavity flow at Re = 10000 and natural convection at Ra = 1e8 for the first time in PINN literature, even while requiring only 680s and 231s. It further highlights the effectiveness of FFV-PINN in improving both speed and accuracy, marking another step forward in the progression of PINNs as competitive neural PDE solvers.

---

## 论文详细总结（自动生成）

### FFV-PINN 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
物理信息神经网络（PINNs）在求解偏微分方程（PDEs）方面展现了巨大潜力，但传统 PINN 框架存在三大痛点：
*   **物理约束薄弱**：依赖自动微分（AD）计算单点残差，忽略了空间邻域点之间的守恒关系。
*   **训练不稳定与收敛慢**：优化景观高度非凸，导致在处理高非线性流体问题时容易震荡或停滞。
*   **复杂流体挑战**：在处理高雷诺数（$Re$）或高瑞利数（$Ra$）问题时，传统 PINN 往往需要大量标注数据辅助，否则难以收敛。

本文提出的 **FFV-PINN** 旨在通过集成**简化有限体积离散**和**残差修正损失**，构建一个既能严格遵守物理守恒定律，又能实现极速收敛的“数据驱动+数值格式”混合框架。

---

#### 2. 方法论：核心思想与关键技术
FFV-PINN 的核心由两个创新模块组成：

*   **简化有限体积法（Simplified FVM）**：
    *   **核心思想**：不同于传统 FVM 需要根据流向选择复杂的离散格式（如一阶迎风、二阶迎风或中心差分），FFV-PINN 利用神经网络可以在任意坐标求值的特性，直接在控制体界面（Control Surfaces）上采样。
    *   **技术细节**：将动量方程转化为积分形式，对流项通过界面值直接近似。傅里叶分析表明，该方案具有**零耗散（Zero-dissipation）**特性，且色散误差极小，能更好地捕捉高频物理信息。
*   **残差修正损失（Residual Correction Loss, $L_{rc}$）**：
    *   **核心思想**：借鉴计算流体力学（CFD）中经典的 **SIMPLE 算法**思想。
    *   **算法流程**：利用当前 epoch 和前一 epoch 的预测值进行二阶外推，估算满足物理约束所需的修正量 $R$。通过引入松弛因子 $\alpha$，强制神经网络的参数更新方向与物理残差减小的方向一致。这不仅增强了物理约束，还起到了类似二阶优化器的加速效果。

---

#### 3. 实验设计
论文针对流体力学中极具挑战性的基准问题进行了验证：
*   **场景与数据集**：
    1.  **2D 顶盖驱动方腔流（LDC）**：稳态（$Re$ 高达 10,000）与瞬态（$Re=400$）。
    2.  **3D 稳态顶盖驱动方腔流**：$Re=1,000$。
    3.  **后向台阶流（BFS）**：$Re$ 范围 100-800，关注分离点与再附着点。
    4.  **自然对流（NC）**：$Ra$ 高达 $10^8$ 的热流耦合问题。
*   **Benchmark（基准）**：采用 Ghia (1982) 的经典数据、Erturk (2008) 的高精度数值解以及作者自研的高精度 CFD 求解器结果。
*   **对比方法**：与 JAXPI, PirateNet, ev-NSFnet, TSONN 以及最新的 SOTA 方法 **SOAP** 进行了精度和速度的全面对比。

---

#### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA RTX 3090 GPU** 上完成。
*   **软件框架**：基于 **JAX** 库实现。
*   **训练时长**：
    *   $Re=10,000$ 的 2D LDC 问题：仅需 **680 秒**。
    *   $Ra=10^8$ 的自然对流问题：仅需 **231 秒**。
    *   相比之下，SOTA 方法 SOAP 在更高端的 A6000 GPU 上处理 $Re=5,000$ 问题需 8.25 小时，FFV-PINN 实现了约 **125 倍的加速**。

---

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 2D 到 3D、从稳态到瞬态、从纯流体到热流耦合的 5 大类典型实验。
*   **参数覆盖**：对 $Re$ 数和 $Ra$ 数进行了宽量程扫描（如 $Re$ 从 100 跨越到 10,000），验证了模型在不同非线性强度下的鲁棒性。
*   **充分性评价**：实验设计非常充分且具有挑战性。特别是 $Re=10,000$ 和 $Ra=10^8$ 是 PINN 领域公认的“硬骨头”，论文在**无数据（Data-free）**条件下取得成功，证明了方法的客观有效性。

---

#### 6. 主要结论与发现
*   **性能突破**：FFV-PINN 是文献中首个在无数据辅助下成功求解 $Re=10,000$ 顶盖驱动流和 $Ra=10^8$ 自然对流问题的 PINN 变体。
*   **效率飞跃**：通过残差修正损失，模型在极短的时间内（数百秒）即可达到传统方法数小时甚至数天才能达到的精度。
*   **物理一致性**：FVM 的引入确保了局部和全局的守恒特性，使得预测的流场结构（如次级涡、热边界层）与高精度 CFD 结果高度吻合。

---

#### 7. 优点（亮点）
*   **简化离散**：摆脱了传统 FVM 对迎风格式的依赖，降低了数值实现的复杂度。
*   **数值特性优异**：零耗散特性使其在捕捉复杂涡结构时比传统基于 AD 的 PINN 更具优势。
*   **极速收敛**：残差修正项有效地引导了优化轨迹，解决了 PINN 训练中的“梯度病态”问题。
*   **通用性强**：可直接扩展至 3D 和非稳态问题，且不依赖初始条件计算守恒量。

---

#### 8. 不足与局限
*   **极小尺度捕捉不足**：在 $Re=10,000$ 实验中，虽然捕捉到了主涡和主要次级涡，但未能完美还原左下角极小尺度的第三级涡结构。
*   **超参数敏感性**：残差修正损失中的松弛因子 $\alpha$ 仍需根据具体问题进行经验性调节（通常在 0.7 到 0.98 之间），缺乏自动化的确定机制。
*   **网格依赖**：虽然是神经网络，但其物理损失构建在控制体网格上，对于极度复杂的几何边界，网格划分的质量仍会影响最终精度。

（完）
