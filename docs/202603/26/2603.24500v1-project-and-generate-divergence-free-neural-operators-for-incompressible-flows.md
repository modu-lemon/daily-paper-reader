---
title: "Project and Generate: Divergence-Free Neural Operators for Incompressible Flows"
title_zh: 投影与生成：用于不可压缩流体的无散神经营算子
authors: "Xigui Li, Hongwei Zhang, Ruoxi Jiang, Deshu Chen, Chensen Lin, Limei Han, Yuan Qi, Xin Guo, Yuan Cheng"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24500v1"
tags: ["query:pinn"]
score: 10.0
evidence: 用于不可压缩流体动力学的无散度神经算子
tldr: 针对流体力学学习模型中物理约束缺失导致的模拟不稳定问题，本文提出了一种统一框架，为确定性和生成式模型引入硬性无散度约束。通过集成微分谱Leray投影和构建基于旋度的无散度高斯参考测度，确保速度场严格遵循不可压缩连续性方程。实验证明该方法在2D纳维-斯托克斯方程上实现了极高的物理一致性和长期模拟稳定性。
selection_source: fresh_fetch
motivation: 现有的基于学习的流体模型通常在无约束空间运行，导致物理上不可行且模拟不稳定，而软约束方法无法提供结构性保证。
method: 结合基于亥姆霍兹-霍奇分解的微分谱Leray投影处理确定性模型，并利用旋度映射构建无散度高斯参考测度以支持生成式建模。
result: 在2D纳维-斯托克斯方程的实验中，该方法实现了除离散化误差外的精确不可压缩性，并显著提升了模拟的稳定性和物理一致性。
conclusion: 将物理定律作为硬约束嵌入神经算子框架，是提升流体动力学模型可靠性和泛化能力的有效途径。
---

## 摘要
基于学习的流体动力学模型通常在无约束的函数空间中运行，导致物理上不可行且不稳定的模拟。虽然基于惩罚的方法提供了软正则化，但它们没有提供结构性保证，从而导致伪散度产生和长期崩溃。在这项工作中，我们引入了一个统一框架，将不可压缩连续性方程作为确定性建模和生成式建模的硬性内在约束。首先，为了将确定性模型投影到无散子空间，我们集成了一个基于 Helmholtz-Hodge 分解的可微谱 Leray 投影，从而将回归假设空间限制在物理上可行的速度场内。其次，为了生成物理一致的分布，我们表明当先验不兼容时，仅投影模型输出是不够的。为解决这一问题，我们通过基于旋度的前推（pushforward）构建了一个无散高斯参考测度，从而通过构造确保整个概率流保持子空间一致性。在二维 Navier-Stokes 方程上的实验表明，在离散化误差范围内实现了精确的不可压缩性，并显著提高了稳定性和物理一致性。

## Abstract
Learning-based models for fluid dynamics often operate in unconstrained function spaces, leading to physically inadmissible, unstable simulations. While penalty-based methods offer soft regularization, they provide no structural guarantees, resulting in spurious divergence and long-term collapse. In this work, we introduce a unified framework that enforces the incompressible continuity equation as a hard, intrinsic constraint for both deterministic and generative modeling. First, to project deterministic models onto the divergence-free subspace, we integrate a differentiable spectral Leray projection grounded in the Helmholtz-Hodge decomposition, which restricts the regression hypothesis space to physically admissible velocity fields. Second, to generate physically consistent distributions, we show that simply projecting model outputs is insufficient when the prior is incompatible. To address this, we construct a divergence-free Gaussian reference measure via a curl-based pushforward, ensuring the entire probability flow remains subspace-consistent by construction. Experiments on 2D Navier-Stokes equations demonstrate exact incompressibility up to discretization error and substantially improved stability and physical consistency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Project and Generate: Divergence-Free Neural Operators for Incompressible Flows》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在流体动力学（CFD）的深度学习建模中，不可压缩性（$\nabla \cdot \mathbf{u} = 0$）是核心物理准则。
*   **核心痛点**：现有的神经算子（如 FNO）通常在无约束的函数空间运行。即使训练数据是无散的，模型预测仍会产生“伪散度”。传统的“软约束”（如 PINNs 的惩罚项）无法保证精确一致性，导致长期模拟时能量谱畸变、产生非物理的源/汇，甚至导致数值模拟崩溃。
*   **研究动机**：将不可压缩约束从“外部惩罚”转变为“内在结构”，通过硬性约束确保模型输出始终位于物理上可行的无散子空间内。

### 2. 方法论
论文提出了一个名为 **PROJECT & GENERATE** 的统一框架，涵盖了确定性回归和生成式建模：

*   **核心思想**：利用 **Helmholtz-Hodge 分解**，将任意矢量场分解为无散项（Solenoidal）和梯度项（Irrotational）。通过 **Leray 投影 (P)** 算子强制移除梯度项。
*   **关键技术细节**：
    *   **确定性回归 (Project)**：在神经算子（如 FNO）的输出端集成一个可微的“谱 Leray 投影层”。在傅里叶空间中，利用公式 $\hat{\mathbf{u}}(\mathbf{k}) = (\mathbf{I} - \frac{\mathbf{k} \otimes \mathbf{k}}{\|\mathbf{k}\|^2}) \hat{\mathbf{w}}(\mathbf{k})$ 强制消除散度。该过程不含可学习参数，且计算效率极高。
    *   **生成式建模 (Generate)**：论文指出，仅投影输出是不够的，因为如果先验噪声与物理约束不兼容，概率流会变得定义不良。
    *   **无散高斯参考测度**：通过“旋度前推”（Curl-based pushforward）构造噪声。先采样一个标量流函数 $\psi$，再通过 $\mathbf{u} = \nabla^\perp \psi$ 构造初始噪声。这确保了从初始噪声到目标数据的整个概率路径（Flow Matching）都严格处于无散子空间内。

### 3. 实验设计
*   **数据集/场景**：二维不可压缩 Navier-Stokes 方程，雷诺数 $Re = 1000$。
*   **任务设置**：给定 15 帧历史观测，预测后续 35 帧，并进行长达 300 步的长期外推（Rollout）以测试稳定性。
*   **Benchmark 与对比方法**：
    *   **Reg. Base**：无约束的 FNO 回归模型。
    *   **Gen. Base**：标准条件流匹配（Conditional Flow Matching）生成模型。
    *   **Reg. Ours / Gen. Ours**：集成了 Leray 投影和无散先验的本文方法。
*   **评估指标**：均方误差 (MSE)、散度误差 (Div)、能谱分布 (Enstrophy Spectrum) 以及压力场重建质量。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 `dopri5` 自适应 ODE 求解器进行采样，设置了 $10^{-5}$ 的容差，并在推理时使用了 50 次函数评估 (NFE)。

### 5. 实验数量与充分性
*   **实验规模**：使用了 10,000 条训练轨迹，测试集包含 100 条长轨迹。
*   **充分性分析**：
    *   实验对比了回归和生成两种主流范式，证明了硬约束在不同架构下的普适性。
    *   **长期稳定性测试**：外推步数达到了训练步数的 8 倍以上（300 步 vs 35 步），这在神经算子研究中是非常严苛且充分的测试。
    *   **统计显著性**：生成模型的结果基于 20 次独立运行的均值和标准差，结果具有说服力。
    *   **多维度验证**：不仅对比了像素级误差（MSE），还通过物理量（压力、能谱）验证了物理一致性。

### 6. 主要结论与发现
*   **硬约束是稳定性的关键**：无约束模型（Base）在长期外推时散度误差迅速累积，最终导致数值爆炸（Inf）；而 Ours 模型散度保持在机器精度（$10^{-7}$），模拟极其稳定。
*   **生成模型更具优势**：在处理混沌流体系统时，Gen. Ours 的长期预测误差远低于 Reg. Ours。这表明在物理流形上学习概率分布比单纯的确定性回归更能捕捉复杂的动力学特征。
*   **压力场恢复**：只有满足硬性无散约束的模型才能通过泊松方程重建出平滑、准确的压力场。

### 7. 优点
*   **物理严谨性**：通过数学构造而非损失函数强制实现物理定律，解决了深度学习模型常见的“非物理伪影”问题。
*   **模块化与通用性**：Leray 投影层可以无缝集成到任何现有的神经算子架构中，无需复杂的超参数调优（如 PINNs 的权重平衡）。
*   **计算高效**：利用 FFT 实现谱投影，避免了传统 CFD 中耗时的迭代式压力求解。

### 8. 不足与局限
*   **几何局限性**：目前的谱实现主要针对周期性边界条件的矩形区域（单元环面）。对于具有复杂固体边界或非规则几何形状的流场，需要开发基于非结构化网格或稀疏算子的投影方法。
*   **维度扩展**：虽然理论框架支持 3D，但论文仅展示了 2D 实验结果，3D 情况下的计算开销和表现尚待验证。
*   **算力透明度**：缺乏具体的训练资源消耗说明，不便于其他研究者评估复现成本。

（完）
