---
title: Stochastic Dimension-Free Zeroth-Order Estimator for High-Dimensional and High-Order PINNs
title_zh: 面向高维高阶 PINNs 的随机无维度零阶估计器
authors: "Zhangyong Liang, Ji Zhang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24002v1"
tags: ["query:pinn"]
score: 10.0
evidence: 用于高维高阶PINN的零阶估计器
tldr: 针对高维高阶偏微分方程中物理信息神经网络（PINN）面临的计算复杂度和显存开销巨大的挑战，本文提出了随机无维度零阶估计器（SDZE）。该框架通过公共随机数同步技术消除了零阶优化与随机空间算子结合时的方差爆炸问题，并利用隐式子空间投影降低了参数搜索方差。SDZE 实现了空间和显存复杂度的双重维度无关性，支持在单张 GPU 上训练千万维度的 PINN，显著提升了训练效率。
selection_source: fresh_fetch
motivation: 旨在解决高维高阶 PINN 在计算空间导数时的高复杂度以及反向传播带来的巨大显存瓶颈。
method: 提出 SDZE 框架，通过同步随机种子消除方差爆炸，并采用隐式子空间投影实现显存高效的零阶优化。
result: 该方法成功在单块 A100 GPU 上完成了千万维度 PINN 的训练，在速度和显存利用率上均优于现有技术。
conclusion: SDZE 为极高维度物理问题的数值求解提供了一种计算复杂度与维度无关的高效优化方案。
---

## 摘要
用于高维高阶偏微分方程（PDEs）的物理信息神经网络（PINNs）主要受限于 $\mathcal{O}(d^k)$ 的空间导数复杂度和反向传播（BP）带来的 $\mathcal{O}(P)$ 内存开销。虽然随机空间估计器成功地将空间复杂度降低至 $\mathcal{O}(1)$，但其对一阶优化的依赖在规模扩大时仍会导致极高的内存消耗。零阶（ZO）优化提供了一种无需反向传播的替代方案；然而，简单地将随机空间算子与零阶扰动结合会引发 $\mathcal{O}(1/\varepsilon^2)$ 的方差爆炸，从而导致数值发散。为了应对这些挑战，我们提出了随机无维度零阶估计器（SDZE），这是一个在空间和内存方面均实现维度无关复杂度的统一框架。具体而言，SDZE 利用公共随机数同步（CRNS），通过在不同扰动间锁定空间随机种子，从代数上抵消了 $\mathcal{O}(1/\varepsilon^2)$ 的方差。此外，引入了一种隐式无矩阵子空间投影，将参数探索方差从 $\mathcal{O}(P)$ 降低至 $\mathcal{O}(r)$，同时保持 $\mathcal{O}(1)$ 的优化器内存占用。实验结果表明，SDZE 能够在单块 NVIDIA A100 GPU 上训练千万维度的 PINNs，在速度和内存效率上较现有最先进的基准方法有显著提升。

## Abstract
Physics-Informed Neural Networks (PINNs) for high-dimensional and high-order partial differential equations (PDEs) are primarily constrained by the $\mathcal{O}(d^k)$ spatial derivative complexity and the $\mathcal{O}(P)$ memory overhead of backpropagation (BP). While randomized spatial estimators successfully reduce the spatial complexity to $\mathcal{O}(1)$, their reliance on first-order optimization still leads to prohibitive memory consumption at scale. Zeroth-order (ZO) optimization offers a BP-free alternative; however, naively combining randomized spatial operators with ZO perturbations triggers a variance explosion of $\mathcal{O}(1/\varepsilon^2)$, leading to numerical divergence. To address these challenges, we propose the \textbf{S}tochastic \textbf{D}imension-free \textbf{Z}eroth-order \textbf{E}stimator (\textbf{SDZE}), a unified framework that achieves dimension-independent complexity in both space and memory. Specifically, SDZE leverages \emph{Common Random Numbers Synchronization (CRNS)} to algebraically cancel the $\mathcal{O}(1/\varepsilon^2)$ variance by locking spatial random seeds across perturbations. Furthermore, an \emph{implicit matrix-free subspace projection} is introduced to reduce parameter exploration variance from $\mathcal{O}(P)$ to $\mathcal{O}(r)$ while maintaining an $\mathcal{O}(1)$ optimizer memory footprint. Empirical results demonstrate that SDZE enables the training of 10-million-dimensional PINNs on a single NVIDIA A100 GPU, delivering significant improvements in speed and memory efficiency over state-of-the-art baselines.

---

## 论文详细总结（自动生成）

### 论文总结：面向高维高阶 PINNs 的随机无维度零阶估计器 (SDZE)

#### 1. 核心问题与整体含义（研究动机和背景）
物理信息神经网络（PINNs）在求解高维、高阶偏微分方程（PDEs）时面临严重的“维度灾难”：
*   **空间复杂度瓶颈**：计算 $d$ 维 $k$ 阶导数的复杂度为 $\mathcal{O}(d^k)$，随维度和阶数指数级增长。
*   **显存墙瓶颈**：传统 PINN 依赖反向传播（BP）计算梯度，其显存开销与模型参数量 $P$ 成正比。即使使用随机空间估计器（如 SDGD）将空间复杂度降至 $\mathcal{O}(1)$，BP 过程产生的计算图缓存仍会导致在大规模参数下发生显存溢出（OOM）。
*   **双重方差死锁**：若直接将零阶（ZO）优化与随机空间算子结合，空间采样的随机性会被有限差分的扰动步长 $\epsilon$ 放大，导致 $\mathcal{O}(1/\epsilon^2)$ 的方差爆炸，使模型无法收敛。

#### 2. 论文提出的方法论：SDZE 框架
SDZE 旨在实现空间计算和显存占用的双重“维度无关性”。其核心思想包括：
*   **公共随机数同步 (CRNS)**：这是解决方差爆炸的关键。在进行零阶优化的正负两次扰动评估时，强制锁定并使用完全相同的空间随机种子（即相同的维度采样索引）。通过代数抵消，空间截断噪声在有限差分中被消除，从而将方差从 $\mathcal{O}(1/\epsilon^2)$ 降至常数级。
*   **隐式无矩阵子空间投影**：为了降低零阶优化的参数搜索方差（从 $\mathcal{O}(P)$ 降至 $\mathcal{O}(r)$），SDZE 将扰动限制在低秩子空间内。
*   **关键算法流程**：
    1.  利用伪随机数生成器（PRNG）动态构建层级低秩子空间。
    2.  **隐式计算**：利用张量结合律，在不显式构造巨大投影矩阵的情况下，直接在低维空间进行前向传播计算。
    3.  通过 CRNS 同步空间算子，计算标量损失的有限差分，得到伪梯度并更新参数。

#### 3. 实验设计
论文在多种极高维度和高阶 PDE 场景下进行了验证：
*   **高维 PDE 场景**：包括 Poisson、Allen-Cahn、Sine-Gordon 方程，以及半线性抛物线方程。测试维度从 $10^2$ 一直扩展到极端的 $10^7$（千万维）。
*   **高阶 PDE 场景**：包括 2D KdV 方程（含混合偏导）、2D KP 方程（4 阶）以及带梯度正则化的 g-KdV 方程。
*   **对比基准 (Benchmarks)**：
    *   **一阶方法 (FO)**：SDGD (Backward mode), Forward Laplacian, STDE (目前最强的随机 Taylor 估计器), HTE (Hutchinson 迹估计)。
    *   **零阶方法 (ZO)**：RS-PINN (基于随机平滑的 ZO 方法)。

#### 4. 资源与算力
*   **硬件平台**：所有实验均在单张 **NVIDIA A100 GPU (40GB)** 上完成。
*   **训练时长**：论文提供了迭代速度（it/s）对比。在 100 万维度下，SDZE 的速度约为 35.82 it/s，而所有一阶方法在 100 万或 1000 万维度下均因显存不足（OOM）而无法运行。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从低维到极高维（10M）、从低阶到高阶（4 阶）的广泛实验。
*   **消融实验**：详细验证了 CRNS 对抑制方差的必要性，以及子空间秩 $r$ 和更新频率 $F$ 对收敛性能的影响。
*   **客观性**：实验对比了多种最先进的基准方法，并使用了统一的 JAX 框架实现，确保了内存和速度对比的公平性。实验结果证明了 SDZE 在极端规模下的唯一可行性。

#### 6. 主要结论与发现
*   **突破显存限制**：SDZE 是首个能在单张 GPU 上训练千万维度、全连接且非权值共享 PINN 的框架。
*   **双重方差湮灭**：通过 CRNS 和子空间投影，成功解决了随机空间算子与零阶优化结合时的数值不稳定性。
*   **性能优势**：在同等维度下，SDZE 的显存占用远低于一阶方法，且由于避开了复杂的反向传播计算图编译，其运行速度在超高维度下具有显著优势。

#### 7. 优点
*   **理论完备**：提供了关于方差抵消、子空间梯度对齐以及非凸收敛性的严格数学证明。
*   **工程巧妙**：隐式子空间投影技术极大地节省了显存，使 $\mathcal{O}(1)$ 的内存占用成为可能。
*   **通用性强**：作为一个“元优化器”，SDZE 可以无缝升级现有的随机空间估计器（如 SDGD, STDE），使其具备处理超大规模模型的能力。

#### 8. 不足与局限
*   **收敛速度**：作为零阶优化方法，其收敛到高精度解所需的迭代次数通常多于一阶方法（Adam 等）。
*   **超参数敏感性**：子空间的秩 $r$ 和更新频率 $F$ 需要针对具体问题进行调优，缺乏自动化的自适应机制。
*   **算子限制**：虽然适用于通用 PDE，但对于某些具有特殊结构的算子，可能存在比通用随机采样更高效的特定优化手段。

（完）
